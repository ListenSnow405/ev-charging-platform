# 数据清洗基本流程操作手册

本文档将《数据清洗基本流程-完整版》整理为可重复执行的数据清洗标准操作流程。核心原则是先评估、后清洗，先定义规则、再批量执行，并通过前后对比、质量报告和版本存档保证结果可验证、可复现、可回溯。

## 1 核心流程

```text
数据探查 -> 质量评估 -> 制定清洗规则 -> 执行清洗 -> 清洗校验 -> 质量报告与存档
```

六个阶段的目标如下：

| 阶段 | 主要动作 | 必须产出 |
| --- | --- | --- |
| 1 数据探查 | 查看规模、字段、类型、分布、缺失、重复和异常 | 数据概况与初始统计 |
| 2 质量评估 | 按六个质量维度定位问题并排序 | 数据质量问题清单 |
| 3 制定规则 | 明确每类问题的检测条件、处理方式和理由 | 清洗规则清单 |
| 4 执行清洗 | 将规则脚本化并批量、可重复地执行 | 清洗数据、异常记录、执行日志 |
| 5 清洗校验 | 对比清洗前后指标并抽样复核 | 校验结果与未解决问题 |
| 6 报告与存档 | 记录处理数量、原因、版本和影响 | 数据质量报告、脚本与版本归档 |

## 2 开始前需要准备的内容

开始清洗前，至少确认以下信息：

- 原始数据源、文件或表名、数据时间范围和更新时间。
- 字段清单、字段含义、预期类型、允许为空情况和合法取值范围。
- 业务主键，例如订单号、用户 ID、设备编号；不要用不稳定字段强行去重。
- 单位、币种、时区、日期格式、码表和状态枚举的标准口径。
- 多数据源冲突时的权威来源和合并优先级。
- 关键字段、目标变量以及不得自动删除或修改的字段。
- 允许自动修正、需要人工复核和必须拒绝入库的边界。

原始数据应保留只读快照。大数据分层场景中保持 ODS 原始层不动，在 DWD 等加工层执行清洗。

## 3 阶段一 数据探查

### 3.1 读取与结构检查

1. 记录数据源、文件大小、表数量、总行数、总列数和时间范围。
2. 查看字段名、字段类型、样例值、空值形式和唯一值数量。
3. 识别混合类型，例如数值列混入 `"1,000"`、`"¥120"`、`"12%"`。
4. 检查字符编码、分隔符、换行、制表符、全角字符和不可见字符。
5. 检查主键是否为空、是否唯一，跨表外键是否能关联。

### 3.2 分布与质量概览

对每个字段至少统计：

- 缺失数、缺失率和常见伪缺失值，如空字符串、`NULL`、`N/A`、`-`。
- 最小值、最大值、均值、中位数、分位数和标准差。
- 类别频数、未知类别、低频类别和拼写变体。
- 完全重复数、主键重复数和疑似相似重复数。
- 格式合规率、越界数量、逻辑冲突数量和异常占比。
- 时间序列的频率、缺口、重复时间点、跳变和周期性。

异常检测前先用直方图、箱线图等观察分布，再用统计方法或业务规则确认，避免只看阈值误判。

## 4 阶段二 数据质量评估

从以下六个维度评估，并按影响范围、业务风险和修复难度排序：

| 维度 | 检查重点 | 常用指标 |
| --- | --- | --- |
| 完整性 | 应填字段是否缺失 | 缺失率 = 缺失数 / 总数 |
| 准确性 | 数据是否真实、是否存在明显错误 | 抽样正确率、异常占比 |
| 一致性 | 同一对象在不同字段、表或数据源中是否一致 | 冲突记录数 / 总数 |
| 时效性 | 数据是否按时更新、是否已经过期 | 更新滞后时长 |
| 唯一性 | 同一业务实体是否只有一条标准记录 | 重复率 |
| 有效性 | 数据是否符合格式、范围和业务规则 | 格式合规率、规则通过率 |

问题清单至少包含：问题编号、字段或表、问题类型、检测条件、影响数量、影响比例、典型样例、严重程度和建议处理方式。

## 5 阶段三 制定清洗规则

每条规则应明确以下内容：

| 字段 | 说明 |
| --- | --- |
| 规则编号 | 可稳定引用的唯一编号 |
| 作用对象 | 数据源、表、字段或记录范围 |
| 检测条件 | 可执行的判断表达式或查询条件 |
| 处理动作 | 修正、填充、合并、保留、标记待核或删除 |
| 处理依据 | 业务规则、数据字典、统计依据或权威来源 |
| 影响评估 | 预计影响行数、字段及下游任务 |
| 异常去向 | 人工复核表、隔离区或拒绝入库记录 |
| 验证方式 | 清洗后必须满足的断言或指标 |

统一处理原则：

- 能按确定规则修正的，自动修正并记录旧值和新值。
- 无法确定但仍有价值的，保留并标记为待核。
- 真实异常不因数值极端而自动删除，可保留并按需要变换。
- 删除是最后手段，必须记录删除数量、原因和样例。
- 关键字段的缺失、冲突或重复不得用通用规则一刀切处理。

## 6 阶段四 执行清洗

建议按以下顺序执行。先统一基础表示，再处理依赖业务含义的问题，可减少漏检和误判。

### 6.1 统一空值、编码和字符

1. 将约定的空字符串、`NULL`、`N/A`、`-` 等伪缺失值归一为空值。
2. 检测真实编码后统一为 UTF-8，不要在编码未知时直接反复转码。
3. 清理零宽字符、控制字符和其他不可见字符。
4. 去除首尾空格，压缩连续空格，规范换行与制表符。
5. 统一全角和半角字符、中英文标点以及必要字段的大小写。

### 6.2 转换数据类型与标准格式

- 日期字符串转换为时间类型，并统一输出为 `YYYY-MM-DD HH:MM:SS`。
- 数值转换前去除千分位、货币符号和百分号，明确百分比的换算规则。
- 金额使用 Decimal 或整数最小货币单位等定点表示，避免浮点误差。
- 身份证号、订单号、手机号等标识符按字符串保存，避免前导零或大数精度丢失。
- 布尔值将 `True/False`、`1/0`、`是/否`、`Y/N` 统一为一种标准形式。
- 类别值先按码表归一，再进行 One-Hot 等模型编码。

类型转换失败的值不要静默变为空值，应输出失败记录与原始值供复核。

### 6.3 处理缺失值

先判断缺失机制，再选择方法：

| 缺失机制 | 含义 | 建议处理 |
| --- | --- | --- |
| MCAR 完全随机缺失 | 与任何变量无关 | 可按缺失率删除或简单填充 |
| MAR 随机缺失 | 与其他可观测变量有关 | 基于其他变量做回归或多重插补 |
| MNAR 非随机缺失 | 与变量自身有关 | 单独建模、增加缺失指示器，并说明局限 |

缺失率可作为初步参考：

- `<5%` 且确认为 MCAR：可删除或简单填充。
- `5%–20%`：一般不直接删除，结合业务重要性选择插补方法。
- `>50%`：评估字段信息量，可剔除字段或将“是否缺失”作为特征。

常用处理方式：

- 数值型：均值适合近似正态分布；中位数适合偏态或含异常值的数据；也可使用回归、多重插补 MICE。
- 类别型：使用众数、单独的“未知”类别，或基于其他特征预测。
- 时间序列：使用前向填充、后向填充或线性插值；插值前后检查趋势和平稳性。
- 对有信息价值的缺失新增 `feature_X_is_missing` 等缺失指示器，同时保留处理后的原字段。

关键字段如业务主键、金额和目标变量缺失时，优先按专门规则处理，不得直接套用普通填充。

### 6.4 处理重复值

1. 完全重复：所有字段相同，保留一条。
2. 主键重复：同一业务主键出现多条，按业务规则确认后处理。
3. 部分字段重复：先判断是重复录入，还是正常的一对多明细。
4. 相似记录重复：姓名、电话、地址近似时，可用编辑距离或相似度阈值识别。

保留记录时，按预先定义的优先级选择信息最全、时间最新或质量最高的一条，并记录被合并或删除的记录。不要把同一用户多次下单、同一订单多行明细等正常业务数据当作重复值。

### 6.5 处理异常值

常用检测方法：

- IQR：小于 `Q1 - 1.5 × IQR` 或大于 `Q3 + 1.5 × IQR`。
- Z-score：近似正态分布时，`|Z| > 3` 可作为异常候选。
- Isolation Forest：用于多维异常检测。
- 业务规则：例如年龄大于 150、金额小于 0。

检测结果只是异常候选，必须继续判断异常类型：

| 异常类型 | 示例 | 处理方式 |
| --- | --- | --- |
| 录入错误 | 年龄 999 | 有依据则修正，否则删除或待核 |
| 单位错误 | 应为万元却填成元 | 按单位规则换算并记录 |
| 真实异常 | 合法账户金额极高 | 保留，必要时做对数等变换 |
| 测量误差 | 传感器瞬时跳变 | 平滑、插值、隔离或删除 |

### 6.6 统一口径并校验逻辑

- 统一单位、币种、时区、字段名、分类标签和状态枚举。
- 使用码表统一同义表示，例如省份简称、性别代码、支付状态。
- 校验数值范围，例如年龄 `0–120`、百分比 `0–100`、金额和数量不小于 0。
- 校验字段关系，例如年龄与出生日期一致、订单金额等于明细合计。
- 校验时间顺序，例如下单时间不晚于支付、发货和签收时间。
- 校验跨表主外键和同一实体在多源数据中的一致性。
- 冲突时使用已定义的权威来源；无法判断时进入待核队列，不擅自覆盖。

### 6.7 清洗文本数据

- 用正则表达式提取邮箱、手机号、URL、金额等结构化信息。
- 删除或替换 HTML 标签、URL、表情、特殊符号和重复标点等噪声。
- 规范全角半角、中文标点和多余空白。
- 按任务需要处理繁简转换、分词、同义词和别称归一、常见错别字。
- 文本建模时按需要处理停用词和低频噪声词。
- 识别广告、重复刷屏和无意义文本，先标记再决定是否剔除。

### 6.8 清洗时间序列数据

- 统一时间格式和时区，按时间排序并处理重复时间点。
- 按业务频率建立完整时间索引，识别缺失时间点。
- 根据业务含义选择前向填充、后向填充或线性插值。
- 用滑动均值、中位数或差分阈值检测毛刺与跳变。
- 按日、周、月检查连续性和周期性，识别异常周期与数据缺口。
- 明确处理夏令时、闰秒和跨时区事件对齐。

### 6.9 大数据和流式场景

- TB/PB 级数据使用 Spark、Hive、Flink 等分布式工具，不依赖单机 Pandas 全量处理。
- 先采样探查，再对全量数据统计关键质量指标。
- 将规则写成参数化、可重跑的数据处理任务并纳入调度系统。
- 流式数据在接入时使用 Flink、Kafka Streams 或规则引擎在线校验。
- 记录数据血缘、规则版本、执行时间、输入输出版本和影响行数。

## 7 阶段五 清洗校验

清洗完成不等于任务完成。至少执行以下验证：

### 7.1 结构与数量

- 输出行数、列数和字段顺序符合预期。
- 字段类型、精度、编码和日期时区符合数据字典。
- 主键非空且唯一，外键关联完整。
- 删除、合并、新增和待核记录的数量能够对账。

### 7.2 质量指标前后对比

- 缺失率、重复率、异常占比和格式合规率达到规则目标。
- 逻辑冲突、越界值和类型转换失败数量为 0，或均进入已说明的例外清单。
- 关键统计量和分布没有出现无法解释的剧烈变化。
- 时间序列没有新增缺口、错序或不合理跳变。

### 7.3 抽样与业务验证

- 对每类规则抽样查看原值、处理后值和处理原因。
- 重点复核关键字段、被删除记录、自动修正记录和多源冲突记录。
- 执行业务断言，例如金额合计、库存关系、时间顺序和状态约束。
- 在相同输入和规则下重复运行，结果应一致；可重复执行的脚本不应不断产生新变化。

任何误删、误改、关键指标异常漂移或对账失败，都应停止发布，回到规则阶段修正。

## 8 阶段六 数据质量报告与存档

最终报告至少包含：

1. 数据概况：数据源、规模、时间范围、字段清单和输入版本。
2. 质量评估：六维度结果及缺失率、重复率、格式合规率等指标。
3. 问题清单：每类问题的数量、占比、严重程度和典型样例。
4. 处理动作：采用的规则、处理数量，以及删除、填充、修正和合并明细。
5. 清洗前后对比：关键指标、分布和业务断言的变化。
6. 遗留问题：无法自动处理的记录、风险及人工复核建议。
7. 版本信息：脚本版本、规则版本、执行时间、执行人、输入输出位置和数据血缘。

需要共同归档：原始数据只读快照、规则清单、清洗脚本、运行参数、执行日志、待核记录、清洗结果和质量报告。

## 9 常用操作速查

| 场景 | Pandas | SQL |
| --- | --- | --- |
| 查看缺失 | `df.isnull().sum()` | `COUNT(*) - COUNT(col)` |
| 填充缺失 | `df.fillna(...)` | `COALESCE(...)` 或条件更新 |
| 去除重复 | `df.drop_duplicates(...)` | `DISTINCT` 或 `ROW_NUMBER()` |
| 去除空格 | `df["x"].str.strip()` | `TRIM(col)` |
| 类型转换 | `df["x"].astype(...)` | `CAST(col AS ...)` |
| 条件替换 | `df.replace(...)` | `CASE WHEN ...` |
| 筛选异常 | IQR 或 Z-score 条件 | `WHERE` 业务或统计条件 |

这些命令只负责执行，不代替业务规则判断。任何填充、去重和删除操作都应先明确条件、保留策略和验证方式。

## 10 最终验收清单

- [ ] 原始数据已保留，只在副本或加工层执行清洗。
- [ ] 已完成字段、类型、分布、缺失、重复和异常探查。
- [ ] 已按完整性、准确性、一致性、时效性、唯一性、有效性评估。
- [ ] 每类问题都有可执行、可解释、可验证的规则。
- [ ] 缺失值先判断机制，异常值先判断类型，重复值先判断业务语义。
- [ ] 编码、空白、格式、单位、码表、类型和时区已经统一。
- [ ] 关键字段、主外键、数值范围、字段关系和时间顺序已校验。
- [ ] 删除、修正、填充、合并和待核数量均可追溯。
- [ ] 清洗前后质量指标和关键分布已对比。
- [ ] 已抽样复核，并通过业务断言和对账检查。
- [ ] 脚本可重复执行，相同输入能得到一致结果。
- [ ] 已生成质量报告并归档规则、脚本、日志、版本和数据血缘。

## 11 后续调用模板

后续可将下面内容连同待清洗数据或路径一起提交：

```text
请按照《数据清洗基本流程操作手册》处理以下数据。

数据位置：
数据用途：
业务主键：
关键字段：
标准单位、时区和日期格式：
合法取值或状态码：
多源冲突时的权威来源：
允许自动修正的范围：
必须人工确认的操作：
期望输出位置与格式：

请依次完成数据探查、六维度质量评估、规则清单、清洗执行、前后校验和质量报告。
保留原始数据，不要静默删除或覆盖；无法确定的记录进入待核清单。
```

## 12 最简记忆版

```text
先留原始快照
再做探查和六维评估
先定规则再执行
先统一表示和类型，再处理缺失、重复、异常、一致性和逻辑
不能确定就待核，删除必须留痕
最后做前后对比、抽样验证、对账、报告和版本存档
```

来源：项目根目录《数据清洗基本流程-完整版.docx》。
