# ---- 本地密钥与配置（禁止提交）----------------------------------------------
# 腾讯位置服务 Key 等凭据只存本地，仓库里只保留 .example 模板
config/*
!config/*.example
!config/.gitkeep
*.key
.env

# ---- Qt / C++ 构建产物 -------------------------------------------------------
build/
build-*/
*.o
*.so
*.a
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h
Makefile
Makefile.*
*.pro.user
*.pro.user.*
.qmake.stash

# ---- 运行期数据（数据库由 db-schema.sql 生成，不入库）--------------------------
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
logs/
*.log

# ---- Python（ml/ 与 dataviz/）------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
# 第二阶段独立虚拟环境（Python 3.11 + PySpark）；与一阶段的 .venv（3.10）并存
.venv-phase2/

# ---- 第二阶段数据分层产物（均由脚本重生成，不入库）----
# ODS 原始层：bigdata/spark/export_ods.py 从 charging.db 导出，产物为 444 只读
bigdata/ods/*.csv
bigdata/ods/_manifest.json
# DWD 清洗层：parquet 体量大且可由 cleaning.py 重生成
bigdata/dwd/
#
# 注意：bigdata/quality/** 与 bigdata/analysis/** **刻意不忽略**。
# SOP 第 8 节要求归档「规则清单、清洗脚本、执行日志、待核记录、清洗结果和质量报告」，
# 这些就是那份归档本身——是答辩证据，不是临时产物。体量也小（最大 60 行）。
# 代价是每次重跑会因 generated_at 变化产生 diff，可接受。
*.pkl
*.joblib

# ---- 个人工作文档（不入库）----------------------------------------------------
# L5 的任务规划与上下文交接，只对本人有意义，团队公开信息在 docs/conventions.md
L5-PLAN.md

# ---- 编辑器与系统 ------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store

# ---- 报告导出件（由服务端 3744 生成，运行期产物）----
export/

# ---- 大屏运行期快照（由 ml/export_snapshot.py 生成）----
dataviz/data/*.json

# ---- 模型产物（由 ml/train_forecast.py 训练，含 meta.json，随训练重生成）----
ml/data/models/

# ---- 历史数据生成器产物（由 ml/gen_history.py 生成）----
ml/data/*.csv
# 播种记录与各人的开发库绑定，且每次重播全量重写（约 60KB），不入库。
# 它的作用是让 --reset 能精确删除本脚本播种的行，见 ml/CLAUDE.md。
ml/data/seed_manifest.json

# ---- 第二阶段前端 ----
bigdata/web/node_modules/
bigdata/web/dist/
