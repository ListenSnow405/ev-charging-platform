# AGENTS.md — 通用 Agent 说明（Codex 等）

> **本文件不重复正文内容。项目的 agent 上下文以 [CLAUDE.md](CLAUDE.md) 为唯一事实来源。**
>
> 开始任何工作前，**先完整阅读 [CLAUDE.md](CLAUDE.md)**，再回到本文件看工具相关的补充说明。

## 为什么是两份文件

组内成员使用的 agent 工具不同：Claude Code 默认读取 `CLAUDE.md`，Codex 等工具默认读取 `AGENTS.md`。两份文件的**唯一目的**是让不同工具都能进入同一份上下文。

因此本文件只写工具差异，**不复制技术基线、目录归属、编码规范、避坑规则**——那些一律看 `CLAUDE.md`。

> ⚠️ **禁止把 CLAUDE.md 的内容抄一份到这里。** 两份长文档必然随时间分叉，那正是本项目要防的事故：五个 agent 读到不同的事实，生成五套互不兼容的实现，而且每套都能编译通过。若发现两份说法冲突，**一律以 CLAUDE.md 为准**，并按变更流程修正本文件。

## 会话开始必读清单

按顺序读完这四份，再动手：

1. [CLAUDE.md](CLAUDE.md) —— 技术基线、目录归属、五条硬性规则、业务规则要点
2. [docs/protocol.md](docs/protocol.md) —— 通信协议（冻结契约）
3. [docs/db-schema.sql](docs/db-schema.sql) —— 数据库结构（冻结契约）
4. [DIVISION-OF-LABOR.md](DIVISION-OF-LABOR.md) —— 自己负责哪条线、第二顶帽子是什么

涉及网络收发的任务，额外读 `common/frame.h`。

## 标记约定速查

项目全部文档与代码注释使用两个标记，详见 CLAUDE.md 第 0 节：

- `[说明书]` —— 说明书正文明文要求，**不得擅自更改**
- `[本组自定]` —— 说明书未规定，本组决定，可走变更流程调整

**生成代码时如果需要引入新的技术选择、表字段或协议字段，必须在注释里标注属于哪一类**，不要含糊带过。

## 硬性边界（与 CLAUDE.md 第 3、4 节一致）

- 只修改自己归属目录下的文件
- `docs/protocol.md`、`docs/db-schema.sql`、`common/**` 是**只读契约**，非属主不得修改
- 需要改契约 → 群里提出走变更流程，不要为了让自己这边编译通过就改接口

## Codex 使用补充

- **上下文不会自动携带。** 新开一个会话时，Codex 只会读到本文件；请在首轮对话中明确让它打开并读取上面「必读清单」里的文件，否则它不知道协议与表结构。
- **审批模式**：涉及 `docs/`、`common/`、`CLAUDE.md`、`AGENTS.md` 的写操作一律人工确认后再执行；自己目录下的常规编辑可放开。
- **不要让 agent 直接执行 `git push`。** 提交由本人确认后手动推送。
- **契约变更通知后必须重开会话或重新读取契约文件**——旧会话里缓存的是过期的协议，人知道了不等于 agent 知道了。

## 其他工具

若后续引入其他 agent 工具，同样在此文件追加一节工具差异说明，正文仍指向 `CLAUDE.md`。
