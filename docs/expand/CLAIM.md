# 扩展模块认领看板

> 规则见 [00-扩展功能模块实施路径推荐.md](00-扩展功能模块实施路径推荐.md) 第 5 节。
> **不登记不算认领。** 改本文件时只改自己那一行，减少 rebase 冲突。

## 门禁状态

> 下表状态经 **2026-09-09 实测核对**（`grep registerHandler server/`、`server/main.cpp` 注册清单、`common/protocol.h`），不是文档推断。

| 门禁 | 内容 | 状态 |
| --- | --- | --- |
| R0 | 1203/1204/1205 · 9001–9006 · 2112 · 2305 · 1208 | ✅ **已通过**。核心 35 个命令字全部注册；充电三步在 `order_flow_service.cpp`，2112→9003 重启链路在 `pile_service.cpp`，1208 走 `user_registry` 推送 |
| B1 | 服务端定时任务 | ◐ 会话过期清理已接上（`tcp_server.cpp` 每 60s 调 `sweepExpired()`）；通用定时任务框架仍未抽出 |
| B2 | 服务端主动推送通道 | ✅ `device_registry`（→电桩）与 `user_registry`（→用户）均已就绪，1208 已在其上跑通 |
| B3 · B4 | ext 建表 · 功能开关 | ✅ 2026-09-07 完成，2026-09-08 双双追认。B3：`build-all.sh` 每次构建按文件名顺序执行全部 ext SQL；B4：`main.cpp::FeatureFlags` 一次查询 + 启动汇总日志 |

**R0 门禁已解除，第一批扩展模块（01、02、08）可以开工。** 03/04/06 依赖 02 与通用定时任务框架，
按 00 第 5.4 节挂阻塞即可，**不要自己造定时器或旁路 socket**。

**模块被卡 ≠ 切片被卡。** 05 的建表 + 管理端时段价格配置页、07 的评价闭环
（`t_station_review` 已在冻结 schema 内，6477 单已结算订单可直接评）、02 的模拟器侧场景脚本
（`tools/` 本就是 L1 自己的目录）都可先行推进，仍须先在认领表登记。

## 认领表

| # | 模块 | 认领人 | 认领日 | 当前步骤 | 阻塞 | 开关键 |
| --- | --- | --- | --- | --- | --- | --- |
| 01 | 权限与审计中心 | — | — | 未认领 | 无（R0 已通过；是 03/05/07 的前置，2–3 日） | `feat_01_perm` |
| 02 | 设备协议模拟与数字孪生 | — | — | 未认领 | 无（R0 已通过） | `feat_02_twin` |
| 03 | 设备告警与运维工单 | — | — | 未认领 | 02、B1（定时任务框架） | `feat_03_alert` |
| 04 | 智能排队与候补预约 | — | — | 未认领 | B1（定时任务框架） | `feat_04_queue` |
| 05 | 分时计价与营销中心 | — | — | 未认领 | 无（R0 已通过） | `feat_05_price` |
| 06 | 智能充电与站点能源管理 | — | — | 未认领 | 02、B1（定时任务框架） | `feat_06_energy` |
| 07 | 客服评价与申诉 | — | — | 未认领 | 01（评价切片不受阻） | `feat_07_support` |
| 08 | 碳减排与能源报告 | **L5** | 2026-09-07 | ✅ **全部完成并追认**（S1–S5，8 个命令字 + 管理端 + 大屏 + 对拍）；2026-09-08 L1/L2/L3 授权后完成跨目录收尾：`errMsg` 扩展文案挂钩、B4 批量开关、导航成对登记 | 无 | `feat_08_carbon` |
| 09 | 企业车队管理 | — | — | 原则上不做 | 01、05 | `feat_09_fleet` |
| 10 | 车网互动 V2G 实验 | — | — | 原则上不做 | 02、06 | `feat_10_v2g` |

「当前步骤」写模块文档第 6 节的步骤号，例如 `步骤 3 / 共 7`。

## 底座任务（不占认领名额）

| 编号 | 内容 | 主责 | 前置 | 状态 |
| --- | --- | --- | --- | --- |
| B1 | 服务端定时任务框架 | L1 | 无 | ◐ 会话清理定时器已接（`tcp_server.cpp` 每 60s）；03/04/06 要的通用框架仍未抽出 |
| B2 | 服务端主动推送通道（`token/pileCode → fd`） | L1 | 无 | ✅ `device_registry` + `user_registry`，1208 已在其上跑通 |
| B3 | `build-all.sh` 自动执行 `docs/db-schema-ext-*.sql` | L3 | **无** —— 08 一建表就要用 | ✅ 正式完成；2026-09-08 L3 已追认；08/L5 代实现，L3 integration 已验证 ext SQL 双遍幂等及扩展开启时核心回归 |
| B4 | `t_sys_config` 功能开关读取与条件注册 | L2 | 首个扩展模块注册 handler 之前 | ✅ 正式完成；2026-09-08 L2 已追认。`server/main.cpp::FeatureFlags` 一次查询读回全部 `feat_*` 并缓存，启动打一行开关汇总；读不到按关闭处理并记 `LOG_E` |
| B5 | 动作—权限注册表（并入模块 01） | 01 认领人 | 模块 01 启动 | ⬜ |

## 段号占用登记

新增命令字/错误码后在此登记，L1 每周复核一次。段的划分见 00 第 4.3 节。

| 模块 | 已占用命令字 | 已占用错误码 | 已建表 | ext 脚本 |
| --- | --- | --- | --- | --- |
| 01 | — | — | — | ⬜ `docs/db-schema-ext-01.sql` |
| 02 | — | — | — | ⬜ `docs/db-schema-ext-02.sql` |
| 03 | — | — | — | ⬜ `docs/db-schema-ext-03.sql` |
| 04 | — | — | — | ⬜ `docs/db-schema-ext-04.sql` |
| 05 | — | — | — | ⬜ `docs/db-schema-ext-05.sql` |
| 06 | — | — | — | ⬜ `docs/db-schema-ext-06.sql` |
| 07 | — | — | — | ⬜ `docs/db-schema-ext-07.sql` |
| 08 | 3740–3748（**9 个全部实现**） | 6701–6707 | `t_carbon_factor` `t_carbon_daily` `t_carbon_report` | ✅ `docs/db-schema-ext-08.sql`；运行手册 [08-运行手册.md](08-运行手册.md) |

## 放弃 / 移交记录

| 日期 | 模块 | 原认领人 | 停在哪一步 | 已合入什么 | 遗留什么 |
| --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — |
