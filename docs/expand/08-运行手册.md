# 08 · 碳减排与能源报告 —— 运行手册

> 认领人 **L5**，开工 **2026-09-07**，S1–S5 于 **2026-09-08 全部完成并获 L1/L2/L3 追认**。
> 任务规格见 [08-碳减排与能源报告.md](08-碳减排与能源报告.md)，底座与分段规则见
> [00-扩展功能模块实施路径推荐.md](00-扩展功能模块实施路径推荐.md) 第 4、5 节。
> 本文是**已交付模块的运行与维护文档**：怎么开关、怎么验、口径是什么、当初为什么这么定。

## 0 交付范围

全栈：`docs/db-schema-ext-08.sql` 建表 + 服务端 9 个命令字（3740–3748）+ 管理端「碳排放报告」页
+ 碳排放大屏 + CSV/HTML 导出 + Python 独立对拍。

跨目录改动均事前取得授权（2026-09-07 L2/L3，2026-09-08 L1）并做成**独立提交**，接缝清单见第 8 节。
顺带落地了两项底座：**B3** `build-all.sh` 按文件名顺序执行全部 `docs/db-schema-ext-*.sql`（每次构建都跑，脚本幂等）、
**B4** `server/main.cpp::FeatureFlags` 一次查询读回全部 `feat_*` 开关并缓存，启动打一行汇总，
读不到按关闭处理并记 `LOG_E`。两项均已由原主责 L3 / L2 追认。

## 0.1 运行

建表随 `bash scripts/build-all.sh` 自动执行，无需手工建库。开关在 `t_sys_config`：

```bash
sqlite3 charging.db "UPDATE t_sys_config SET value='0' WHERE key='feat_08_carbon';"   # 关
sqlite3 charging.db "UPDATE t_sys_config SET value='1' WHERE key='feat_08_carbon';"   # 开
```

开关改完**要重启服务端**（启动时读一次并缓存）。关闭时 handler 不注册，管理端页面显示「该功能未启用」，
核心闭环不受影响——这一点由 `scripts/test-carbon-integration.sh` 每次验证。

验证入口（详见 [../RUNBOOK.md 第 5 节](../RUNBOOK.md)）：

| 命令 | 验什么 |
| --- | --- |
| `bash scripts/test-carbon-calc.sh` | 计算核心 61 项手算断言，不连库不起服务，秒级 |
| `python3 scripts/smoke-carbon.py` | 9 个命令字的协议契约、鉴权门、幂等、错误码 |
| `.venv/bin/python ml/carbon_crosscheck.py` | 从 `t_order` 独立重算，与 `t_carbon_daily` 逐格比对，不一致退出码 2 |
| `bash scripts/test-carbon-integration.sh` | 隔离式集成：ext SQL 双遍幂等、开关双向、扩展开启时核心无回归 |

聚合是**懒聚合**：3740 查询时按日核对 `order_cnt`，不一致就地重算该日，今日永远重算；
另有 3745 显式重算。**不依赖定时任务**（B1 未做，且 00 §5.4 明令禁止各模块自造定时器），
代价是首次查询略慢，8292 单量级无感。

## 1 设计裁决记录

开发中与文档不一致或实测发现的问题，均在此定案。**改动本模块前先读这张表**——大部分「看起来不对」的行为都在这里有理由。

| # | 冲突 | 裁决 | 理由 |
| --- | --- | --- | --- |
| D1 | 08 §5.0 要求「基础指标由 SQL/C++ 完成，Python 只做图表」；CLAIM.md 的豁免说「离线聚合 = 零接缝」 | **聚合在 C++**。Python 侧只做两件事：只读导出大屏快照、**独立重算一遍做对拍** | 两份文档都满足，还白赚一层对账。Python 全程只读，`ml/CLAUDE.md` 的权限规则一个字都不用改 |
| D2 | 08 §5.0 说峰平谷时段读 `t_sys_config`；但该表只有 4 个键，且 `ml/CLAUDE.md` 禁止 L5 写它 | 由 `docs/db-schema-ext-08.sql` 用 `INSERT OR IGNORE` 种子键值 | 00 §4.4 明文允许 ext 脚本 `INSERT OR IGNORE`；这是唯一被批准的写法。运行期仍只读 |
| D3 | 08 文档要求「跨午夜订单切片后计算」，但又要求 `date(settle_time)` 按日聚合 | **日归属按 `date(settle_time)`（整单归一天），峰平谷按真实钟点切分（可跨两天）** | 日总量必须与 2301/2302 营收同源，否则大屏与管理端并排是两个形状；而分时口径讲的是「电在哪个时段用掉的」，必须按真实时间。**实测 553 单（8.5%）跨午夜，这条不写下来会被当成 bug** |
| D4 | 08 文档要求定时聚合（依赖 B1 定时任务，主责 L1，未做） | **懒聚合**：3740 查询时按日核对 `order_cnt`，不一致就地重算该日；另给 3745 显式重算 | 不自己造定时器（00 §5.4 明令禁止），也不阻塞在 B1 上。代价是首次查询慢一点，8292 单量级无感 |
| D5 | 6701–6704 是扩展错误码，而 `buildResponse()` 写死 `errMsg(code)`，客户端会收到「未知错误(6701)」 | **已于 2026-09-08 经 L1 授权解决**：`common/error_code.h` 加 `ExtMsgProvider` 函数指针挂钩，`server/main.cpp` 启动时 `registerExtMsgProvider(&errMsgExt)`。冻结契约**不 include** `error_code_ext.h`，避免 L1 的契约被各扩展模块的进度绑架；未注册时行为与从前完全一致 | 原方案（页面本地映射）只能救 Qt 客户端，脚本与调试工具只看 `msg`。挂钩是加法、向后兼容、对所有扩展模块通用。页面本地映射保留为兜底（对旧版服务端仍显示人话，且能给出贴合本页的指引）|
| D12 | 停用的因子行留在表里，`effect_to` 停在被停用的那一刻不再跟随时间线；而撤销时前驱的生效止会被恢复到同一时刻 —— 于是「两个因子闭合在同一时刻」，`factorClosedAt()` 判为破损，撤下一个因子直接 5001。对已停用因子再点一次撤销还会**重跑时间线恢复**，把一条完好的时间线改出重叠 | **时间线只由启用因子构成**：`loadFactors()` 只读 `enabled = 1`，撤销/彻底删除的前驱查找都走这一份；3747 对已停用因子改为**幂等空操作**（`alreadyDisabled=true`，不写库）；管理端按行的启用状态给「撤销」按钮置灰。想让停用行彻底消失走 3748 | 停用行是历史存根，只为「历史结果里的 `factor_version` 还查得到当时的 g/度与来源」而存在，本就不该参与任何区间判定。把它算进时间线是把存档和现役混为一谈 —— 2026-09-10 那个 5001 就是这么来的 |
| D11 | 因子版本设计为不可变（历史行靠 `factor_version` 追溯），但「刚加错了」是真实场景，没有撤销手段就只能上裸 SQL —— 实际也确实发生了 | **3747 按引用情况分两条路**：无 `t_carbon_daily`/`t_carbon_report` 引用 → 物理删除；已被引用 → 停用（`enabled=0`）保留行。两条路都恢复时间线；拒绝撤销最后一个启用因子 | 从没影响过任何结果的因子，删了不留痕也无所谓；已经影响过结果的，行必须留着，否则那些历史结果里的 `factor_version` 就查不到对应的 g/度 与来源，追溯链断掉。**不恢复时间线的话，撤销会在时间线上留一个洞，洞里的日期查询直接 6701** |
| D10 | `factorOverlaps` 只在**同区域**内查重叠，而 `pickFactor` **完全不看区域** —— 换个区域名就能塞进时间上重叠的因子（实测：加一个「华东电网」因子，强度从 581 变成 999，历史数字被悄悄改掉）| **重叠校验改为全局**。本系统只有一条因子时间线：`t_carbon_daily` 没有区域列、`t_station` 也没有，`region` 纯粹是因子上的描述性标签 | 要么让选择也认区域（需要站点带区域维度，超出范围且表结构不支持），要么让校验不认区域。后者才与现有数据模型自洽。**自主测试阶段发现并定案** |
| D9 | 种子因子是 `[2000-01-01, 无穷)`，在 D10 的全局时间线下，**任何新因子都永远加不进去** —— 3742 出厂即不可用 | **支持「接续发布」**：新因子起点晚于某个开区间因子的起点时，不判冲突，而是在同一事务里把旧因子闭合到新因子起点。响应回 `supersededFactorId` | 排放因子在现实中就是接续发布的（2023 版一出，2022 版自然截止）。接续点**之前**的历史数字完全不变，之后的日期改用新因子重算，覆盖这些日期的报告转 `STALE` —— 正是发布新因子应有的效果。往已闭合区间里插队仍拒为 6702。**自主测试阶段发现并定案** |
| D8 | `STALE` 报告导出时，每日明细该取哪一批数据 —— 报告行只存总计，明细只能来自 `t_carbon_daily` 的**当前**数据 | **过期报告只导总计快照，省略每日明细**，并在文件头写明原因；`READY` 报告才附明细（此时两者同源） | 把生成当时的总计与现在的明细并排放进一张表，两批数字加起来对不上，看的人只会更糊涂。**S5 开发中发现并定案** |
| D7 | 3742 的参数表原列 `reqId` 做写幂等，但 `t_carbon_factor` 没有 `req_id` 列，而它已有 `UNIQUE(region, version)` | **用 `(region, version)` 作写幂等键，不收 `reqId`**。同版本内容一致 → 幂等返回原 `factorId`；内容不同 → `ERR_PARAM`，要求发新版本号 | 因子版本一经写入即不可变，是不变量 6 的前提 —— `t_carbon_daily` 与 `t_carbon_report` 都靠 `factor_version` 追溯当时用的是哪个数，允许就地改内容等于让所有历史结果的出处凭空变样。**S4 开发中发现并定案** |
| D6 | 08 §5.3 要求「跨因子生效时间的订单必须切片后再计算」，但 `t_carbon_daily` 一行只记一个 `factor_version`，且它是 UNIQUE 键的一部分 —— 一天横跨两个因子版本时一行装不下 | **因子生效边界必须对齐自然日 `00:00:00`**，3742 写入前校验，不合法返回 `ERR_PARAM`。于是按日选因子即天然精确，日内切片这件事不存在 | 比「一行塞两个版本」或「日内切片再合并」都简单得多，且对一个按年发布的电网因子毫无实际损失。**S2 开发中发现并定案** |

## 2 冻结口径

**改动任何一条都要同时改 `ALGO_VERSION`**（见本节末），并同步 08 文档的命令字登记表。

**单位**（00 §4.5）：电量 `kwh_x100` 整数；因子 `factor_g_per_kwh` 整数克/度；排放 `emission_g` 整数克；
强度 `intensity_g_per_kwh` 整数克/度；完整度与估算占比整数 0..100，**总量为 0 时一律 `-1` 表示「不适用」**，不伪装成 100。

**算式**（全整数，四舍五入用 `+除数/2` 而非浮点）：

```
emission_g          = (kwh_x100 * factor_g_per_kwh + 50) / 100
intensity_g_per_kwh = (emission_g * 100 + total_kwh_x100 / 2) / total_kwh_x100     // total=0 → -1
completeness        = (100 * (total_kwh_x100 - unalloc_kwh_x100) + total/2) / total // total=0 → -1
```

中间值一律 `qint64`：实测全量 `kwh_x100` 合计 36,638,035，乘 581 约 2.1×10^10，**32 位必溢出**。

**防脏数据的两个上限** `[本组自定]`：单笔电量 `KWH_X100_MAX = 1e11`（10 亿度）、
单次会话 `SESSION_SEC_MAX = 7 天`。超限整单判 `UNAVAILABLE` 不强行分摊，
既挡住 `qint64` 边界，也避免一条脏时间把按日推进的重叠计算拖成死循环。

**总排放取逐行之和，不由总电量重算** `[本组自定]`：两者会差几克（各行各自四舍五入，
实测全量 60 天 × 6 站 = 360 行仅差 2 g，上限为行数 × 0.5 g），但「表格各行相加 = 总计」
才是看报表的人真正会去验的那条等式，优先保它。

**算法版本 `ALGO_VERSION = "carbon-v1"`**：写进 `t_carbon_daily` 的 UNIQUE 键。
**改动任何算式或口径都必须同时改它** —— 旧结果因此得以保留而不是被静默覆盖。

**分时切分**（守恒，最大余数法）：单笔订单按 `[start_time, end_time)` 与三类时段的**重叠秒数** `d_peak/d_flat/d_valley` 分摊：
`raw_i = kwh_x100 * d_i / D` 整除，余数最大者优先领取 `kwh_x100 - Σraw_i` 的剩余单位，
并列时固定顺序 峰 > 平 > 谷。**保证 `peak + flat + valley + unalloc ≡ total`，逐单成立，聚合后也成立。**

**固定时段** `[本组自定]`，模块 05 未做时的降级口径，页面与导出**必须标注「固定时段口径」**：

| 段 | 区间（左闭右开，本地时间） | 时长 |
| --- | --- | --- |
| 峰 | `10:00–15:00`、`18:00–21:00` | 8h |
| 平 | `07:00–10:00`、`15:00–18:00`、`21:00–23:00` | 8h |
| 谷 | `23:00–07:00` | 8h |

**因子时间线**：全局唯一，`region` 只是描述性标签、不参与选择（裁决 D10）；新版本以「接续」方式发布，自动闭合上一个开区间因子（裁决 D9）。

**演示因子**：`581 g/kWh`，region `全国电网平均`，version `demo-2022`，
source「生态环境部 2022 年全国电网平均二氧化碳排放因子 0.5810 tCO2/MWh，**课程项目演示用，非认证数据**」，
`effect_from = '2000-01-01 00:00:00'`，`effect_to = NULL`（右开无穷）。生效区间**左闭右开**，重叠由服务端在 3742 拒绝。

**数据质量**：`t_order` 无表计区间数据，MVP 中 `METER_INTERVAL` **永不出现**（列保留，文档注明未使用）。
`start_time`/`end_time`/`kwh_x100` 齐全且 `end > start` → `UNIFORM_ESTIMATE`；否则 → `UNAVAILABLE`，
该单电量整体计入 `unalloc_kwh_x100` 并拉低完整度。实测当前 6477 单**零缺失**，因此完整度恒为 100 ——
夹具库里必须**人为造缺失单**，否则降级路径根本没被验过。

**免责声明**（页眉、导出文件头、快照 JSON 三处同一份字符串）：
`课程项目估算，非认证碳数据，不可用于碳交易或监管申报`。

## 3 数据模型 · `docs/db-schema-ext-08.sql`

只允许 `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` / `INSERT OR IGNORE`（00 §4.4），**幂等，可重复执行**。

| 表 | 关键列 | 约束 |
| --- | --- | --- |
| `t_carbon_factor` | `factor_id` PK, `region`, `effect_from`, `effect_to`, `factor_g_per_kwh` INTEGER, `source`, `version`, `enabled`, `create_time` | `UNIQUE(region, version)`；区间重叠由服务端校验（SQLite 表约束表达不了区间重叠） |
| `t_carbon_daily` | `station_id`, `stat_date`, `total_kwh_x100`, `peak/flat/valley/unalloc_kwh_x100`, `order_cnt`, `emission_g`, `intensity_g_per_kwh`, `completeness`, `estimated_pct`, `quality`, `factor_version`, `algo_version`, `cutoff_time`, `update_time` | **`UNIQUE(station_id, stat_date, factor_version, algo_version)`** + `INSERT OR REPLACE` = 幂等重算 |
| `t_carbon_report` | `report_id` PK, `scope`, `station_id`, `date_from`, `date_to`, `version`, `status`, `run_time`, `cutoff_time`, `factor_version`, `algo_version`, 指标快照列, `output_path`, `req_id` | `UNIQUE(req_id)`（00 §4.6 幂等）；`status ∈ GENERATING/READY/FAILED/STALE` |

`station_id = 0` 在 `t_carbon_daily` 中**不使用**——全站数字一律由分站行求和得到，避免两份可能对不上的总量。

种子（`INSERT OR IGNORE`）：演示因子一行；`t_sys_config` 三个键 `feat_08_carbon=1`、
`carbon_peak_ranges`、`carbon_valley_ranges`（平段 = 24h 扣掉峰谷，不单独配，杜绝三者配不满 24 小时）。

## 4 命令字与错误码（段内自治，无需 CR）

命令字段 `3700–3799` 中的管理端子段 `3740–3779`；错误码段 `6700–6799`。写入新建的
`common/protocol_ext.h` / `common/error_code_ext.h`（00 §5.3，非冻结文件，只加不改）。

| 命令字 | 常量 | `data` 入 → 出 |
| --- | --- | --- |
| 3740 | `CMD_EXT_CARBON_METRIC` | `{stationId, dateFrom, dateTo}` → `{list[], totals, factorVersion, algoVersion, cutoffTime, completeness, tariffMode, disclaimer}` |
| 3741 | `CMD_EXT_FACTOR_LIST` | `{}` → 因子列表 |
| 3742 | `CMD_EXT_FACTOR_SET` | `{region, version, effectFrom, effectTo, factorGPerKwh, source}` → `{factorId, created, supersededFactorId}`。**写幂等键是建表已有的 `UNIQUE(region, version)`**，故不收 `reqId`；同版本内容不同 → `ERR_PARAM`（因子版本不可变，修订请发新 version）；重叠判定**跨区域生效**（D10）；起点更晚则自动**接续**开区间因子并在同一事务闭合它（D9）|
| 3743 | `CMD_EXT_REPORT_GEN` | `{scope, stationId, dateFrom, dateTo, reqId}` → `{reportId, version, status, created}`。`reqId` UNIQUE 写幂等；同范围重生成 → `version` 递增，旧版本行原样保留 |
| 3744 | `CMD_EXT_REPORT_EXPORT` | `{reportId, format, allowStale?}` → `{path, absPath, format, stale}`，**文件落盘不走 Socket**（protocol.md §2）。`STALE` 报告默认拒为 `6703`，客户端确认后带 `allowStale` 重发；报告不存在 → `6704` |
| 3745 | `CMD_EXT_CARBON_AGGREGATE` | `{stationId, dateFrom, dateTo}` → `{days, rewritten, stationId, cutoffTime}`，显式重算（替代 B1 定时任务）。**`stationId` 只做校验与回显，重算一律按日整体进行（含全部站点）** —— 半拉的行集对不了账 |
| 3746 | `CMD_EXT_REPORT_LIST` | `{}` → 报告列表含 `status` 与指标快照，用于展示 `STALE` 角标 |
| 3747 | `CMD_EXT_FACTOR_DELETE` | `{factorId}` → `{factorId, version, removed, disabled, alreadyDisabled, restoredFactorId, dailyRefs, reportRefs}`。撤销因子版本：**无历史引用则物理删除，已被引用则改为停用**（保追溯）；两条路都会**恢复时间线**（把当初被接续的前驱的生效止还回去）；对**已停用**的因子是幂等空操作，返回 `alreadyDisabled=true` 且一个字节都不写（见 D12）；拒绝撤销最后一个启用因子 → `6706`；因子不存在 → `6705` |
| 3748 | `CMD_EXT_FACTOR_PURGE` | `{factorId}` → `{factorId, region, version, wasEnabled, dailyDeleted, reportsDeleted, filesDeleted, restoredFactorId}`。**彻底删除，不可恢复**：因子行 + 它算出的 `t_carbon_daily` 行 + 引用它的 `t_carbon_report` 快照 + 已导出的 CSV/HTML 文件一并删除；仍在启用时间线内时同样**恢复时间线**（已停用的因子在停用那一刻恢复过了，不再做第二遍）；拒绝删除最后一个启用因子 → `6706`；因子不存在 → `6705`；版本号被第二行因子共用 → `6707`（级联按 `version` 字符串匹配，重名会误伤别人的历史行） |

全部 `needAuth = true` 且 `req.session.role != ROLE_ADMIN → ERR_NO_PERMISSION`，与 2301 一致。

错误码：`6701 ERR_CARBON_NO_FACTOR` / `6702 ERR_CARBON_FACTOR_OVERLAP` / `6703 ERR_CARBON_REPORT_STALE` / `6704 ERR_CARBON_REPORT_NOT_FOUND` / `6705 ERR_CARBON_FACTOR_NOT_FOUND` / `6706 ERR_CARBON_LAST_FACTOR` / `6707 ERR_CARBON_VERSION_SHARED`（段内自治新增）。

## 5 服务端结构

`server/biz/ext_08_carbon_calc.{h,cpp}` —— **纯函数、零数据库依赖**：时段切分、最大余数分摊、
因子选择、整数排放与强度。所有手算夹具打在这一层。

`server/biz/ext_08_carbon_service.cpp` —— 导出 `registerExt08CarbonService()`，9 个 handler。

- **只读** `t_order` / `t_station`，**只写** `t_carbon_*`。全文件 `grep` 不得出现对
  `t_user` / `t_wallet_tx` / `t_pile` / `t_order` 的 `UPDATE`/`INSERT`/`DELETE`。
- 连接一律 `ecp::threadDb()`；SQL 一律 `prepare` + `addBindValue`。
- **不持长事务**：先定 `cutoff_time = 本次开始时刻`，再**按日**读取、计算、短事务写入。
- **懒聚合**：3740 对范围内每一天比对 `t_carbon_daily.order_cnt` 与
  `SELECT COUNT(*) FROM t_order WHERE status=3 AND date(settle_time)=?`，不一致（含无行）则重算该日。
  今日永远重算。重算后若某 `READY` 报告覆盖该日且数字变了 → 该报告置 `STALE`（不覆盖旧数字）。
- **并发**：同范围两个请求同时重算，靠 `UNIQUE` + `INSERT OR REPLACE` 收敛为同一结果，不加额外锁。
- **导出**：写到 `export/carbon/report_<id>_v<n>.{csv,html}`，返回相对路径。
  假设管理端与服务端同机（演示场景），**须在页面上写明**。

## 6 管理端结构

`admin-client/ext_08_carbon_page.{h,cpp}`，导航项「碳排放报告」，照 `overview_page` 的
`NetClient::send` + `response` 信号范式，**不新增线程**（硬性规则 4）。

指标卡（总电量 / 估算排放 / 排放强度 / 完整度）、峰平谷构成、排放趋势（`HAVE_CHARTS` 未定义时降级为表格，
与 `overview_page` 一致）、因子版本选择、报告生成与导出、`STALE` 角标。
页眉常驻免责声明与「固定时段口径」。

**开关关闭时**：handler 未注册 → 返回 `ERR_CMD_UNKNOWN(1005)` → 页面显示「该功能未启用」。
不新增「查询开关」命令字（那要动核心），符合 00 §4.7「拿不到开关状态时按关闭处理」。

## 7 大屏与对拍

- `ml/export_carbon_snapshot.py` → `dataviz/data/carbon.json`，**只读**，只读 `t_carbon_daily`。
- `dataviz/carbon.html` 独立页面，`index.html` 只加一行入口链接。
  **`ml/export_snapshot.py` 与 `index.html` 既有图表一律不动**（口径已与 2301–2303 对齐，改动走 CR）。
- `ml/carbon_crosscheck.py` —— 从 `t_order` 独立重算一遍，与 `t_carbon_daily` 逐格比对，
  任一格不等即退出码 2。这是 D1 换来的免费对账层。

## 8 接缝清单（逐文件）

| 文件 | 属主 | 改动量 |
| --- | --- | --- |
| `docs/expand/CLAIM.md` | 共管 | 认领行 + 段号登记行 |
| `docs/db-schema-ext-08.sql` | 新建 · L5 | — |
| `common/error_code.h` | **L1（冻结）** | 2026-09-08 L1 授权：+`ExtMsgProvider` 挂钩，核心段行为不变，未注册时与从前完全一致 |
| `common/protocol_ext.h` `common/error_code_ext.h` | 新建 · 共管 | 只写 08 段；`errMsgExt()` 兼作文案提供者，**未知码返回空串**以免盖住核心兜底 |
| `server/biz/ext_08_carbon_calc.{h,cpp}` `ext_08_carbon_service.cpp` | 新建 · L5 | 计算核心 + 7 个 handler，另有 `ext_08_carbon_calc_test.cpp` 手算夹具（不入 `server.pro`） |
| `server/server.pro` | L2 | SOURCES +3、HEADERS +1 |
| `server/main.cpp` | L2 | 声明 +1、注册 +1、`featureEnabled()` helper（B4） |
| `admin-client/ext_08_carbon_page.{h,cpp}` | 新建 · L5 | 含 `CarbonFactorDialog`（同文件，不另开文件）与报告区（S5） |
| `admin-client/main_window.cpp` | L3 | **+2 行**（`addItems` 一处、`addWidget` 一处 —— 是 `QStackedWidget` + 导航列表，不是 `addTab`，08 文档写的「一行 addTab」与仓库不符） |
| `admin-client/admin-client.pro` | L3 | SOURCES +1、HEADERS +1 |
| `scripts/build-all.sh` | L3 | B3，约 8 行 |
| `.gitignore` | 共管 | `export/` 一行（报告导出件是运行期产物） |
| `scripts/smoke-carbon.py` | 新建 · L5 | 照 `smoke-admin.py` 范式。**已覆盖全部 7 个命令字**：鉴权门、入参校验、幂等、分站求和、SQLite 对拍、6701/6702/6704、报告生成与导出（写入测试在 `--mutating` 后） |
| `ml/` `dataviz/` | L5 | **全部落地**：`ml/carbon_crosscheck.py`（S4）、`ml/export_carbon_snapshot.py`、`dataviz/carbon.html`（S5）；`index.html` +3 行入口链接，其余内容未动 |

**不改**：`common/protocol.h`、`common/error_code.h`、`common/frame.h`、`server/net/**`、
`docs/protocol.md`、`docs/db-schema.sql`、`ml/export_snapshot.py`、其他人的 service 与页面文件。

## 9 交付切片与验收结果

| # | 内容 | 验收 |
| --- | --- | --- |
| S1 | ✅ **已完成 2026-09-07** —— 另加 3741 因子列表（纯只读），否则 B4 开关的验收无从验证 | ✅ 幂等两遍无重复；开关双向生效；权限门三档（1002/1003/1004）；`smoke-admin` RESULT: PASS；快照 `revenue.total` 未变 |
| S2 | ✅ **已完成 2026-09-08** —— `server/biz/ext_08_carbon_calc.{h,cpp}` 纯函数 + `ext_08_carbon_calc_test.cpp` 手算夹具，`scripts/test-carbon-calc.sh` 单独编译运行（不连库、不起服务） | ✅ 61 项断言全过：手算表逐格一致；5000 例模糊测试守恒；1e11 边界不溢出；编译零告警 |
| S3 | ✅ **已完成 2026-09-08** —— 3740 懒聚合查询 + 3745 显式重算 + `scripts/smoke-carbon.py` | ✅ 连查两次逐格一致、360 行无重复；3745 重算 60 天后 totals 不变；订单数 = `COUNT(status=3)` = 6477；总电量 = `SUM(kwh_x100)` = 36,638,035 与 SQLite 直算一致；空范围返回空列表且完整度 = −1；**迟到订单被自动发现并只重算受影响那天**；Python 独立重算 360 行零不一致；业务表与备份逐字节相同 |
| S4 | ✅ **已完成 2026-09-08** —— `admin-client/ext_08_carbon_page.{h,cpp}`（含新增因子对话框）+ 服务端 3742 + `ml/carbon_crosscheck.py` | ✅ 页面数字 = `t_carbon_daily` = 独立重算三方一致（360 行零不一致，故意改坏一行能被抓出并退出码 2）；重叠因子拒为 `6702`、相邻区间放行；无因子时段 `6701` **且页面显示中文而非「未知错误(6701)」**；7 种非法入参被拒；因子版本不可变、重复提交幂等；离屏跑通真实渲染 |
| S5 | ✅ **已完成 2026-09-08** —— 3743/3744/3746 + STALE 标记 + 管理端报告区 + `ml/export_carbon_snapshot.py` + `dataviz/carbon.html` | ✅ 报告快照与 3740 汇总逐格一致；导出 CSV/HTML 含免责声明与口径标注；**迟到数据使旧报告转 `STALE` 且数字一字未改**，未确认导出拒为 `6703`、确认后导出历史快照并附过期告警；同范围重生成版本递增、旧版本仍可查；不存在的报告 → `6704`；大屏数字与 `t_carbon_daily` 同源；`smoke-carbon.py` 全绿 |

## 10 遗留

| 事项 | 交给谁 | 说明 |
| --- | --- | --- |
| B3 / B4 的实现 | L3 / L2 | ✅ **均已追认**。B3 于 2026-09-08 由 L3 追认（`1b10b1e`）；B4 于 2026-09-08 获 L2 授权后重做为 `FeatureFlags` 类：一次查询读回全部 `feat_*` 键并缓存，启动时打一行开关汇总，数据库未就绪记 `LOG_E`（那意味着所有扩展被静默关掉）|
| ~~`errMsg()` 对扩展码的 fallback~~ | ~~L1（走 CR）~~ | ✅ **2026-09-08 已完成**，见裁决 D5。`ExtMsgProvider` 挂钩，smoke 已断言服务端 `msg` 为中文 |
| 峰平谷改真实分时电价 | 模块 05 认领人 | 届时把固定时段常量换成 `t_price_*` 查询，页面标注同步改口径 |
| `METER_INTERVAL` 质量档 | 模块 02 / 06 | 有表计区间数据后才可能出现，列已预留 |
