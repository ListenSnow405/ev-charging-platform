# 第二阶段答辩 PPT 大纲与讲稿

> 大数据可视化大屏与机器学习智能分析子系统 · 课程设计答辩
> **总时长 10:00　7 个 Part / 约 21 页　讲稿约 2200 字　语速 220 字/分钟**
> 体例沿用第一阶段 [DEFENSE-SCRIPT.md](DEFENSE-SCRIPT.md)：**分镜截图，不录屏**，节奏由主讲人按空格控制。
> 讲稿里的 `⟨切 X-XX⟩` 表示在此处按一次空格翻页。

## 0. 本稿的汇报口径

第二阶段全部任务按**数据流水线的四个环节**分给五条线：

```text
数据清洗 ──→ 数据分析 ──→ 可视化 ──→ 机器学习
   L1          L2         L3 + L4       L5
```

这个切法不是硬凑：它与第一阶段的五条线**职责同构**——L1 本就管底层设施与数据入口，
L2 本就管数据存储与服务，L3/L4 本就是两个界面端，L5 本就是机器学习。
老师如果追问「第二阶段你们怎么分的」，就按这句话答：**同一批人，同一套职责，换了一套技术栈重做一遍**。

> ⚠ **上台前的硬性前提**：本稿把工作按人分段，**每条线的人必须能答自己那段的问题**。
> 第 9 节给每条线准备了 4 道必答题，各人自己过一遍，别指望主讲人替所有人挡。
> 代码仓库里第二阶段的实现集中在 `bigdata/**`，分线对应目录见第 2 节表格右列。

## 时间预算

| Part | 内容 | 页数 | 起止 | 时长 | 讲稿字数 |
| --- | --- | --- | --- | --- | --- |
| 1 | 封面 | 1 | 00:00–00:15 | 15s | ≈55 |
| 2 | 人员分工（四段流水线） | 1 | 00:15–01:00 | 45s | ≈165 |
| 3 | 第二阶段定位与数据流向 | 2 | 01:00–02:10 | 70s | ≈255 |
| 4 | 技术栈与版本 | 1 | 02:10–02:50 | 40s | ≈145 |
| **5** | **四段成果展示** | **13** | 02:50–08:00 | **310s** | ≈1135 |
| 6 | 难点怎么解决 · 技能提升 | 2 | 08:00–09:15 | 75s | ≈275 |
| 7 | 心得 · 局限 · 致谢 | 1 | 09:15–10:00 | 45s | ≈165 |

Part 5 占 310 秒，是全场重心，13 张图平均 24 秒一张。其余六个 Part 合计 290 秒。

## 前置确认

**全链路能真跑。** T0–T8 全部完成：ODS 已落 HDFS（Hadoop 3.3.6 伪分布式），
清洗六阶段产出齐全、校验 **82/82 PASS**，分析 **13 维度 + 3 组对比**落 MySQL，
Flask 冒烟 **14/14 PASS**，大屏渲染冒烟 **15/15 PASS**（含真实截图），MLlib **6 个模型**评估报告已出。
复现步骤见 [PHASE2-PLAN.md](PHASE2-PLAN.md) 第 14 节。

**测试有据。** [第二阶段测试用例.xlsx](第二阶段测试用例.xlsx)：11 张表 **169 条用例** + 缺陷 9 条（8 条已闭环，1 条待修复），
表格由 `scripts/gen-testcases-phase2.py` 生成，不手工维护。被问「第二阶段你们怎么测的」直接翻这份。

---

# Part 1 · 封面

**00:00–00:15　15 秒　1 页**

### 画面

- 标题：**大数据可视化大屏与机器学习智能分析子系统**
- 副标题一行小字：基于 Hadoop + Spark + Flask + Vue3 的充电桩运营数据分析
- 五位组员姓名（按 L1–L5 顺序横排）、指导教师、班级、日期
- 衬底用大屏「运营总览」页截图，压低透明度，不要盖住文字

### 讲稿

> 各位老师好。我们小组第二阶段的课题是「大数据可视化大屏与机器学习智能分析子系统」，是在第一阶段充电桩管理平台的基础上，用另一套技术栈把数据分析这条线重做一遍。我是主讲人某某，下面开始汇报。

*≈58 字 · 目标 15 秒*

---

# Part 2 · 人员分工（四段流水线）

**00:15–01:00　45 秒　1 页　★ 这一页是本次汇报的骨架，后面每一页都挂在它上面**

### 画面

**上半**：一条横向流水线示意，四个环节四种颜色，箭头串起来：

```text
① 数据清洗        ② 数据分析         ③ 可视化           ④ 机器学习
   L1               L2                L3 + L4             L5
HDFS/ODS         Spark 多维分析      Vue3 + DataV        Spark MLlib
SOP 六阶段        MySQL 结果层        5 页 18 面板        6 个 GBT 模型
                 Flask 只读 API      渲染冒烟 15/15      双基线评估
```

**下半**：五行分工表。**后面 Part 5 的每一段都用对应颜色的色条打头**，观众一眼知道讲到谁。

| 姓名 | 环节 | 负责内容 | 代码位置 | 第二顶帽子 |
| --- | --- | --- | --- | --- |
| （L1） | **① 数据清洗** | ODS 只读原始层、HDFS 部署、SOP 六阶段清洗 | `bigdata/spark/{export_ods,profiling,cleaning,validation,quality_report}.py`、`scripts/*hadoop*`、`ods-to-hdfs.sh` | 环境与契约 |
| （L2） | **② 数据分析** | 13 个分析维度 + 3 组对比、MySQL 结果表、Flask 只读接口 | `bigdata/spark/analysis.py`、`bigdata/api/` | 口径对账 |
| （L3） | **③ 可视化** | Vite + Vue3 工程、DataV 组件、5 页布局与路由 | `bigdata/web/src/{App.vue,pages.js,components/}` | 集成与构建 |
| （L4） | **③ 可视化** | 15 个 ECharts 图表实现、9 类图表形态、渲染冒烟 | `bigdata/web/src/charts.js`、`scripts/smoke-screen-phase2.py` | 测试与用例集 |
| （L5） | **④ 机器学习** | 特征工程、6 个 GBT 模型、双基线评估、预测回写 | `bigdata/mllib/{features,train,predict,report}.py` | 文档归档 |

### 讲稿

> 先说分工。第二阶段我们按数据流水线切成四段：数据清洗、数据分析、可视化、机器学习，五个人各认一段。
>
> 一号线做数据清洗，负责把第一阶段的业务库导成只读原始层、部署 HDFS，再按数据清洗的标准流程走完六个阶段。二号线做数据分析，用 Spark 做十三个分析维度和三组对比，结果落 MySQL，再用 Flask 开只读接口。三号和四号一起做可视化，三号搭 Vue 工程和大屏布局，四号写十五个图表和渲染测试。五号做机器学习，用 Spark MLlib 训练负荷预测模型。
>
> 这个切法和第一阶段是对应的：一号线原来就管底层通信和契约，二号线原来就管数据和服务，三四号原来就是两个界面端，五号原来就是机器学习。**同一批人、同一套职责，换一套技术栈重做一遍。**

*≈272 字 · 目标 45 秒偏满 —— 若时间紧，删掉中间那段逐条点名，只留第一段和最后一句，压到 ≈160 字*

---

# Part 3 · 第二阶段定位与数据流向

**01:00–02:10　70 秒　2 页**

## 3-1　第二阶段是什么、不是什么

### 画面

左右分栏对照，**用「是 / 不是」两列强对比**：

| ❌ 不是 | ✅ 是 |
| --- | --- |
| 给第一阶段补功能 | 用另一套技术栈把同类分析**重做一遍** |
| 替换第一阶段 | 与第一阶段**并存**，Qt 平台一行不改 |
| 大屏改接服务端 | 只**共用同一份数据源**，两条链路解耦 |

右下角一句结论加粗：**Qt 服务端始终保持纯 Socket，不提供 HTTP；Flask 是大数据子系统自己的一层。**

## 3-2　数据流向全图

### 画面

一张四层流向图，从左下到右上，**每层标注职责边界那一句**：

```text
第一阶段（不动）                        第二阶段（新建）
┌──────────────┐
│ Qt 客户端 ×2  │                    ┌──────────────────────────────┐
│   ↕ 自定义TCP │                    │ Vue3 + DataV 大屏（Node 24）  │  ← L3 + L4
│  ecp-server  │                    └───────────────┬──────────────┘
│  纯 Socket    │                                    │ HTTP / JSON
└──────┬───────┘                    ┌───────────────┴──────────────┐
       │ 写                          │ Flask 只读 API（4 个接口）     │  ← L2
       ↓                            └───────────────┬──────────────┘
  charging.db                                       │ SQL
   (SQLite)                                         ↓
       │  ① 导出（只读，chmod 444）            MySQL 分析结果层
       └────→ HDFS / ODS 只读原始层                  ↑  ③ 回写
                    │ ② 读                           │
                    └──→ Spark 清洗 → 分析 → MLlib ───┘
                           L1      L2       L5
```

图下四行小字，每层一句职责边界：

- **ODS 原始层（HDFS）**：业务库的只读快照，**任何清洗都不在这一层做**
- **Spark**：清洗（DWD）→ 分析（DWS）→ 建模，输出全部进 MySQL
- **MySQL**：只存**分析结果**，不存明细，所以 Flask 查询压力可控
- **Flask**：只读，不写任何业务库，不现场触发 Spark job

### 讲稿

> 先说清楚第二阶段是什么。它**不是**给第一阶段补功能，而是用另一套技术栈把同类的数据分析重做一遍；两个阶段**并存**，第一阶段的 Qt 平台一行代码都没改，两边只共用同一份数据源。这一点很关键，因为说明书对第一阶段的考核点是纯 Socket 通信，我们新引入的 Flask 不挂在 Qt 服务端上，而是大数据子系统自己的一层，所以那个考核点完好。
>
> ⟨切 3-2⟩ 这是数据的流向。左边是第一阶段的业务库，我们把它**只读导出**成原始层放到 HDFS 上；Spark 从 HDFS 读，做清洗、做多维分析、做机器学习建模，结果全部写进 MySQL；MySQL 只存分析结果不存明细，所以上面那层 Flask 查询压力可控；最上面是 Vue3 加 DataV 的大屏，通过 HTTP 取数。
>
> 四层各有一条不能越的边界：原始层只读，清洗一律在下一层做；Flask 只读，不写业务库也不现场触发 Spark。

*≈296 字 · 目标 70 秒*

---

# Part 4 · 技术栈与版本

**02:10–02:50　40 秒　1 页**

### 画面

**不要做成一张平铺的版本表**——把版本号直接标注在 3-2 的流向图上，做成同一张图的「带版本」变体，
观众的视线不用重新找位置。四个环节配四种颜色的角标：

| 环节 | 组件 | 实测版本 |
| --- | --- | --- |
| ① 清洗 | JDK / Hadoop / Python / PySpark | **17** / **3.3.6** 伪分布式 / **3.11.15** / **3.5.9** |
| ② 分析 | PySpark / MySQL / Flask | 3.5.9 / **8.0.46** / 只读 4 接口 |
| ③ 可视化 | Node / Vue / Vite / ECharts / DataV | **v24.1.0** / **3.5.42** / 6.4.3 / 5.6.0 / `@kjgl77/datav-vue3` 1.7.4 |
| ④ 机器学习 | Spark MLlib | GBTRegressor × 6 |

底部一行加粗：**版本全部由 `scripts/check-env-phase2.sh` 实测输出——它会真建一次 SparkSession、真连一次 NameNode，不是只看 `pip list`。**

### 讲稿

> 技术栈我标在同一张图上。清洗这一层是 JDK 17、Hadoop 3.3.6 伪分布式、Python 3.11.15 加 PySpark 3.5.9；分析层结果存 MySQL，上面是 Flask；可视化是 Node 24、Vue 3.5.42、Vite，图表用 ECharts 加 DataV 的开源 Vue3 组件库——**是开源组件库，不是阿里云的收费服务**；机器学习是 Spark MLlib。
>
> 这些版本不是从文档抄的，是环境自检脚本实测输出的：它会真的建一次 SparkSession、真的连一次 NameNode，而不是只看包列表。因为这条栈上版本是强绑定的——PySpark 3.5 只支持到 Python 3.11，这一条决定了整条栈的版本组合。

*≈208 字 · 目标 40 秒偏满 —— 偏慢就删掉最后一句版本联动*

---

# Part 5 · 四段成果展示

**02:50–08:00　310 秒　13 页**

> 全场重心。**每段开头放一张与 Part 2 同色的色条 + 环节名 + 负责人名**，观众始终知道现在讲到哪一段、谁负责。
> 截图规范完全沿用第一阶段（见 [DEFENSE-SCRIPT.md](DEFENSE-SCRIPT.md) 文末），这里不重复。

## 5-A　① 数据清洗 · L1（4 张 · 90 秒）

| # | 截图内容（文字说明，拍图时照此取景） | 标注要求 |
| --- | --- | --- |
| A-01 | **终端**：`hdfs dfs -ls -R /ecp/ods` 输出，15 个文件，权限列清楚显示目录 `dr-xr-xr-x`、文件 `-r--r--r--`，属主列是 `ecp_ods` | 红框圈**权限列与属主列**，旁注「只读不是靠自觉，是靠权限」 |
| A-02 | **SOP 六阶段产出总览**：`bigdata/quality/` 目录列表，01~06 加 09、10 八份产物 + `pending/` 14 份待核清单 | 每份文件右侧贴一个阶段号标签①~⑥ |
| A-03 | **质量报告截图**：`06_quality_report.md` 的第 2 节「六维度质量评估」表（完整/准确/一致/时效/唯一/有效，共 11 条问题） | 圈「准确性 0 —— 6477 笔已结算订单金额与单价×电量逐笔吻合」那行 |
| **A-04** | **校验结果**：`validation.py` 运行末尾输出 **82/82 PASS**，并排放一张清洗前后对比小表（订单 8292 → 8292，删除 0 行，改值 0 处，剔除 2 列，标记待核 1 条） | **「8292 → 8292」放大**，旁注「全程零删除」 |

### 讲稿

> 第一段是数据清洗，一号线负责。
>
> ⟨切 A-01⟩ 先看数据落在哪。我们把第一阶段的业务库只读导出成原始层，推到 HDFS 上，十五个文件。请看权限列和属主列：目录是 555、文件是 444，属主是一个独立的 HDFS 身份 `ecp_ods`，分析侧用另一个身份连。**原始层只读这件事我们是靠权限保证的，不是靠自觉**——推送脚本最后还会以分析身份试写一次，并且期待它失败。
>
> ⟨切 A-02⟩ 清洗严格按数据清洗的标准流程走六个阶段：数据探查、质量评估、制定规则、执行清洗、清洗校验、报告存档，每个阶段都有规定的产出，一共八份产物加十四份待核清单。⟨切 A-03⟩ 质量评估是按完整性、准确性、一致性、时效性、唯一性、有效性六个维度做的，一共发现十一条问题。这里请看准确性那行：六千四百七十七笔已结算订单，金额与单价乘电量逐笔吻合，容差一分钱。
>
> ⟨切 A-04⟩ 最后是校验，八十二项断言全部通过，覆盖十一张表的主键、十二条外键、四十五条取值范围。我们想强调的是这组数字：订单进来八千二百九十二笔，出去还是八千二百九十二笔，**全程零删除、零改值**。标准流程把删除列为最后手段，实测发现的问题要么可以标记、要么属正常业务，都不需要动行。

*≈446 字 · 目标 90 秒*

## 5-B　② 数据分析 · L2（4 张 · 85 秒）

| # | 截图内容（文字说明） | 标注要求 |
| --- | --- | --- |
| B-01 | **维度全景图**：一张 13 宫格示意（不是截图，是画的），D1~D14 各一格，格内写维度名，D12 那格标「MLlib 预测」并用第④段的颜色 | 三组对比 C1/C2/C3 单独一行，用加粗边框 |
| B-02 | **MySQL 结果表**：`SHOW TABLES;` 输出 + 任取一张结果表的 `SELECT ... LIMIT 5` | 旁注「只存分析结果，不存明细」 |
| **B-03** | **口径对账**：一张竖向串联图，五个环节各标营收数字，全部是 **53,936,279 分** —— 清洗校验 → 7 个分析维度 → MySQL 落库 → 特征面板守恒自检 → 第一阶段碳排放对拍脚本 | 五个数字**全部加粗同色**，旁注「逐环节对账，不是『应该没问题』」 |
| B-04 | **Flask 接口**：`curl /api/dimensions` 的 JSON 响应 + 冒烟脚本输出 **14/14 PASS** | 圈响应里的 `dimension_count`、`comparison_count`；冒烟输出里圈那条 **SQL 注入尝试**用例 |

### 讲稿

> 第二段是数据分析，二号线负责。
>
> ⟨切 B-01⟩ 老师要求分析维度不少于八个、其中至少两组做对比，我们做了**十三个维度和三组对比**。维度包括营收趋势、站点排行、电桩利用率、时段负荷、充电量分布、订单终态、在线率、设备事件、碳排放、地理分布、时长分布这些；三组对比是快充对慢充、工作日对周末、六个站点横向对标。
>
> ⟨切 B-02⟩ 结果全部写进 MySQL，**只存分析结果不存明细**，所以上层查询压力可控。
>
> ⟨切 B-03⟩ 这一页是我们比较自豪的一张。营收总额五千三百九十三万六千二百七十九分这个数字，在清洗校验、七个分析维度、MySQL 落库、机器学习的特征面板守恒自检，一共五个环节上逐一对账，完全一致；其中最后一个还是第一阶段一个独立脚本算出来的。**口径在环节之间悄悄漂移是这类项目最常见的暗伤**，第一阶段我们就踩过——大屏和管理端把同一个指标显示成了两个形状。所以第二阶段每一环都显式对账。
>
> ⟨切 B-04⟩ 上层是 Flask 的四个只读接口，接口冒烟十四项全过。维度目录不是硬编码的，是启动时从数据库元信息读的，分析脚本增删维度接口不用改。另外因为表名会拼进 SQL，白名单是唯一防线，冒烟里专门留了一条 SQL 注入尝试的用例。

*≈418 字 · 目标 85 秒*

## 5-C　③ 可视化 · L3 + L4（3 张 · 75 秒）

| # | 截图内容（文字说明） | 标注要求 |
| --- | --- | --- |
| C-01 | **大屏「运营总览」页整页**：深色底，顶部 6 个 DataV 数字翻牌 KPI，下方营收与订单趋势（折线+渐变面积，双轴）、订单终态活动环图、站点营收胶囊图、时段负荷、充电量分桶 | 每个面板左上角的**维度编号角标**（D1/D6/D2/D4/D5）单独圈出来，旁注「面板带维度编号，便于对照」 |
| C-02 | **五页缩略图拼版**：运营总览 / 站点与设备 / 对比分析 / 负荷预测 / 碳排放，五张缩略图横排 | 底部一行标「5 页 · 18 面板 · 9 类图表形态」 |
| C-03 | **对比分析页整页**：快充 vs 慢充分组柱（对数轴）、站点多指标雷达、充电时长分布、工作日 vs 周末 24 小时双折线面积 | 圈雷达图，旁注「取消率已取反归一 —— 面积大 = 表现好」 |

### 讲稿

> 第三段是可视化，三号和四号一起做。
>
> ⟨切 C-01⟩ 这是大屏的运营总览页。技术上是 Vite 加 Vue3 的工程，组件用 DataV 的开源 Vue3 库——顶部这六个数字翻牌、右边的胶囊图和活动环图都是它的组件；图表用 ECharts。请注意每个面板左上角有维度编号，D1、D6、D2 这些，和刚才二号线的维度表一一对应，答辩时要查哪个维度可以直接点。
>
> ⟨切 C-02⟩ 整个大屏一共五页、十八个面板：运营总览、站点与设备、对比分析、负荷预测、碳排放。老师要求「图表格式不要过于单一」，我们做了**九类形态**：折线加渐变面积、柱、横向条形、堆叠柱、分组柱、活动环图、胶囊图、仪表盘、雷达、散点，另外还有三张滚动表格。
>
> ⟨切 C-03⟩ 这是对比分析页。举一个做图时的判断：右边这张站点雷达图，取消率这个轴在服务端已经取反了——因为雷达图的直觉是「面积大等于好」，如果直接画取消率，面积大反而是坏事，会被读反。另外左边快充慢充这张用的是对数轴，四个指标量纲从两位数跨到四位数，线性轴会把小的那一侧压成贴地的一条线。
>
> 大屏还有渲染冒烟测试，开无头浏览器逐页截图并对 DOM 下断言，十五项全过。

*≈437 字 · 目标 75 秒偏满 —— 若超时，删掉 C-03 的对数轴那句和最后一句冒烟*

## 5-D　④ 机器学习 · L5（2 张 · 60 秒）

| # | 截图内容（文字说明） | 标注要求 |
| --- | --- | --- |
| **D-01** | **评估报告表格**：`07_forecast_eval.md` 第 3 节两张表（负荷 kW / 并发会话数，各 3 个 horizon，列含测试 MAE、RMSE、R²、基线 A、基线 B、vs A、vs B、选中超参） | 圈 **vs B 列**，三个正数（+1.0% / +0.2% / +7.9%）绿色、三个负数红色，**不要修饰** |
| D-02 | **大屏「负荷预测」页**：MLlib 1/6/24h 分组柱 + DataV 滚动表格（含空闲桩数与高峰时段） | 圈滚动表格里的空闲桩与高峰列，旁注「由预测值派生，不单独建模」 |

### 讲稿

> 第四段是机器学习，五号线负责。
>
> ⟨切 D-01⟩ 说明书要求预测未来 1、6、24 小时的充电负荷、空闲桩数和高峰时段。我们按第二阶段要求改用 Spark MLlib，训了六个梯度提升树模型：负荷和并发会话数各三个周期。空闲桩数和高峰时段是由预测值派生的，不单独建模——单独训分类器会出现「负荷预测很低但标成高峰」这种自相矛盾。
>
> 评估上我们做了三件事：**按时间切分，绝不随机切**，因为同一天相邻小时高度相关，随机切会把未来样本混进训练集，指标会虚高到没有意义；**两条基线**，基线 A 是历史同小时均值，基线 B 再分工作日和周末，以 B 为准；**超参在验证段上选**，绝不用测试段调参。
>
> 结果如实报告：**六个模型三个跑赢基线 B**，负荷 1 小时加 1.0%、24 小时加 0.2%、并发 1 小时加 7.9%，另外三个是负的。原因我们在报告第四节写清楚了：一是按规则只能用原始层的数据，少了第一阶段有的天气特征；二是框架差异，小样本上另一个框架更抗过拟合。**这是真实差异，不是没调好，我们没有修饰这张表。**
>
> ⟨切 D-02⟩ 预测结果回写 MySQL，上到大屏第四页，右边表格里就是空闲桩数和高峰时段。

*≈437 字 · 目标 60 秒严重偏满 —— 必须删：把「三件事」压成一句「时间切分、双基线、验证段选参」，省约 90 字*

---

# Part 6 · 难点是怎么解决的 · 技能提升

**08:00–09:15　75 秒　2 页**

> 与第一阶段的分工：那边讲 C++ 与网络编程的坑，**这边只讲大数据栈的坑，不要重复**。

## 6-1　四个真实踩过的坑（每段流水线各一个）

### 画面

**四栏，颜色与 Part 2 的四个环节对应**，每栏「现象 → 根因 → 修法」三行：

| 环节 | 现象 | 根因与修法 |
| --- | --- | --- |
| ① 清洗 | 换到 HDFS 后 Spark 报「Path does not exist」，路径看上去完全正常 | 路径被包成了 `pathlib.Path`，而 **pathlib 会折叠连续斜杠**，`hdfs://` 悄悄变成 `hdfs:/`，被当成本地相对路径。改为字符串拼接 |
| ① 清洗 | 清单文件 `hdfs dfs -cat` 读得到，Spark 读不到 | Hadoop 的 `FileInputFormat` **默认过滤 `_` 和 `.` 开头的文件**，而清单恰好叫 `_manifest.json`。改用 `FileSystem.open` 直读，绕开这层过滤 |
| ② 分析 | 大屏某个图「是空的」，浏览器不报任何错 | MySQL 的 `SUM()` 经驱动返回 `Decimal`，Flask 默认序列化成**字符串**，图表库拿到字符串静默不画。序列化层统一转 float，并加了防回归断言 |
| ④ 建模 | 训练跑了 3.5 小时「没动静」，其实早就崩了 | 等待器用 `pgrep -f "train.py"` 判断进程，**它匹配到了自己**，条件永远成立。改用 PID 文件。另有 GBT 血缘线性增长导致爆栈，`checkpointInterval` **不设检查点目录会静默不生效** |

**页脚一行加粗**：这四个坑有个共同点 —— **它们都不报错，或者报的错指向别处**。

## 6-2　技能提升

### 画面

左右两栏：

- **硬技能**：HDFS 伪分布式部署与权限模型 · Spark DataFrame 与执行计划 · Spark MLlib 回归与超参搜索 · Flask 只读接口与 SQL 注入防线 · Vue3 组件化与 ECharts 配置项
- **方法论**：数据清洗六阶段的完整走法（规则八字段、删除留痕、前后对账）· 时序模型的评估纪律（时间切分、双基线、验证段选参）· **换介质才知道哪里不成立**——先用本地目录跑通全链路，最后换 HDFS，三个坑全在换的那一刻集中暴露

### 讲稿

> 这一页讲第二阶段踩的坑，四个环节各一个。
>
> 清洗这边两个都出在 HDFS 上：一个是路径被包成了 Path 对象，而 Python 的 Path 会把连续斜杠折叠掉，`hdfs` 冒号双斜杠变成了单斜杠，Spark 当成本地相对路径找，报的错完全看不出根因；另一个是我们的清单文件叫下划线开头的 `_manifest.json`，而 Hadoop 默认会过滤下划线和点开头的文件，命令行读得到、Spark 读不到。
>
> 分析这边是个更隐蔽的：MySQL 的求和经过驱动返回的是 Decimal，Flask 默认把它序列化成字符串，前端图表库拿到字符串**不报错也不画**，浏览器里只表现为「图是空的」。建模这边最值得说：我们的等待脚本用 pgrep 按命令行判断训练进程还在不在，**结果它匹配到了自己**，于是在训练早就崩掉之后，它还持续报告了三个半小时「在跑」。
>
> ⟨切 6-2⟩ 这四个坑的共同点是**都不报错，或者报的错指向别处**。所以最大的方法论收获是：先用本地目录把全链路跑通，最后才换 HDFS——这三个坑都只在换介质那一刻暴露，一开始就背着 Hadoop 调试，它们会混在业务 bug 里一起出现。

*≈413 字 · 目标 75 秒偏满 —— 若超时，删掉「清单文件」那个坑，四个变三个*

---

# Part 7 · 心得 · 局限 · 致谢

**09:15–10:00　45 秒　1 页**

### 画面

三段式：

- **主动交代的局限**（这一栏放左边，**别藏着**）：
  - 不做用户维度 —— 8292 笔订单只分布在 4 个用户上，用户分层在数据层面是空的
  - D8 设备维度**被实测推翻**并降级 —— 原设计「故障分析」，实测故障事件 0 行，如实改为事件时序分布
  - 模型 6 个只赢 3 个 —— 少天气特征 + 框架差异，原因写在报告第 4 节
  - 数据为合成数据，绝对精度不外推到真实部署
- **心得**：原始层只读要靠权限而不是自觉 · 口径要逐环节对账 · 报告由脚本生成而非手写，手写的报告迟早和结果对不上
- **致谢**：指导教师、同组成员

### 讲稿

> 最后讲局限和心得。有三件事我们主动交代：第一，不做用户维度，因为八千多笔订单只分布在四个用户上，用户分层在数据层面是空的；第二，有一个设备故障分析的维度被实测推翻了——数据里故障事件是零行，我们如实降级成了事件时序分布，并写进报告，这比硬凑一张图有说服力；第三，模型六个只赢三个，原因刚才讲过。
>
> 心得是三句话：原始层的只读要靠文件权限保证，不能靠自觉；指标口径要在每个环节显式对账，不能想当然；报告要由脚本从结果里生成，手写的报告迟早和实际结果对不上。
>
> 感谢指导教师的指导，也感谢组里成员的配合。汇报到这里，请各位老师批评指正，谢谢。

*≈259 字 · 目标 45 秒偏满 —— 删掉第三条局限，因为 Part 5-D 已经讲过*

---

# 8. 老师六条要求 → 证据对照（备用页，不讲，被问时翻出来）

放在正片之后做**附录页**，讲的时候跳过，提问环节直接跳转。

| # | 要求 | 达标 | 现场可查的证据 | 谁答 |
| --- | --- | --- | --- | --- |
| 1 | Python 3.11 或 3.12 | ✅ **3.11.15** | `bash scripts/check-env-phase2.sh` | L1 |
| 2 | 文件存储 Hadoop 3.x | ✅ **3.3.6 伪分布式，ODS 已落 HDFS** | `08_hdfs_deploy.json` 逐文件 MD5 | L1 |
| 3 | PySpark 数据清洗 | ✅ SOP 六阶段产出齐全 | `bigdata/quality/01`~`06`、`09`、`10` | L1 |
| 3 | 分析维度 ≥ 8 | ✅ **13 个** | `/api/dimensions` 的 `dimension_count` | L2 |
| 3 | ≥ 2 组对比分析 | ✅ **3 组** | 大屏第 3 页 | L2 |
| 3 | Flask 处理 web 请求 | ✅ 4 个只读接口，冒烟 14/14 | `bigdata/api/README.md` | L2 |
| 4 | Node 23+ / Vue 3 | ✅ Node **v24.1.0** / Vue **3.5.42** | `bigdata/web/package.json` | L3 |
| 5 | DataV 大屏，图表不单一 | ✅ **5 页 18 面板 / 9 类图表**，DataV 5 类组件 | `bigdata/web/README.md` 面板对照表 | L3 / L4 |
| 6 | 机器学习预测 + 模型评估 | ✅ 6 个 MLlib 模型，MAE/RMSE/R² + **双基线** | `07_forecast_eval.md` | L5 |

---

# 9. 分线提问预案（各人自己背，别指望主讲人挡）

> 每条线 4 题。**答不上来的题现在就去看对应文件**，第 2 节表格右列有代码位置。

## L1 · 数据清洗

1. **「ODS 只读你们怎么保证的？」** —— 本地靠 `chmod 444`；HDFS 上 444 挡不住超级用户（超级用户就是启动 NameNode 的那个账号，就是我们自己），所以 ODS 交给独立身份 `ecp_ods`，分析侧固定以 `ecp_analyst` 连；推送脚本末尾会**以分析身份试写一次并期待它失败**，把这条保证验出来而不是声称。
2. **「六个阶段具体是哪六个？每个交了什么？」** —— 数据探查（数据概况）、质量评估（六维度问题清单 11 条）、制定规则（R001–R011，每条含编号/对象/检测条件/处理动作/依据/影响评估/异常去向/验证方式**八个字段**）、执行清洗（DWD + 待核清单 + 执行日志）、清洗校验（前后对比 + 抽样 + 业务断言 + Schema 契约断言，82 项）、报告存档。
3. **「为什么一条都没删？」** —— SOP 把删除列为最后手段。实测 11 个问题里：1 条非法手机号**标记待核**不删、冻结用户有订单是**正常业务**（冻结是后置管理动作）、取消订单的时间字段缺失是**合理缺失**（1815 条缺失恒等于取消单数）、两列近乎全空的字段**剔除列不动行**。删除、修正、填充都要留痕，无法判定的进待核清单，不静默丢弃。
4. **「为什么不直接读 `charging.db` 做分析？」** —— 与业务库解耦、可重跑、可回溯。代码只认路径不认介质，本地是目录、答辩是 HDFS，同一份代码。

## L2 · 数据分析

1. **「十三个维度都是什么？挑一个讲口径。」** —— 照 B-01 那张宫格图念。挑 D1：按已结算订单（status=3）的 `settle_time` 聚合到日，60 天跨度，金额全程整数分，只在展示层除 100。
2. **「三组对比得出了什么结论？」** —— C1 快充慢充：慢充均时长 282.5 分钟是快充 38.5 分钟的 **7.3 倍**，取消率 32.76% 接近快充 17.33% 的两倍，但**度均价同为 147 分**——单价按站点定不按桩型，差异全在电量和时长上。C2 工作日周末：工作日日均 112.5 单高于周末 95.4 单，但单均金额几乎持平（83.38 vs 82.93 元），说明周末是**来的人少**不是**单笔变小**。C3 站点对标：深圳湾公园单量时长满分但取消率最高，市民中心营收单价双第一。
3. **「Flask 怎么防注入？」** —— 表名会拼进 SQL，**白名单是唯一防线**，维度目录启动时从 `information_schema` 读；冒烟里有一条专门的注入尝试用例。另外只监听 `127.0.0.1`，本服务无鉴权，不暴露到局域网。
4. **「为什么 MySQL 只存结果不存明细？」** —— 明细在 HDFS 的原始层和 DWD 层，MySQL 是给 Flask 查的结果层，只存聚合后的行，查询压力才可控；也避免两处存同一份明细导致口径分叉。

## L3 · 可视化（工程与布局）

1. **「DataV 是哪个 DataV？」** —— `@kjgl77/datav-vue3` **开源组件库**，不是阿里云那个收费 SaaS。用了边框、装饰、数字翻牌、胶囊图、活动环图五类组件。
2. **「为什么要重建前端工程？第一阶段不是有大屏吗？」** —— 第一阶段是单文件 HTML 加 CDN 引 ECharts，没有 `package.json` 也没有构建工具，无法演进。第二阶段要求 Node 23+ 和 Vue3，所以重建成 Vite + Vue3 工程；**统计口径直接复用第一阶段，重写的只是渲染层**。
3. **「大屏怎么取数？会不会连到 Qt 服务端？」** —— 不会。大屏只走 Flask 的 HTTP 接口读 MySQL，Qt 服务端始终是纯 Socket、不提供 HTTP。开发和预览都通过 Vite 的 `/api` 代理走同源，前端代码路径两种模式一致。
4. **「五页分别是什么？」** —— 运营总览、站点与设备、对比分析、负荷预测、碳排放，共 18 面板。

## L4 · 可视化（图表与测试）

1. **「九类图表形态具体是哪九类？」** —— 折线+渐变面积、柱、横向条形、堆叠柱、分组柱、DataV 活动环图、DataV 胶囊图、仪表盘、雷达、散点；另有 3 张滚动表格。
2. **「为什么有两张图用对数轴？」** —— C1 四个指标量纲从两位数跨到四位数，D11 慢充尾巴拖到 420 分钟且每桶只有个位数，线性轴都会把小的一侧压成贴地的一条线。
3. **「雷达图那个取消率是不是画反了？」** —— 没有，服务端已经取反归一（`cancel_score` 越高 = 取消率越低），所以各轴统一「越大越好」，面积大 = 综合表现好。直接用 `cancel_rate_pct` 画才会反着读。
4. **「渲染怎么测？」** —— 无头浏览器逐页截图并对 DOM 下断言，15 项。有一条断言曾经写错值得一提：DataV 的数字翻牌渲染进 `<canvas>`，**没有任何文本节点**，用 `innerText` 断言永远误报，改成统计 canvas 的非透明像素数。另外冒烟里写死过「面板总数 == 17」，加了预测页之后一直失败没人发现，现在改成下限式断言（缺陷单 BUG-P2-001）。

## L5 · 机器学习

1. **「为什么不用第一阶段的 scikit-learn？」** —— 第二阶段任务书明文要求 Spark MLlib。**方法论完整沿用**第一阶段：时间切分、双基线、验证段选超参、主动说明限制。
2. **「六个模型只赢三个，是不是没调好？」** —— 是真实差异，两个原因：一是按规则只能用 ODS 层数据，少了第一阶段有的天气特征；二是框架差异，sklearn 的 HistGB 在 7.5k 行这种小样本上更抗过拟合。并发 24h 两个阶段收敛到同一数量级，佐证该周期的信号本就接近零。
3. **「为什么不随机切分？」** —— 同一天相邻小时高度相关，随机切会把未来样本混进训练集，指标虚高到没有意义。同理基线均值**只用训练段**算，用全量算等于把测试段泄漏进基线。
4. **「空闲桩数和高峰时段的模型呢？」** —— 由负荷和并发预测值**派生**，不单独建模。单独训分类器会出现「负荷预测很低但标成高峰」这种自相矛盾。

---

# 10. 截图清单与上台前检查

## 需要拍的 13 张（命名沿用第一阶段规范）

```text
A-01-hdfs权限.png      A-02-六阶段产出.png    A-03-质量报告.png      A-04-校验82全过.png
B-01-维度全景.png      B-02-mysql结果表.png   B-03-口径对账.png      B-04-flask冒烟.png
C-01-运营总览页.png    C-02-五页缩略.png      C-03-对比分析页.png
D-01-评估报告表.png    D-02-预测页.png
```

其中 **B-01（维度全景）、B-03（口径对账）、3-2（数据流向）** 三张是**画的图不是截的图**，
需要提前在 PPT 里做好，别到拍图那天才发现没有素材。

## 检查清单

- [ ] 拍图前按 `scripts/hdfs-ctl.sh start` 起 HDFS，否则 A-01 拍不到
- [ ] 拍图前按顺序起 Flask（:5000）再起大屏（:5173），大屏所有数据都来自 Flask，**顺序反了图全是空的**
- [ ] 终端截图统一配色与字号，行宽固定，**小字必须局部放大**（HDFS 权限列、冒烟输出都是小字）
- [ ] 大屏截图用 1920×1080 全屏，五页缩略图用同一比例缩放，不要一张宽一张窄
- [ ] D-01 的评估表格**不修饰负数**，红绿标色即可
- [ ] 每段开头的色条与 Part 2 分工表颜色一一对应
- [ ] 第 9 节的分线预案**打印五份，每人一份**，不是只给主讲人
- [ ] 完整试讲计时，控制在 9:30 以内（Part 5-D 和 Part 6 是预留的裁剪位）
- [ ] 准备三个高频追问：原始层只读怎么保证、十三个维度怎么来的、模型为什么没全赢基线

---

*本文件属 L5（配置管理与文档归档）维护。数字全部取自 2026-09-15 的仓库状态，
重跑后如有变化，以 `bigdata/quality/` 下的实际产物为准，不要照抄本稿。*
