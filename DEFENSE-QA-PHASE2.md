# 第二阶段答辩 · 分线问答稿

> 大数据可视化大屏与机器学习智能分析子系统 · 课程设计答辩
> **用途**：老师按人提问时，每个人照自己那一节答。分工口径来自 [DEFENSE-SCRIPT-PHASE2.md](DEFENSE-SCRIPT-PHASE2.md) 第 2 节，
> 本稿是它第 9 节的**展开版**（每条线从 4 题扩到 10 题左右，并补了追问链与兜底话术）。
> 讲稿、时间预算、截图清单仍以 [DEFENSE-SCRIPT-PHASE2.md](DEFENSE-SCRIPT-PHASE2.md) 为准，本稿只管问答。

## 怎么用这份稿

1. **各人只背自己那一节 + 第 0 节 + 第 6 节。** 第 0 节是全员共答题（谁被问到都要能答），第 6 节是兜底话术。
2. 每题标了难度：`★` 必答（答不上来会直接扣分）、`★★` 常见追问、`★★★` 深挖题（答不全不致命，但答得上来是加分）。
3. **答案里加粗的数字要背下来**，其余可以用自己的话说。数字取自 2026-09-15 仓库状态，重跑后以 `bigdata/quality/` 实际产物为准。
4. 每节末尾有「**现场能翻出来的证据**」，答不上来时至少要知道去哪儿翻。

---

# 0. 全员共答题（不分线，谁都可能被问）

### Q0-1　第二阶段和第一阶段是什么关系？`★`

> 不是补功能，也不是替换，是**用另一套技术栈把数据分析这条线重做一遍**。两个阶段**并存**，
> 第一阶段的 Qt 平台一行代码都没改，两边只共用同一份数据源——业务库 `charging.db` 的只读快照。

### Q0-2　你们引入了 Flask，第一阶段「Socket 编程」的考核点还在吗？`★`

> 在。Flask **不挂在 Qt 服务端上**，它是大数据子系统自己的一层，只读 MySQL 里的分析结果，
> 跟 Qt 服务端毫无关系——Qt 服务端始终是纯 Socket，不提供 HTTP。大屏也只连 Flask，不连 Qt 服务端。
> 两条链路从物理上就是解耦的。

### Q0-3　第二阶段你们怎么分工的？`★`

> 按数据流水线切成四段：**数据清洗（L1）→ 数据分析（L2）→ 可视化（L3+L4）→ 机器学习（L5）**。
> 这个切法和第一阶段职责同构：L1 本就管底层设施与数据入口，L2 本就管数据存储与服务，
> L3/L4 本就是两个界面端，L5 本就是机器学习。**同一批人、同一套职责，换一套技术栈重做一遍。**

### Q0-4　数据是真实的吗？`★★`

> 不是，是合成数据，由第一阶段的 `ml/gen_history.py` 生成，订单跨度 **2026-07-07 ~ 2026-09-04**，
> 共 **8292 笔订单 / 14 张表 / 10524 行**。所以我们**不把模型的绝对精度外推到真实部署**，
> 报告第 4 节写明了这一条。方法和流程是真的，数据规模是课程设计量级。

### Q0-5　整条链路现在能跑起来吗？`★`

> 能。复现步骤在 [PHASE2-PLAN.md](PHASE2-PLAN.md) 第 14 节。顺序是：起 HDFS → 起 Flask（:5000）→ 起大屏（:5173）。
> **顺序不能反**，大屏所有数据都来自 Flask，Flask 没起大屏就是一片空的。

### Q0-6　第二阶段怎么测的？`★★`

> [第二阶段测试用例.xlsx](第二阶段测试用例.xlsx)：**11 张表 169 条用例**，缺陷 **9 条（8 条已闭环，1 条待修复）**。
> 表格由 `scripts/gen-testcases-phase2.py` 从实际产物生成，**不手工维护**——手写的用例迟早和代码对不上。
> 另有三套自动冒烟：清洗校验 **82/82**、Flask 接口 **14/14**、大屏渲染 **15/15**。

---

# 1. L1 · 数据清洗

## 1.1 负责内容

| 项 | 内容 |
| --- | --- |
| **环节** | ① 数据清洗（流水线第一段） |
| **职责** | 把第一阶段业务库导成 **ODS 只读原始层**、部署 **HDFS**、按 SOP 走完**六阶段清洗**、产出 DWD 加工层 |
| **代码** | `bigdata/spark/{export_ods,profiling,cleaning,validation,quality_report,dwd_schema}.py`、`scripts/hdfs-ctl.sh`、`scripts/ods-to-hdfs.sh`、`scripts/check-env-phase2.sh` |
| **产物** | `bigdata/quality/01_profile.json`～`06_quality_report.md`、`08_hdfs_deploy.json`、`09_dwd_schema.json`、`10_dwd_profile.json`、`pending/`（14 份待核清单）、`bigdata/dwd/*.parquet`（11 张表） |
| **第二顶帽子** | 环境与契约（版本自检脚本、ODS 路径规范） |
| **必背数字** | HDFS **15 个文件**、权限**目录 555 / 文件 444**、属主 **`ecp_ods`**、分析身份 **`ecp_analyst`**、校验 **82/82 PASS**、问题 **11 条（Q001–Q011）**、规则 **11 条（R001–R011）**、订单 **8292 → 8292 零删除零改值** |

## 1.2 模拟提问

### Q1-1　ODS 只读你们怎么保证的？`★`

> 分两层。本地是 `chmod 444`；但 **HDFS 上 444 挡不住超级用户**——超级用户就是启动 NameNode 的那个账号，
> 也就是我们自己。所以 ODS 目录交给一个独立身份 **`ecp_ods:ecp`**，分析侧固定以 **`ecp_analyst`** 连接。
> 更关键的是：推送脚本末尾会**以分析身份试写一次，并且期待它失败**——
> **只读这件事我们是验出来的，不是声称的。**

**追问：为什么不干脆在代码里判断一下？** `★★`
> 代码里的判断只约束我们自己写的代码；权限约束的是**所有访问者**，包括手工敲的命令。
> SOP 第 2 节要求原始快照一个字节不改，这是「可复现、可回溯」的唯一保证，靠自觉靠不住。

### Q1-2　SOP 的六个阶段具体是哪六个？每个交了什么？`★`

| 阶段 | 名称 | 产物 |
| --- | --- | --- |
| ① | 数据探查 | `01_profile.json`——14 表逐表行数、列数、缺失率、重复行 |
| ② | 质量评估 | `02_issues.csv`——六维度问题清单 **11 条** |
| ③ | 制定规则 | `03_rules.md`——**R001–R011**，每条含编号/对象/检测条件/处理动作/依据/影响评估/异常去向/验证方式**八个字段** |
| ④ | 执行清洗 | `bigdata/dwd/*.parquet` + `04_clean_log.json` + `pending/` 待核清单 |
| ⑤ | 清洗校验 | `05_validation.json`——**82 项断言，0 失败** |
| ⑥ | 报告存档 | `06_quality_report.md`——由脚本从①～⑤产物生成，**不手写** |

### Q1-3　六个质量维度是哪六个？各查出什么？`★★`

> 完整性、准确性、一致性、时效性、唯一性、有效性。
> - **完整性 8 条**：`t_pile_log` 的 `old_status`（缺失 100%）、`new_status`（98.67%）近乎全空；`event` 字段 0 行记录；几张事实表数据量不足
> - **准确性 0 条**：**6477 笔已结算订单，金额与单价 × 电量逐笔吻合，容差 1 分**
> - **一致性 1 条**：冻结用户存在订单 4136 笔——判定为正常业务
> - **时效性 1 条**：最新结算滞后 10 天，合成数据集属预期
> - **唯一性 0 条**：完全重复行 0，`order_no` 无重复
> - **有效性 1 条**：1 条手机号不合规

### Q1-4　为什么一条记录都没删？`★`

> SOP 把删除列为**最后手段**。实测 11 个问题里没有一个需要删行：
> - 1 条非法手机号 → **标记待核**，不删不改（R006）
> - 冻结用户有订单 → **正常业务**，冻结是后置管理动作，历史订单本就该在（R007）
> - 取消订单的时间字段缺失 → **合理缺失**，`start_time` 缺失 **1815 条恒等于取消单数**，没开始充过电当然没有开始时间
> - 两列近乎全空的字段 → **剔除列，不动行**（R008）
>
> 所以进来 8292 笔，出去还是 8292 笔。**删除、修正、填充都要留痕，无法判定的进待核清单，不静默丢弃**，这是硬性规则第 9 条。

### Q1-5　那个「设备故障分析」的维度呢？`★★`

> **被实测推翻了。** 原设计是做故障分析，但探查发现 `t_pile_log.event` 是 **0 行**，
> `old_status`/`new_status` 近乎全空——依赖这些字段的维度根本不成立。
> 我们如实把 D8 降级成「设备事件时序分布」，并把这个判断写进质量报告（Q006 / R008）。
> **硬凑一张没有数据支撑的图，比降级更没有说服力。**

### Q1-6　为什么不直接读 `charging.db` 做分析？`★`

> 三个理由：**与业务库解耦**（分析不会锁住在线库）、**可重跑**（原始快照不变，任何一次重跑结果一致）、
> **可回溯**（出了问题能定位到是哪一版快照）。
> 而且代码**只认路径不认介质**——本地开发期这层是文件系统目录，答辩时是 HDFS，**同一份代码不用改**。

### Q1-7　Hadoop 是怎么部署的？怎么证明文件真的上去了？`★★`

> **Hadoop 3.3.6 伪分布式**，ODS 路径 `hdfs://localhost:9000/ecp/ods`，**15 个文件**。
> 证明在 `08_hdfs_deploy.json`：**逐文件 MD5** 校验，而且是 `hdfs dfs -cat` 取回本地重算、**比字节内容而不是块级 CRC**，
> 实测**内容不一致 0 个**。现场也可以直接 `hdfs dfs -ls -R /ecp/ods` 看权限列和属主列。

### Q1-8　版本是怎么确定的？`★★`

> **JDK 17 → Python 3.11.15 → PySpark 3.5.9**，安装顺序不能反。
> 这条栈上版本是强绑定的：**PySpark 3.5 只支持到 Python 3.11**，这一条决定了整条栈的版本组合。
> 系统自带的 3.10 不达标，apt 的 `python3.11` 候选是 RC 版，我们走的 deadsnakes。
> 版本不是从文档抄的，是 `scripts/check-env-phase2.sh` **实测输出**的——它会真建一次 `SparkSession`、真连一次 NameNode，不是只看 `pip list`。

### Q1-9　换到 HDFS 上踩过什么坑？`★★★`

> 两个，**共同点是都不报错，或者报的错指向别处**：
> 1. **Spark 报「Path does not exist」，路径看上去完全正常。** 根因是路径被包成了 `pathlib.Path`，
>    而 **`pathlib` 会折叠连续斜杠**，`hdfs://` 悄悄变成 `hdfs:/`，被当成本地相对路径。改成字符串拼接。
> 2. **清单文件 `hdfs dfs -cat` 读得到，Spark 读不到。** 根因是 Hadoop 的 `FileInputFormat`
>    **默认过滤 `_` 和 `.` 开头的文件**，我们的清单恰好叫 `_manifest.json`。改用 `FileSystem.open` 直读绕开这层过滤。
>
> 方法论收获是：**先用本地目录把全链路跑通，最后才换 HDFS**——这些坑只在换介质那一刻集中暴露，
> 一开始就背着 Hadoop 调试，它们会混在业务 bug 里一起出现。

### Q1-10　DWD 层和 ODS 层的区别？`★★★`

> ODS 是业务库的**只读快照**，一个字节不改；DWD 是**加工层**，所有清洗、纠错、归一、派生都在这里做，
> 输出 11 张 Parquet 表。这是 SOP 第 2 节的硬性要求：**原始层不可变，加工可重做**。
> 契约记在 `09_dwd_schema.json`，校验里有一组 Schema 契约断言防止它悄悄漂移。

## 1.3 现场能翻出来的证据

```bash
hdfs dfs -ls -R /ecp/ods                 # 权限列 / 属主列
cat bigdata/quality/08_hdfs_deploy.json  # 逐文件 MD5
cat bigdata/quality/06_quality_report.md # 六维度评估 + 11 条问题
cat bigdata/quality/03_rules.md          # R001–R011，八字段规则
bash scripts/check-env-phase2.sh         # 版本实测
```

---

# 2. L2 · 数据分析

## 2.1 负责内容

| 项 | 内容 |
| --- | --- |
| **环节** | ② 数据分析（流水线第二段） |
| **职责** | Spark 多维分析、MySQL 结果层、Flask 只读接口 |
| **代码** | `bigdata/spark/analysis.py`、`bigdata/api/app.py`、`bigdata/api/README.md` |
| **产物** | MySQL 分析结果表、`/api/*` 四个只读接口、`scripts/smoke-api-phase2.py` 冒烟 |
| **第二顶帽子** | 口径对账（保证同一个指标在五个环节里是同一个数） |
| **必背数字** | **13 个分析维度 + 3 组对比**、维度编号 **D1–D14**（D12 是 MLlib 回写）、MySQL **8.0.46**、Flask **4 个接口 / 冒烟 14/14**、营收总额 **53,936,279 分**，五环节对账一致 |

## 2.2 模拟提问

### Q2-1　老师要求分析维度不少于 8 个，你们做了几个？`★`

> **13 个分析维度 + 3 组对比分析**，超出要求。维度编号是 D1–D14 共 14 格，
> 其中 **D12 是机器学习的负荷预测回写**，归第四段；Spark 侧自己产出的是 13 个。
> 现场可查：`curl /api/dimensions` 里的 `dimension_count` 和 `comparison_count`，**不是 PPT 上写的数字，是接口实时报的**。

**13 个维度速览**（被要求「念一遍」时照这个念）：

| 编号 | 维度 | 编号 | 维度 |
| --- | --- | --- | --- |
| D1 | 按日营收 / 订单量 / 电量（60 天） | D8 | 设备事件时序 |
| D2 | 6 站营收横向排行 | D9 | 每日碳排放与峰平谷（60 天） |
| D3 | 24 桩利用率 | D10 | 站点经纬度 + 营收气泡 |
| D4 | 24 小时下单分布 | D11 | 充电时长分桶 × 快慢充 |
| D5 | 充电量分桶 | D13 | 分站碳排放汇总 |
| D6 | 订单终态构成 | D14 | 最近 15 日碳排放明细 |
| D7 | 电桩三态与在线率 | | |

### Q2-2　挑一个维度讲讲口径。`★`

> 挑 **D1 营收趋势**：只取**已结算订单**（`status = 3`），按 `settle_time` 聚合到日，跨度 60 天。
> 金额**全程整数「分」**，只在展示层除 100——这是硬性规则第 7 条，
> 八千多笔订单如果读成 float 再累加，误差会攒出来。电量同理，是 `kwh_x100`。

### Q2-3　三组对比得出了什么结论？`★`

> **C1 快充 vs 慢充**：慢充均时长 **282.5 分钟**是快充 **38.5 分钟**的 **7.3 倍**；
> 取消率 **32.76%** 接近快充 **17.33%** 的两倍；但**度均价同为 147 分**——
> 说明**单价是按站点定的，不按桩型定**，差异全落在电量和时长上。
>
> **C2 工作日 vs 周末**：工作日日均 **112.5 单**高于周末 **95.4 单**，
> 但单均金额几乎持平（**83.38 元 vs 82.93 元**）——说明周末是**来的人少**，不是**单笔变小**。
>
> **C3 六站横向对标**：深圳湾公园单量和时长满分，但**取消率最高**；市民中心营收、单价双第一。

**追问：那运营上该怎么办？** `★★★`
> C1 的结论指向「慢充桩的取消率要单独治理」——占桩时间长、取消率又高，翻台率被压住。
> C2 的结论指向「周末问题是引流不是客单价」，所以促销应该拉人次而不是拉单笔金额。
> 这些建议我们在大屏上做成了「智能运营建议」面板。

### Q2-4　营收这个数你们怎么保证各处一致？`★★`

> 这是我们比较自豪的一条。营收总额 **53,936,279 分**在**五个环节**上逐一对账，完全一致：
> 清洗校验 → 7 个分析维度 → MySQL 落库 → 机器学习的特征面板守恒自检 → 第一阶段一个独立的碳排放对拍脚本。
> **最后一个是第一阶段的代码算出来的，等于交叉验证。**
>
> 为什么这么较真：**口径在环节之间悄悄漂移是这类项目最常见的暗伤**。
> 第一阶段我们踩过——大屏和管理端把同一个指标显示成了两个形状。所以第二阶段每一环都显式对账，不靠「应该没问题」。

### Q2-5　为什么 MySQL 只存结果不存明细？`★`

> 明细在 HDFS 的 ODS 原始层和 DWD 加工层，**MySQL 是给 Flask 查的结果层**，只存聚合后的行。
> 两个好处：**查询压力可控**（每张结果表都是几行到几十行，最大的 D1 也只有 60 行）；
> **避免两处存同一份明细导致口径分叉**。这也是硬性规则第 8 条的要求。

### Q2-6　Flask 有几个接口？为什么是只读的？`★`

> **4 个**：`/api/health`、`/api/dimensions`、`/api/dimension/<name>`、`/api/overview`。
> 只读是硬性规则第 8 条：**只查 MySQL 分析结果表，不写任何业务库，不现场触发 Spark job**。
> 现场触发 Spark 会让一个 HTTP 请求挂着几分钟，而且把分析的可重跑性破坏掉。
>
> 一个细节：`/api/health` **会真的探一次库**，避免出现「服务活着但库连不上」的假健康。

### Q2-7　Flask 怎么防 SQL 注入？`★★`

> `/api/dimension/<name>` 的 `name` 会拼进 SQL 的**表名位置**——表名没法用参数绑定，
> 所以**白名单是唯一防线**：只接受启动时从 `information_schema` 读到的表名，其余一律 404。
> 冒烟里有一条专门的注入尝试用例（`d1_revenue_trend; DROP TABLE ...`），验证它被挡下**且目录完好**。
> 另外服务**只监听 127.0.0.1**——它本身无鉴权，不应该暴露到局域网，生产部署需要加反向代理和鉴权。

### Q2-8　加一个新维度，接口要改代码吗？`★★`

> 不用。**维度目录不是硬编码的**，`app.py` 启动时从 `information_schema` 读表名，
> `analysis.py` 增删维度之后重启一下服务就生效。这也是为什么 `dimension_count` 能当现场证据用——它是数出来的，不是写死的。

### Q2-9　分析这边踩过什么坑？`★★★`

> 一个很隐蔽的：**大屏上某个图「是空的」，浏览器不报任何错**。
> 根因是 MySQL 的 `SUM()` 经 pymysql 驱动返回的是 **`Decimal`**，Flask 默认把它序列化成**字符串**（`"78.11"`），
> 图表库拿到字符串**不报错也不画**。修法是在序列化层统一转 `float`，并且加了一条防回归断言。
> 这类坑的特征是**报错不指向根因，甚至根本不报错**。

### Q2-10　为什么不用 Spark SQL 直接对外提供查询？`★★★`

> 因为 Spark job 是**离线批处理**，启动一个 `SparkSession` 就要几十秒，做不了在线接口。
> 分层的意义正在这里：Spark 负责算，MySQL 负责存结果，Flask 负责查——各自在自己合适的时间尺度上工作。

## 2.3 现场能翻出来的证据

```bash
curl 127.0.0.1:5000/api/dimensions            # dimension_count / comparison_count
curl 127.0.0.1:5000/api/dimension/c1_fast_vs_slow
mysql> SHOW TABLES;                            # 结果表清单
.venv-phase2/bin/python scripts/smoke-api-phase2.py   # 14/14
cat bigdata/api/README.md                      # 接口清单 + 安全说明
```

---

# 3. L3 · 可视化（工程与布局）

## 3.1 负责内容

| 项 | 内容 |
| --- | --- |
| **环节** | ③ 可视化（流水线第三段，与 L4 合作） |
| **职责** | Vite + Vue3 工程搭建、DataV 组件集成、**5 页布局与路由**、取数层 |
| **代码** | `bigdata/web/src/{App.vue,pages.js,api.js,main.js,style.css}`、`bigdata/web/src/components/{Panel,EChart,DataTable}.vue`、`vite.config.js` |
| **第二顶帽子** | 集成与构建 |
| **必背版本** | Node **v24.1.0**、Vue **3.5.42**、Vite **6.4.3**、ECharts **5.6.0**、DataV **`@kjgl77/datav-vue3` 1.7.4** |
| **必背数字** | **5 页 18 面板**、DataV 用了 **5 类组件** |

## 3.2 模拟提问

### Q3-1　DataV 是哪个 DataV？`★`

> **`@kjgl77/datav-vue3`，开源组件库**，不是阿里云那个收费的 SaaS 产品。
> 任务书要求的就是开源的 Vue3 组件库。我们用了它 **5 类组件**：边框、装饰、**数字翻牌**、**胶囊图**、**活动环图**。

### Q3-2　Node 和 Vue 版本达标吗？`★`

> 要求 Node 23+ / Vue 3，我们是 **Node v24.1.0 / Vue 3.5.42**。
> 选 v24 不选 v23 是因为 **v23 是奇数线、已经 EOL 了**，v24 是 LTS。版本在 `package.json` 里可查。

### Q3-3　第一阶段不是已经有大屏了吗？为什么要重建前端工程？`★`

> 第一阶段那个是**单文件 HTML + CDN 引 ECharts**，没有 `package.json` 也没有构建工具，无法演进；
> 而且它的取数方式是**轮询一个静态 JSON 快照**，因为 Qt 服务端是纯 Socket，浏览器连不上。
> 第二阶段要求 Node 23+ 和 Vue3，所以重建成 Vite + Vue3 工程。
> **要强调的是：统计口径直接复用第一阶段，重写的只是渲染层。**

### Q3-4　大屏怎么取数？会不会连到 Qt 服务端？`★`

> 不会。大屏只走 **Flask 的 HTTP 接口读 MySQL**，Qt 服务端始终是纯 Socket、不提供 HTTP。
> 开发和预览都通过 **Vite 的 `/api` 代理**走同源（`vite.config.js` 里配的 `/api` → `127.0.0.1:5000`），
> 所以**前端代码里的路径在 dev 和 preview 两种模式下完全一致**，不用写环境判断。

### Q3-5　五页分别是什么？`★`

> **运营总览 / 站点与设备 / 对比分析 / 负荷预测 / 碳排放**，共 **18 个面板**（15 个图表 + 3 张滚动表格）。
> 每个面板左上角带**维度编号角标**（D1、D6、D2 这些），和数据分析那边的维度表一一对应——
> 答辩时老师要查哪个维度，可以直接点。

### Q3-6　工程是怎么组织的？加一个面板要改几处？`★★`

> 三个通用组件：`Panel.vue`（统一的面板外框与标题、维度角标）、`EChart.vue`（ECharts 容器）、`DataTable.vue`（DataV 滚动表格）。
> 页面布局和面板清单集中在 **`pages.js`**，一处声明；图表配置集中在 `charts.js`；取数集中在 `api.js`。
> 加一个面板正常只动 `pages.js` 和 `charts.js` 两处。
>
> **但有个教训**：`bigdata/web/README.md` 里的面板对照表和渲染冒烟脚本**也要跟着改**，
> T7 加「负荷预测」页时这两处都漏了——详见 L4 那边的 BUG-P2-001。

### Q3-7　集成 DataV 时有没有踩坑？`★★`

> 有两个：
> 1. **DataV 的样式要手动引入。** 它的 `package.json` 里**没有 `style` 字段**，但 `dist/style.css` 确实存在。
>    不手动引的话边框和装饰组件全是裸的——不报错，就是难看。处理在 `src/main.js`。
> 2. **胶囊图要用「万元」不能用「元」。** 它底部会画数值刻度轴，10 万级的数字会把刻度标签挤成一串连在一起的乱码，
>    实测挤出来是 `021139422786341784556105695` 这种东西。

### Q3-8　为什么 `EChart.vue` 用 `ResizeObserver` 而不是 `window.resize`？`★★★`

> 因为大屏是**网格布局**，面板自身的尺寸变化不一定会触发 `window.resize`——
> 比如某一列内容变了导致行高重算，窗口根本没动。`ResizeObserver` 观察的是元素本身。
> 另外组件销毁时**必须 `dispose()`**，否则大屏长时间挂着会泄漏。

### Q3-9　大屏为什么是深色底？`★★★`

> 大屏是远距离、长时间常亮的场景，深色底降低整体亮度、让数据色块跳出来，这也是 DataV 这类组件库的默认语境。
> 分辨率按 **1920×1080** 设计。

### Q3-10　构建产物怎么部署？`★★★`

> `npm run build` 出 `dist/`，`npm run preview` 起在 4173 端口做生产预览。
> 因为 `/api` 走的是代理，正式部署时把代理换成 Nginx 的 `location /api` 转发即可，前端代码不用改。

## 3.3 现场能翻出来的证据

```bash
cat bigdata/web/package.json          # Node / Vue / Vite / ECharts / DataV 版本
cat bigdata/web/README.md             # 面板 ↔ 维度 ↔ 图表形态 对照表
cat bigdata/web/src/pages.js          # 5 页 18 面板的声明
node -v && npm -v
```

---

# 4. L4 · 可视化（图表与测试）

## 4.1 负责内容

| 项 | 内容 |
| --- | --- |
| **环节** | ③ 可视化（流水线第三段，与 L3 合作） |
| **职责** | **15 个 ECharts 图表实现**、9 类图表形态、3 张滚动表格、**渲染冒烟测试** |
| **代码** | `bigdata/web/src/charts.js`、`scripts/smoke-screen-phase2.py`、`scripts/gen-testcases-phase2.py` |
| **第二顶帽子** | 测试与用例集 |
| **必背数字** | **9 类图表形态**、渲染冒烟 **15/15 PASS**、测试用例 **11 张表 169 条**、缺陷 **9 条（8 闭环 / 1 待修）** |

## 4.2 模拟提问

### Q4-1　「图表格式不要过于单一」这条你们怎么满足的？`★`

> 做了 **9 类图表形态**：折线 + 渐变面积、柱、横向条形、堆叠柱、分组柱、
> DataV 活动环图、DataV 胶囊图、仪表盘、雷达、散点；另外还有 **3 张 DataV 滚动表格**。
> **形态是跟着数据选的，不是为了凑数**——比如占比用环图、排行用横向条形、地理分布用散点、多指标对标用雷达。

### Q4-2　举例说说某个图为什么用这个形态？`★★`

> - **D1 营收与订单趋势**用**折线 + 渐变面积 + 双轴**：营收是「分」、订单是「单」，量纲差三个数量级，必须双轴。
> - **D2 站点营收排行**用 **DataV 胶囊图**：只有 6 个站，排行类数据横向排布最好读。
> - **D6 订单终态**用**活动环图**：两项构成比例，环图直接表达占比。
> - **D10 站点地理分布**用**经纬度散点**，点径随营收变化——位置和量级一张图讲完。
> - **D7 在线率**用**仪表盘**：单一比例值，仪表盘最直观。

### Q4-3　为什么有两张图用对数轴？`★★`

> **C1** 四个指标的量纲从两位数跨到四位数（时长 38.5 分 vs 营收几千），
> **D11** 慢充的尾巴拖到 420 分钟、而每个桶只有个位数。
> 这两种情况下**线性轴会把小的那一侧压成贴地的一条线**，等于那半边数据白画。对数轴才能同框可读。

### Q4-4　雷达图那个取消率是不是画反了？`★★`（**这是最容易被抓的一题**）

> 没有反。**服务端已经取反归一了**——字段叫 `cancel_score`，**值越高代表取消率越低**。
> 这么做的原因是雷达图的阅读直觉是「**面积大 = 好**」；如果直接画 `cancel_rate_pct`，
> 取消率高的站反而面积大，会被读成表现好。取反之后各轴统一「越大越好」，面积大 = 综合表现好。
> 处理放在服务端而不是前端，是为了让**口径只有一处**。

### Q4-5　渲染怎么测的？`★`

> `scripts/smoke-screen-phase2.py`：**开无头浏览器逐页截图，并对 DOM 下断言**，共 **15 项，全过**。
> 不只是截图存档，是真的断言「这个面板的 canvas 画出东西了」。

### Q4-6　有没有写错过断言？`★★★`（**主动讲，这是加分题**）

> 有两条，都值得说：
> 1. **DataV 的数字翻牌渲染进 `<canvas>`，没有任何文本节点**——用 `innerText` 断言永远误报「没渲染」。
>    改成**统计 canvas 的非透明像素数**，有内容像素就算渲染成功。
> 2. **冒烟里曾经写死「面板总数 == 17」**，T7 加了「负荷预测」页之后这条断言一直失败，
>    但当时没人跑冒烟，**一直到 09-15 补测试用例重跑才发现**——这就是缺陷单 **BUG-P2-001**。
>    现在断言改成**下限式**（不少于 N 个），不再写死总数。
>
> 这条教训是：**写死等值的断言会随功能增长而失效，而且失效时看起来像「测试坏了」而不是「代码坏了」。**

### Q4-7　为什么不用 `firefox --headless --screenshot`？`★★★`

> 因为它只能截图，**截完就退出，没法在页面上执行 JS、没法查 DOM**。
> 大屏是异步取数渲染的，必须等数据回来、图表画完才能断言，需要能控制等待和求值的驱动方式。

### Q4-8　测试用例是怎么维护的？`★★`

> **不手工维护。** `scripts/gen-testcases-phase2.py` 从实际产物生成 [第二阶段测试用例.xlsx](第二阶段测试用例.xlsx)：
> **11 张表 169 条用例**，缺陷 **9 条，8 条已闭环、1 条待修复**。
> 理由和质量报告一样：**手写的用例和报告迟早会和实际结果对不上**，生成的不会。

### Q4-9　那 1 条待修复的缺陷是什么？`★★★`

> 现场按表格里的记录如实说，**不要现编**。上台前务必翻一遍缺陷表，确认这一条的现状和不修的理由。
> 口径是：**知道它在、知道影响范围、知道为什么这一轮没修**，比假装没有强。

### Q4-10　D10 那张地理图标签怎么都不全？`★★★`

> 是故意的，开了 `labelLayout.hideOverlap`。六个站点经纬度相近，标签必然互相压；
> **显示不全好过糊成一团**，完整信息在 tooltip 里，鼠标移上去能看到。

## 4.3 现场能翻出来的证据

```bash
.venv-phase2/bin/python scripts/smoke-screen-phase2.py   # 15/15 + 截图
cat bigdata/web/src/charts.js                             # 15 个图表配置
cat bigdata/web/README.md                                 # 9 类形态对照 + 6 条不显然的处理
# 第二阶段测试用例.xlsx —— 169 条用例 + 缺陷表
```

---

# 5. L5 · 机器学习

## 5.1 负责内容

| 项 | 内容 |
| --- | --- |
| **环节** | ④ 机器学习（流水线第四段） |
| **职责** | 特征工程、**6 个 GBT 模型**训练、双基线评估、预测回写 MySQL |
| **代码** | `bigdata/mllib/{features,train,predict,report}.py` |
| **产物** | `bigdata/mllib/data/h*.parquet`（特征面板）、`models/`、`eval.json`、`bigdata/quality/07_forecast_eval.md` |
| **第二顶帽子** | 文档归档 |
| **必背数字** | 特征面板 **8640 行 × 24 特征**、电量守恒偏差 **0.0000 度**、**6 个 GBTRegressor**、切分**测试末 12 天 / 验证为训练段末 8 天**、**6 个模型 3 个跑赢基线 B** |

## 5.2 模拟提问

### Q5-1　为什么不用第一阶段的 scikit-learn？`★`

> 第二阶段任务书**明文要求 Spark MLlib**。
> 但**方法论完整沿用第一阶段**：按时间切分、双基线、验证段选超参、主动说明限制——换的是框架，不是标准。

### Q5-2　预测什么？几个模型？`★`

> 说明书要求预测未来 **1h / 6h / 24h** 的充电负荷、空闲桩数量、高峰时段。
> 我们训了 **6 个 GBTRegressor**：**充电负荷 kW** 和 **并发会话数**各 3 个 horizon。
> 损失函数选 `absolute` 而不是平方损失——因为**负荷分布右偏**，平方损失会被少数峰值主导，而且 `absolute` 与 MAE 指标对齐。

### Q5-3　空闲桩数和高峰时段的模型呢？`★★`

> **由负荷和并发的预测值派生，不单独建模。**
> 理由：单独训一个分类器会出现「**负荷预测很低，但被标成高峰**」这种自相矛盾的输出。
> 派生保证了三个输出**在逻辑上永远自洽**。

### Q5-4　为什么不随机切分数据集？`★`（**这是评估纪律的核心题**）

> 因为**同一天相邻小时高度相关**。随机切会把未来的样本混进训练集，
> 模型等于见过答案，**指标会虚高到没有意义**。
> 我们按时间切：**测试是最后 12 天，验证是训练段末 8 天**。
> 同理，**基线均值也只用训练段算**——用全量算均值等于把测试段泄漏进基线，那样的基线会不合理地强，反过来衬得模型很差。

### Q5-5　为什么要两条基线？`★★`

> **基线 A** 是「历史同小时均值」（站点 × 小时）；**基线 B** 再分工作日和周末（站点 × 小时 × 是否周末）。
> 加 B 的原因是：**各站的周末效应方向相反**——办公型站点周末跌、休闲型站点周末涨，基线 A 抓不到这部分。
> 只赢 A 不能说明模型学到了周内结构，**所以我们以 B 为准，用更严的那把尺子。**

### Q5-6　模型效果如何？`★`（**如实答，不要修饰**）

> **6 个模型，3 个跑赢基线 B**：负荷 1h **+1.0%**、负荷 24h **+0.2%**、并发 1h **+7.9%**；
> 另外三个是负的（负荷 6h −3.0%、并发 6h −3.9%、并发 24h −2.1%）。
> 指标上，负荷 1h 测试 MAE **30.68**、R² **0.21**；并发 1h 测试 MAE **0.614**、R² **0.34**。
> **这张表我们没有修饰。**

### Q5-7　只赢一半，是不是没调好？`★`（**必然被追问，答案要准备充分**）

> 是**真实差异**，有两个可解释的原因：
> 1. **少了天气特征。** 第一阶段有 `weather` / `weather_code`；本阶段按硬性规则**只能用 ODS 层的数据**，
>    天气是外生合成数据、不在业务库内，引入它会让流水线依赖 ODS 之外的产物。
>    第一阶段的报告自己也写明天气对模型有贡献。
> 2. **框架差异。** scikit-learn 的 `HistGradientBoostingRegressor` 用直方图分箱 + 内建 L2 正则，
>    在 **7.5k 行**这种小样本上比 Spark GBT 更抗过拟合。
>
> **一个旁证**：并发数 24h，第一阶段是 **−1.0%**，本阶段加正则化后是 **−2.1%**。
> **两个完全不同的框架、不同的特征集，收敛到同一个数量级**——这支持第一阶段的判断：
> **这个 horizon 上可榨取的信号本来就接近零**，不是模型没调好。继续调参把它做成大正数，只会是在测试集上过拟合。

### Q5-8　为什么 h 越大提升越小？`★★★`

> 是**数据决定的，不是模型缺陷**。训练数据的跨日持续性来自一个 AR(1) 过程，
> **去掉季节性之后，t−24h 的自相关本就很弱**，理论上可榨取的增量接近零。
>
> 还有一条必须一并说明的：**基线其实是模型的一个输入**——
> 特征里含扩展窗口口径的同小时均值（`load_seas_mean` / `sess_seas_mean`，只聚合严格早于当前样本的同键观测，构造上无穿越）。
> 所以那张表不应该读成「模型比基线聪明多少」，而是「**在季节均值之上还能再榨出多少**」。

### Q5-9　超参怎么选的？`★★`

> 网格共 **5 组**，**在验证段上按 MAE 选**，选完用完整训练段重训。
> **绝不用测试段选超参**——那是拿答案调参。
> 网格**刻意开得小**：样本只有 7.5k 行，网格开大只会过拟合验证段。调参走了四轮，每轮的问题与修正记在 `train.py` 的 GRID 注释里。

### Q5-10　特征是怎么做的？怎么保证特征没算错？`★★`

> 从 DWD 层（**不是业务库**）构建**站-小时面板**：6 站 × 小时粒度，**8640 行 × 24 个特征**。
> 正确性靠**电量守恒自检**：摊分到小时之后的总电量，与订单原始总电量**偏差 0.0000 度**。
> 营收也参与了那条五环节对账（**53,936,279 分**）。**特征工程最容易出的错是摊分时对不上账，所以要显式校验。**

### Q5-11　预测结果怎么给到大屏？`★★`

> `predict.py` 把结果**回写 MySQL** 的 `d12_load_forecast` 表（**18 行**），
> 之后就和其他维度一样由 Flask 提供、大屏第 4 页展示——右边那张滚动表格里就是空闲桩数和高峰时段。
> **机器学习没有自己的一条出口，它复用分析侧的整条通路。**

### Q5-12　训练踩过什么坑？`★★★`（**这个坑很有意思，值得主动讲**）

> 两个：
> 1. **训练跑了 3.5 小时「没动静」，其实早就崩了。** 等待脚本用 `pgrep -f "train.py"` 判断训练进程还在不在，
>    **结果它匹配到了自己**（它自己的命令行里就含 `train.py` 这个字符串），条件永远成立，
>    于是在训练早就崩掉之后，它还持续报告了三个半小时「在跑」。改用 PID 文件。
> 2. **GBT 的血缘（lineage）线性增长导致爆栈。** 修法是设 `checkpointInterval`，
>    但有个陷阱：**不设检查点目录的话这个参数会静默不生效**——不报错、不警告，就是没效果。

### Q5-13　这个模型能直接上线吗？`★★★`

> 不能直接外推。训练数据是合成的，而且本轮去掉了天气特征。
> **真实部署这里应该接真实的天气预报**，并且要重新评估。
> 我们在报告第 4 节把这四条限制都写在前面了——**主动讲，不等被问**。

## 5.3 现场能翻出来的证据

```bash
cat bigdata/quality/07_forecast_eval.md   # 双基线评估表 + 4 条限制 + 调参留档
cat bigdata/mllib/eval.json               # 报告的数据来源（报告由它生成，不手写）
mysql> SELECT * FROM d12_load_forecast;   # 回写的 18 行
```

---

# 6. 兜底话术与上台前检查

## 6.1 答不上来的时候

**三句话，按顺序用**：

1. **「这个数字我记不准，现场可以翻出来——在 `bigdata/quality/xx` 里。」**
   　→ 承认 + 指路。比现编一个数字好一万倍，**编错的数字老师一对就露馅**。
2. **「这一块是 X 号线负责的，我说个大概，细节请 X 补充。」**
   　→ 分工清楚本身就是加分项，不要硬扛别人的段落。
3. **「这一条我们确实没做，原因是……」**
   　→ 没做就说没做，把**取舍理由**讲清楚。我们主动交代的局限本来就有四条，多一条不丢人。

**绝对不要做的三件事**：现编数字、把负面结果说成正面（尤其是模型那张表）、用「应该是」「大概」去描述一个可以现场查证的事实。

## 6.2 三个高频追问（人人都要能答出一句）

| 追问 | 一句话版本 |
| --- | --- |
| 原始层只读怎么保证？ | 独立 HDFS 身份 + 444 权限，**推送脚本末尾以分析身份试写并期待失败**——验出来的，不是声称的 |
| 十三个维度怎么来的？ | 探查数据有什么 → 质量评估砍掉撑不住的（D8 就被降级了）→ 剩下的按业务问题组织，**不是先定 13 个再凑** |
| 模型为什么没全赢基线？ | 少天气特征 + 框架差异，**是真实差异**；并发 24h 两阶段收敛到同一数量级，佐证该周期信号本就接近零 |

## 6.3 上台前检查

- [ ] 本稿**打印五份，每人一份**，不是只给主讲人
- [ ] 每个人把自己那节的 `★` 题**出声讲一遍**，卡壳的立刻去翻对应文件
- [ ] 加粗的数字**当场默写一遍**，特别是：8292 / 82 项 / 13+3 / 15 个文件 / 6 个模型赢 3 个 / 53,936,279 分
- [ ] 起 HDFS → 起 Flask（:5000）→ 起大屏（:5173），**顺序不能反**
- [ ] 确认那 1 条待修复缺陷的**当前状态和不修的理由**（Q4-9）
- [ ] ⚠ **口径一致性风险**：仓库里 `bigdata/api/README.md`、`bigdata/web/README.md`、`07_forecast_eval.md`
      的文件头都标着「归属 **L5**」（那是实际开发期的归属），而答辩分工把它们分给了 L2 / L3 / L4。
      **老师如果当场翻到这些文件，会看到和汇报不一致的署名**——
      上台前统一口径：要么改这些文件头，要么准备一句「开发期由 L5 统一搭骨架，第二阶段按流水线四段重新分工，各人负责自己那段的演进与答辩」。

---

*本文件属 L5（配置管理与文档归档）维护，分工口径与 [DEFENSE-SCRIPT-PHASE2.md](DEFENSE-SCRIPT-PHASE2.md) 第 2 节保持一致。*
*数字全部取自 2026-09-15 的仓库状态，重跑后如有变化，以 `bigdata/quality/` 下的实际产物为准，不要照抄本稿。*
