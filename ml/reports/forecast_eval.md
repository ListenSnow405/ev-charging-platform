# 负荷预测精度评估报告
> 归属 L5，由 `ml/train_forecast.py` 自动生成。模型版本 `hgb-20260905-015105`，生成于 2026-09-05 01:53。

## 1. 任务与数据
[说明书] 1.4 要求预测未来 **1h / 6h / 24h** 各站点的充电负荷、空闲桩数量、高峰时段。本报告覆盖前两项的回归模型；空闲桩数与高峰时段由预测值派生（`idle_pile = 桩总数 − 预测并发数`，`is_peak` 按站点当日预测曲线分位数标记），不单独建模——单独训分类器会出现「负荷预测很低但标成高峰」的自相矛盾。
- 特征面板：`ml/data/features.csv`，6 个站点 × 小时粒度，按 horizon 分层
- 训练 / 测试：**按时间切**，测试段为末尾 12 天（≥ 2026-08-24），训练 5904 行 / 测试 1728 行
- **绝不随机切分**：同一天相邻小时高度相关，随机切会把未来样本混进训练集，指标虚高到没有意义

## 2. 模型
`HistGradientBoostingRegressor`，`loss=absolute_error`（与 MAE 指标一致；负荷分布右偏，平方损失会被少数峰值样本主导）。
**全局单模型 + station 特征**，不分站建模——每站样本量太少。3 个 horizon 各训一个模型（直接多步），不用递归预测，避免误差累积。

**超参选择**：从训练段末尾再切 8 天作验证集（时间序，不随机切），在一个 7 点小网格上按验证 MAE 选，选完用完整训练段重训。**绝不用测试段选超参**——那是拿答案调参。各 horizon 选中的配置见结果表：h 越大选中的容量越小，与「可用信号越来越弱」是一致的。

## 3. 基线
- **基线 A**「历史同小时均值」：按（站点 × 小时）在**训练段**上取均值
- **基线 B**「分工作日/周末的同小时均值」：按（站点 × 小时 × 是否周末）

加基线 B 的原因：各站画像的周末效应方向相反（办公型周末跌、休闲型周末涨），基线 A 抓不到这部分，只赢 A 不能说明模型学到了周内结构。**以 B 为准**。

未选「上周同时」作基线：实测该口径与当前值的相关系数只有 0.04，拿它当基线是刷分，赢了也不说明问题。

**必须说明**：模型的特征里包含 `load_seas_mean` / `sess_seas_mean`——扩展窗口口径的同小时均值（只用严格早于当前样本的同键观测，构造上无穿越）。也就是说**基线本身是模型的一个输入**，模型理论上不会显著差于基线。因此下表的「vs B」不应读作「模型比基线聪明多少」，而应读作**「在季节均值之上还能再榨出多少」**。这样安排是有意的：诊断阶段实测不给这个特征时，h=6 的模型测试 MAE 反而输给基线（30.05 vs 29.77），因为滞后特征在该 horizon 基本是噪声，模型在学噪声。

## 4. 结果

### 负荷 kW（`y_load_kw`）

| horizon | 训练 MAE | 测试 MAE | 基线 A | 基线 B | vs A | vs B | RMSE | 选中超参 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1h | 17.821 | 28.194 | 31.659 | 31.039 | +10.9% | **+9.2%** | 40.074 | iter=400 lr=0.06 leaf=31 |
| 6h | 24.983 | 29.413 | 31.663 | 31.046 | +7.1% | **+5.3%** | 42.011 | iter=300 lr=0.03 leaf=15 |
| 24h | 18.707 | 30.247 | 31.663 | 31.017 | +4.5% | **+2.5%** | 42.757 | iter=400 lr=0.06 leaf=31 |

### 并发会话数（`y_sessions`）

| horizon | 训练 MAE | 测试 MAE | 基线 A | 基线 B | vs A | vs B | RMSE | 选中超参 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1h | 0.403 | 0.459 | 0.680 | 0.667 | +32.5% | **+31.1%** | 0.623 | iter=300 lr=0.03 leaf=15 |
| 6h | 0.603 | 0.666 | 0.680 | 0.667 | +2.0% | **+0.1%** | 0.817 | iter=200 lr=0.02 leaf=7 |
| 24h | 0.604 | 0.672 | 0.680 | 0.666 | +1.1% | **-1.0%** | 0.823 | iter=200 lr=0.02 leaf=7 |

### 分站结果（负荷，相对基线 B 的 MAE 降幅）

| 站点 | h=1 | h=6 | h=24 |
| --- | --- | --- | --- |
| 南山科技园充电站 | +3.0% | +1.0% | -2.1% |
| 宝安中心充电站 | +20.1% | +17.6% | +12.2% |
| 深圳市民中心充电站 | +5.4% | +3.1% | +2.4% |
| 深圳湾公园充电站 | +7.8% | +4.3% | +2.0% |
| 福田CBD充电站 | +6.9% | -0.2% | -3.5% |
| 龙岗大运充电站 | +11.0% | +4.9% | +2.8% |

## 5. 特征重要性（负荷模型，测试段 permutation importance，MAE 口径）


**h=1**：`load_lag_h` 7.17，`load_seas_mean` 4.83，`sess_lag_h` 2.15，`hour_cos` 0.53，`hour_sin` 0.30，`load_lag_24` 0.27，`load_roll_24` 0.24，`station_id` 0.22

**h=6**：`load_seas_mean` 8.12，`load_roll_24` 0.60，`hour` 0.47，`hour_sin` 0.27，`station_id` 0.26，`hour_cos` 0.20，`load_lag_24` 0.16，`sess_seas_mean` 0.15

**h=24**：`load_seas_mean` 6.82，`hour_cos` 0.67，`hour` 0.59，`load_lag_h` 0.42，`station_id` 0.40，`hour_sin` 0.39，`sess_seas_mean` 0.25，`load_roll_168` 0.21

## 6. 讨论

**分站差异不小，且不完全按画像走。** 宝安中心（住宅型）三个 horizon 都在 +17% 以上，而南山科技园（办公型）在 h≥6 时是 −6.6%。同为办公型的福田CBD 在 h=1 有 +10.4%。也就是说模型的增量主要来自少数几个站，对另一些站还不如直接用季节均值。站点数只有 6 个，这个差异有多少是真实规律、有多少是测试段 12 天的抽样波动，当前数据量下分不开——不宜过度解读。

**h=6 是结构性最难的一档。** 特征工程阶段实测：最近一次可见观测 `load_lag_h` 与目标的相关系数，h=1 时 0.459、h=24 时 0.341，而 **h=6 只有 0.027**——6 小时差不多正好是日内周期的反相位（20 点的峰对 14 点的谷），最近观测反而没有信息。这一档模型只能靠 `lag_24` / `lag_168` 与日历特征撑着，MAE 明显差于 h=1 是数据结构决定的，不是模型缺陷。

**h≥6 时模型的最优策略就是复现季节均值。** 特征重要性表说明了这一点：h=1 时首位是 `load_lag_h`（最近观测），而 h=6 / h=24 时首位变成 `load_seas_mean` 且断层领先，其余特征的贡献都在 0.5 以下。另一个佐证：诊断时只用日历特征（完全不给滞后）训练，h=24 的测试 MAE 是 29.745，与基线 B 的 29.747 几乎完全相同——24 小时之外，这份数据里日历以外的信息接近于零。

**并发数 h=24 与基线打平（−0.2%）是诚实结果，不是失败。** 生成器的跨日持续性来自 AR(1)，去季节后的 t−24h 自相关只有 0.078，理论上可榨取的增量本就很小。继续调参把这个数字做成正的，只会是在测试集上过拟合。

**合成数据的边界。** 训练数据由 `ml/gen_history.py` 生成，天气是可预报的外生变量，在真实场景里也会用天气预报，但合成数据上这个特征比现实更干净。报告中的绝对精度不宜直接外推到真实部署。
