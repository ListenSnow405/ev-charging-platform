# CLAUDE.md — 全组 Agent 共享上下文

> 本文件是本项目 **agent 上下文的唯一事实来源**。使用 Codex 的成员见 [AGENTS.md](AGENTS.md)，该文件内容以本文件为准，两份不得各写各的。
> 分工与排期见 [DIVISION-OF-LABOR.md](DIVISION-OF-LABOR.md)　|　需求见 [docs/project-spec.md](docs/project-spec.md)

## 0. 标记约定（全项目通用）

本项目所有文档、SQL 注释、代码注释统一使用两个标记，区分「必须遵守」与「可以商量」：

| 标记 | 含义 | 变更方式 |
| --- | --- | --- |
| `[说明书]` | 说明书正文明文要求的技术、字段、流程或默认值 | **不得擅自更改。** 如需偏离必须由 PM 确认，并记入 `docs/conventions.md` 变更记录 |
| `[本组自定]` | 说明书未作规定，本组自行决定（含推荐做法） | 按下方「契约变更流程」走评审即可调整 |

**选型总约定**：技术选型一律以说明书**正文文字说明**（1.2 / 1.4 / 1.5 / 1.6 / 2.x）为准；`docs/assets/project-spec/system-architecture.png` 系统结构图仅作模块与界面参考，**不作为选型依据**。

## 1. 项目一句话

Linux + Qt 的电动汽车充电桩管理平台：充电用户端、PC 管理端两个 Qt 客户端，通过自定义 TCP Socket 连接一个多线程业务服务端，数据存于 QSQLite；另有 ECharts 大屏与 Python 机器学习负荷预测。

## 2. 技术基线

| 技术项 | 结论 | 标记 | 依据 |
| --- | --- | --- | --- |
| 运行系统 | Ubuntu 22.04 及以上 | `[说明书]` | 1.5 |
| 开发工具 | Qt Creator 6.2 及以上 | `[说明书]` | 1.5 |
| UI 框架 | Qt | `[说明书]` | 1.6 |
| 核心语言 | C++ | `[说明书]` | 1.6 |
| 业务数据存储 | QSQLite | `[说明书]` | 1.6 |
| 网络通信 | Socket 编程 | `[说明书]` | 1.6 |
| 并发模型 | 多线程 pthread，主框架为多线程结构 | `[说明书]` | 1.6 + 1.2 |
| C++ 标准 | C++17 | `[本组自定]` | — |
| Qt 版本 | **Qt 6.2.4**（Ubuntu 22.04 apt 版本，全组一致）。`.pro` 中 `QT += core network sql charts webenginewidgets`，按模块裁剪 | `[本组自定]` | 环境清单见 [docs/conventions.md](docs/conventions.md#5-环境基线) |
| 报文格式 | 4 字节长度包头（网络字节序）+ UTF-8 JSON 体 | `[本组自定]` | 说明书 2.2 只要求「通信数据结构设计」 |
| JSON 实现 | Qt 自带 `QJsonDocument`，不引入第三方库 | `[本组自定]` | — |
| 服务端进程 | 独立的 Qt Core 控制台程序（非 GUI），链接 QtCore/QtNetwork/QtSql | `[本组自定]` | 说明书要求 QSQLite 与 Socket，需一个承载进程 |
| 金额表示 | 一律用**整数「分」**存储与计算，仅显示层除 100 | `[本组自定]` | — |
| 时间表示 | SQLite `TEXT`，格式 `YYYY-MM-DD HH:MM:SS`（本地时间） | `[本组自定]` | 便于 `date()` 按日聚合近 7/30 日营收 |
| 大屏取数 | L5 只读 SQLite 定时导出 JSON 快照，大屏轮询静态文件 | `[本组自定]` | **说明书未规定大屏取数方式**；浏览器无法连接自定义 TCP，此方案使服务端保持纯 Socket |

## 3. 冻结契约（改动前必读）

以下四份成果物为**冻结契约**，定稿后进入受控状态：

| 契约 | 属主 | 其他人权限 |
| --- | --- | --- |
| `docs/protocol.md` | L1 | 只读 |
| `docs/db-schema.sql` | L2 | 只读 |
| `common/**` | L1 | 只读 |
| `CLAUDE.md` / `AGENTS.md` | 全组评审 | 只读 |

**变更流程**：① 群里提出，说明改什么、为什么、影响哪几条线 → ② 属主与受影响担当评审，PRL 把关 → ③ **由属主一人修改** → ④ 群里通知，所有人**重新同步各自 agent 的上下文**（重新读本文件与契约文件）→ ⑤ SCML 在 `docs/conventions.md` 记一条。

## 4. 目录归属与写权限

**agent 只能修改自己所属目录下的文件。** 需要改别人的目录或契约文件，走上面的变更流程，不要自己动手改了再说。

```text
CLAUDE.md / AGENTS.md    全组      共享上下文，变更需评审
docs/protocol.md         L1        通信协议（只读契约）
docs/db-schema.sql       L2        数据库 schema（只读契约）
docs/conventions.md      SCML      规范与变更记录
common/                  L1        协议/错误码/日志/时间/帧编解码（只读契约）
server/net/              L1        socket · pthread 线程池 · 会话
server/biz/              L2        九个业务服务
server/dao/              L2        SQLite 访问层
admin-client/            L3        PC 管理端（含 mock/ 数据源）
user-client/             L4        充电用户端（含 mock/ 数据源）
dataviz/                 L5        ECharts 大屏 + JSON 快照
ml/                      L5        数据生成器 · 模型 · 回写脚本
tools/                   L1        电桩模拟器
scripts/                 SCML      环境自检等辅助脚本
```

## 5. 五条硬性规则（agent 最常踩的坑）

1. **收发必须走 `common/frame.h` 的 `FrameCodec`，禁止假设一次 `read()` 就是一个完整包。** TCP 有粘包与半包；直接把一次读到的字节当一帧解析，小报文测试看不出问题，报文变长或连发必错乱。

2. **禁止跨线程共享同一个 `QSqlDatabase` 连接。** 服务端是 pthread 线程池，每个工作线程必须用自己的连接名 `QSqlDatabase::addDatabase("QSQLITE", connName)`；连接名用线程 id 生成。

3. **金额一律用整数「分」（`qint64`），禁止出现 `double` 或 `float` 的金额变量。** 计费、余额、充值、营收统计全部如此，只在界面显示时除以 100 保留两位。

4. **禁止在网络线程里直接操作 Qt 控件。** 跨线程一律 `emit` 信号 + `Qt::QueuedConnection`，由 UI 线程的槽函数更新界面。直接 `setText()` 会随机崩溃且难复现。

5. **本项目是 Qt 6.2.4，禁止使用 Qt 5 的已废弃 API。** 用 `QRegularExpression` 而非 `QRegExp`；Qt6 的 charts 类不再包在 `QtCharts` 命名空间里，不要写 `QT_CHARTS_USE_NAMESPACE`；`QString::split` 等接口注意 Qt6 签名。生成代码后先确认能在 **Qt 6.2.4** 下编译——不要凭 Qt5 记忆写。

## 6. 业务规则要点

说明书明确规定、不得改动的部分：

- `[说明书]` 管理员默认账号 **admin / 123456**
- `[说明书]` 用户端**手机号免密登录**：输入 11 位手机号，库中存在即登录；不存在则**自动注册**，默认昵称为 `用户` + 手机号后 4 位，默认灰色头像
- `[说明书]` 电桩状态三种：**在用 / 闲置 / 故障**；电桩类型两种：**快充 / 慢充**
- `[说明书]` 用户状态两种：**正常 / 冻结**，管理员可手动冻结/解冻
- `[说明书]` 进入充电页面前必须校验当前用户是否有「充电中」的未完成订单；有则弹窗提示并**强制跳转结算页**
- `[说明书]` 充电流程：预约 → 开始充电 → 计费 → 订单结算
- `[说明书]` 管理端营收看板需提供**近 7 日 / 近 30 日**趋势与**今日 / 本月 / 总营收**三项指标
- `[说明书]` 电桩管理需支持**远程重启**（模拟下发重启指令）
- `[说明书]` 用户管理需支持**按手机号模糊搜索**
- `[说明书]` 机器学习需预测未来 **1h / 6h / 24h** 的充电负荷、空闲桩数量、高峰时段

## 7. 编码规范 `[本组自定]`

- 文件与目录：小写下划线（`pile_service.cpp`）；类名 `PascalCase`；成员变量 `m_` 前缀；常量与枚举值 `UPPER_SNAKE`
- 每个 `.h` 用 `#pragma once`
- 错误处理：所有对外接口返回 `common/error_code.h` 的 `ErrCode`，不要用裸 `bool`；错误必须记日志再返回
- 日志：统一用 `common/logger.h` 的 `LOG_I / LOG_W / LOG_E`，禁止 `qDebug()` 与 `printf` 混用
- 注释：涉及说明书要求的地方，注释里带上 `[说明书]` 标记与条目号
- 提交：小步提交，信息写清楚解决了什么问题，禁止一次提交上千行

## 8. Agent 会话开始前的检查清单

1. 已读本文件与 `docs/protocol.md`、`docs/db-schema.sql`
2. 确认本次要改的文件在自己的归属目录内
3. 涉及网络收发 → 先看 `common/frame.h`；涉及数据库 → 先看 `docs/db-schema.sql`，不要自己臆造表名字段名
4. 生成完代码后，自己通读一遍再提交（评审与答辩会问「这段为什么这么写」）
