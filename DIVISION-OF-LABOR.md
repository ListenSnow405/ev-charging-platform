# 分工方案：五线并行

> 五人一人一线。**只写分工**——规则见 [CLAUDE.md](CLAUDE.md)，流程见 [WORKFLOW.md](WORKFLOW.md)，冻结与环境见 [docs/conventions.md](docs/conventions.md)。

九条工作线归并为五条。每条线除本职模块外还挂一顶**第二顶帽子**：agent 让 UI 类工作大幅提速，但联调、契约维护、异常路径测试一点没被摊薄，省下的时间必须明确回填过去，否则会消失在「我模块已经写完了」里，而联调周没人扛。

`●●●` 很高 / `●●○` 高 / `●○○` 中 —— agent 在该线的实际加速，也是第二顶帽子轻重的依据。

## 五条线

### L1 · 服务端通信与并发　`TL` 　agent 加速 `●○○`

`C++` `POSIX Socket` `pthread` — 目录 `server/net/`、`common/`、`tools/`

- 通信协议设计与 `docs/protocol.md`，冻结命令字与错误码
- TCP 服务器主框架、连接接入与会话管理
- pthread 线程池（[说明书] 1.6 考核点，不得用 QThread 替代）
- 设备控制服务：远程重启指令下发；电桩模拟器
- **第二顶帽子 · 契约维护者**：`common/` 与 `docs/` 的唯一写入人

依赖：无——本线是被依赖方，进度决定 W3 联调能否开始。
Agent 弱项：粘包/半包与共享数据加锁，生成的版本大概率是错的，必须逐行读。

### L2 · 数据存储与业务服务　`PRL`　agent 加速 `●●○`

`C++` `QSQLite` `DAO` — 目录 `server/biz/`、`server/dao/`

- 数据库 schema 与建库/种子脚本
- DAO 封装，多线程下的连接与加锁策略
- 用户 / 电站 / 电桩 / 预约 / **订单 · 计费 · 钱包** / 统计 九个服务
- **第二顶帽子 · 评审组织者**：每周一次交叉试读，重点样本是计费与订单

依赖：L1 的命令字定义；schema 反过来被所有人依赖。
Agent 弱项：金额默认写成 `double`，事务边界会被拆散。

### L3 · PC 服务器管理端　`PE`　agent 加速 `●●●`

`Qt Widgets` `QChart` — 目录 `admin-client/`

- 管理员登录
- 数据总览：今日/本月/累计营收 + 近 7/30 日趋势 + 电桩状态分布
- 充电站管理（列表 / 站内详情 / 新增）、充电桩管理（列表 + 远程重启）
- 用户管理（模糊搜索 + 冻结解冻）、订单管理
- **第二顶帽子 · 集成与构建**：维护 `scripts/build-all.sh`，W3 起任端到端联调主责

依赖：仅协议文档——先接 `mock/` 假数据，W3 再换真实链路。
这条线 agent 红利最大，腾出的时间正好补上 agent 最帮不上的联调。

### L4 · 充电用户端　`PE`　agent 加速 `●●○`

`Qt Widgets` `QWebEngineView` `腾讯地图 API` — 目录 `user-client/`

- 手机号免密登录，首次自动注册
- 附近充电站：定位 → 地理编码 → 按距离升序
- 一键导航：WebEngine 加载腾讯地图，驾车/步行路线
- 个人中心：头像 / 昵称 / 充值
- 充电全流程：未结算订单拦截 → 预约 → 充电 → 计费 → 结算
- **第二顶帽子 · 测试与回归**：维护全组测试用例集

Agent 弱项：功能路径通常没问题，**异常路径几乎必漏**——余额不足、账号冻结、断网、重复提交。
最高优先风险：WebEngine 依赖与地图 Key 申请，agent 完全帮不上，W1 第一天就要动手。

### L5 · 数据端：可视化大屏 + 机器学习　`SCML`　agent 加速 `●●●`

`Python` `pandas` `时序模型` `ECharts` — 目录 `dataviz/`、`ml/`

- **历史数据生成器**：大屏与模型共同的燃料，W1 就要交付
- 特征工程与时序建模，预测 1h / 6h / 24h 负荷、空闲桩数、高峰时段
- 预测结果回写 `t_load_forecast`；只读 SQLite 导出 JSON 快照供大屏轮询
- ECharts 大屏；输出用户端「低拥堵推荐」与管理端「负荷预警」
- **第二顶帽子 · 配置管理与文档归档**：维护 `docs/conventions.md`

技术栈与 Qt 主线完全解耦，时间弹性最大，因此承担配置职责。
有 agent 辅助，机器学习**不必降级**为移动平均基线，可按说明书完整实现。

## 角色叠加

| 角色 | 担当 | 为什么是这条线 |
| --- | --- | --- |
| PM | 指导教师 / 组长（专职） | 说明书要求专职，把握阶段进度与方向，不承担编码 |
| TL | **L1** | 服务端是全组依赖中心；兼契约维护者，天然掌握各端接入进度 |
| PRL | **L2** | 业务层同时对接两个客户端，最了解各方调用方式。全员用 agent 时此角色权重显著上升 |
| SCML | **L5** | 与 Qt 主线耦合最松、时间弹性最大 |
| PE | **L3 · L4** | 纯开发担当，另挂集成主责与测试主责 |

## 四周排期

W2 开发周压缩（agent 红利在此兑现），W3 联调周加重并提前启动（agent 帮不上，只能靠人和时间）。

| 线 | W1 契约与环境 | W2 单机跑通 | W3 联调周 | W4 加分与验收 |
| --- | --- | --- | --- | --- |
| **全组** | 需求评审 · 四项冻结 · 环境自检 | 中期同步 · 交叉试读 | 每日联调例会 | 同行评审 · 归档 · 答辩演练 |
| **L1** | 协议定稿 + `common/` + 服务端骨架 | 线程池 + 会话；粘包处理人工复核 | 全端联调 + 电桩模拟器接入 | 断线重连 · 压测 · 错误码补全 |
| **L2** | schema 定稿 + 建库脚本 | DAO + 用户/电站/电桩/订单/计费 | 统计服务收口 + 组织交叉评审 | 事务加锁复查 · 金额精度审计 |
| **L3** | 界面原型 + 登录 + 构建脚本 | 六个界面全量产出 | 换真实链路 · **端到端集成主责** | 交互打磨 · 异常提示 · 演示动线 |
| **L4** | **WebEngine + 地图 Key 打通**（第一优先） | 五个界面 + 充电流程 | 导航联调 + 回归用例集 | 异常路径回归 |
| **L5** | 数据生成器 + 大屏布局（假数据） | 特征工程 + 模型 + 全量图表 | 接真库快照 + 预测回写两端 | 精度评估 · 加分项 · 归档 |

> **按里程碑而非人天。** agent 放大了进度差异——有人两天写完模块，有人卡在环境上三天。W1 结束、W3 开始各做一次余量重分配：谁先跑完本职模块，谁就提前接联调或补测试用例。
> 每条线在 W1 都要交出「能跑的空壳」——骨架已就绪，在其上填自己那部分即可。
