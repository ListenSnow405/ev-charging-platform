# 分工方案：五线并行 · 全员 Agent 辅助

> **项目**：东软电动汽车充电桩应用管理平台
> **编制**：5 人 · 一人一线　|　**周期**：4 周　|　**关键路径**：W1 契约冻结
> **需求依据**：[docs/project-spec.md](docs/project-spec.md) 1.4 功能描述 / 1.6 关键技术 / 1.7 开发体制
> **选型约定**：技术选型一律以说明书**正文文字说明**为准，系统结构图仅作界面与模块参考，不作为选型依据

五个人、每人一个 agent，写代码不再是瓶颈。Qt 样板界面、ECharts 配置、pandas 流水线能提速两三倍；跨进程联调、WebEngine 环境依赖、需求决策和答辩讲解，agent 一点忙帮不上。

所以分工的重心要从「谁写得多」，转向**谁扛住 agent 帮不上的那部分**——并且先把契约冻结成五个 agent 都读得到的同一份上下文。

## 目录

- [第一部分 · 需求分析](#第一部分--需求分析)
  - [1.1 五个子系统拆成九条工作线](#11-五个子系统拆成九条工作线)
  - [1.2 Agent 加速分布：瓶颈已经转移](#12-agent-加速分布瓶颈已经转移)
- [第二部分 · 开工前必须冻结的四份成果物](#第二部分--开工前必须冻结的四份成果物)
  - [2.1 什么叫「冻结」](#21-什么叫冻结)
  - [2.2 四份冻结成果物](#22-四份冻结成果物)
  - [2.3 冻结之后怎么改](#23-冻结之后怎么改)
- [第三部分 · Agent 开发的总约束](#第三部分--agent-开发的总约束)
  - [3.1 目录归属与三条协作铁规](#31-目录归属与三条协作铁规)
  - [3.2 评审清单：agent 最常踩的五个坑](#32-评审清单agent-最常踩的五个坑)
  - [3.3 个人纪律：三条](#33-个人纪律三条)
- [第四部分 · 分工方案](#第四部分--分工方案)
  - [4.1 技术基线](#41-技术基线)
  - [4.2 五条线](#42-五条线)
  - [4.3 管理角色如何叠加](#43-管理角色如何叠加)
  - [4.4 四周任务矩阵](#44-四周任务矩阵)

---

## 第一部分 · 需求分析

说明书写的是五个子系统，按可交付物排出来是**九条真实工作线**。先看清工作的形状与 agent 在每条线上的实际加速，才谈得上怎么切人——这一部分的两张表是后面所有分工决策的依据。

### 1.1 五个子系统拆成九条工作线

依据说明书 1.4 功能描述逐项拆解。**第八条最容易漏**：「远程重启」「设备通信」要求有设备侧程序上报状态，而大屏和机器学习都没有真实历史数据可用，必须先造。这条线不显式派人，最后一定没人做。

| 工作线 | 技术栈 | 量级 | 阻塞谁 |
| --- | --- | --- | --- |
| 数据库设计 + DAO | SQLite / SQL | 小 | 全组，唯一的全局关键路径 |
| 通信框架（Socket + pthread） | C++ / pthread | 中 · 难度最高 | 两个 Qt 客户端 |
| 业务服务层 9 个服务 | C++ | 最大 | 客户端所有功能页 |
| PC 管理端 6 界面 + QChart | Qt Widgets | 大 | — |
| 充电用户端 5 界面 + 地图 | Qt + WebEngine | 大 | — |
| 大数据可视化大屏 | ECharts / JS | 中 | — |
| 机器学习负荷预测 | Python | 中 | 用户端推荐、管理端预警 |
| 电桩模拟器 + 历史数据生成 | C++ / Python | 中 · 易漏 | 大屏、机器学习、远程重启演示 |
| 文档 / 评审 / 配置管理 | — | 中 · 评分占比高 | — |

### 1.2 Agent 加速分布：瓶颈已经转移

同样九条线，逐条评估 agent 到底能帮多少。圆点数量代表实际加速倍率（`●●●` 很高 / `●●○` 高 / `●○○` 中或更低）。关键发现是加速分布极不均匀——**原来最重的活变轻了，原来就难的活一点没变轻**。

| 工作线 | Agent 加速 | 为什么 |
| --- | --- | --- |
| PC 管理端 6 界面 | `●●●` 很高 | Qt 样板代码最多，收益最大 |
| ECharts 可视化大屏 | `●●●` 很高 | 图表配置是 agent 最擅长的活 |
| 历史数据生成器 | `●●●` 很高 | 纯脚本，几乎零风险 |
| 数据库 schema + DAO | `●●○` 高 | 结构化、模式明确；多线程加锁要人工定 |
| 业务服务层 9 个服务 | `●●○` 高 | CRUD 与流程是强项；金额精度与事务边界要人工把关 |
| 机器学习负荷预测 | `●●○` 高 | 流水线生成快；调参与结果解释仍靠人 |
| 用户端 5 界面 + 地图 | `●●○` 高（仅 UI） | 界面快；但地图 Key 与 WebEngine 依赖是环境问题 |
| Socket + pthread 框架 | `●○○` 中 | 骨架能生成，粘包/半包与线程安全必须人工复核 |
| 跨进程联调 · 环境搭建 | `●○○` 几乎为零 | agent 看不到两端运行时状态，也解决不了虚拟机与依赖冲突 |

> **本部分结论 · 驱动第四部分的分工**
>
> 原来最重的两条 UI 线（管理端、用户端）现在**相对变轻**，而联调、契约维护、异常路径测试**一点没被摊薄**。所以省下的人力必须被明确回填过去——这就是分工方案里每条线除本职模块外，还要挂一顶「第二顶帽子」的原因。
>
> 另一个直接后果：机器学习**不必再降级**为移动平均基线，五人编制也能按说明书完整实现。

---

## 第二部分 · 开工前必须冻结的四份成果物

技术选型已按「以正文文字为准」的约定确定，结论见 [4.1 技术基线](#41-技术基线)。本部分处理的是另一件事：把技术基线连同接口一起固化成四份成果物，并说明它们为什么必须在写任何业务代码之前定稿。

### 2.1 什么叫「冻结」

**冻结（freeze）是配置管理里的概念：一份成果物定稿后进入「受控状态」，不再由个人随手修改；任何改动都必须走正式的变更流程——提出、评审、由属主统一修改、通知全组。**

判断一份成果物是否已冻结，看三个条件是否同时成立：

1. 文件已合入 `main` 分支
2. 全组评审通过并且都知道它定稿了
3. 此后的修改只能由属主执行，其他人不动手

这个项目为什么必须先冻结再开工，有三层原因：

- **它们是别人开工的前提。** L3 照着协议写请求，L4 照着 schema 显示字段，L5 照着表结构取数。协议今天改一个字段名，三条线的代码明天全废——而且是在各自的 agent 会话里悄悄废掉，本人未必立刻发现。

- **五个 agent 需要同一份事实。** 单人开发时口头约定就够了；五个人各带一个 agent 时，每个 agent 只能看到你喂给它的上下文。喂的不是同一份，生成出来的就是五套互不兼容的实现——**而且每一套都能编译通过、正常流程都能跑通**，直到联调周才发现两端根本不是一套东西。这是本次编制新增的、也是最容易被低估的风险。

- **它是 SCML 的职责所在。** 说明书 1.7 要求配置负责人「按照已经定义好的规范对项目成员的开发流程及成果物进行跟踪，并对过程成果物进行配置」——冻结与变更控制正是这项职责的具体落地，也是实训过程成果物评分的考察点。

**冻结不等于永不修改。** 四周里协议一定会补命令字，schema 一定会加字段。冻结管的不是「改不改」，而是「怎么改」：不是谁需要谁就动手，而是走 [2.3](#23-冻结之后怎么改) 的流程。

### 2.2 四份冻结成果物

| 成果物 | 内容 | 属主 | 冻结时点 |
| --- | --- | --- | --- |
| `docs/db-schema.sql` | 约 10 张表：用户、管理员、充电站、充电桩、订单、钱包流水、设备日志、站点评价、系统配置、操作日志。含字段类型、约束与种子数据脚本 | L2 | W1 内 |
| `docs/protocol.md` | 4 字节长度包头 + JSON 报文体，命令字表、错误码表、鉴权与会话规则。每新增一个功能，先在协议表里占一个命令字 | L1 | W1 内 |
| `common/` | 协议结构体、错误码枚举、日志宏、时间戳格式的唯一实现。让 agent 直接引用，而不是各自重新发明一套 | L1（独占写） | W1 内 |
| `CLAUDE.md` | [4.1 技术基线](#41-技术基线)、目录归属表、编码规范、Qt 具体版本、[3.2](#32-评审清单agent-最常踩的五个坑) 的五条避坑规则。根目录一份全组共享，各模块目录下再放一份模块规则 | 全组评审后定稿 | W1 内 |

前三份是**给人和编译器看的契约**，第四份是**给 agent 看的契约**。四份缺一不可：只冻结前三份，五个 agent 依然会各写各的风格；只写 `CLAUDE.md` 而接口没定稿，agent 会照着不存在的接口生成代码。

### 2.3 冻结之后怎么改

1. **提出** —— 在群里说清楚要改什么、为什么、影响哪几条线。
2. **评审** —— 属主与受影响的担当确认，PRL 把关。
3. **修改** —— 由属主一个人改，其他人不要在自己分支上先改了再说。
4. **通知** —— 改完在群里发一条，所有受影响的人**重新同步自己 agent 的上下文**（重新读一遍 `CLAUDE.md` 与契约文件）。这一步是 agent 协作特有的，也最容易漏——人知道了不等于 agent 知道了。
5. **记录** —— SCML 在 `docs/conventions.md` 的变更记录里留一条，作为过程成果物归档。

---

## 第三部分 · Agent 开发的总约束

以下规则对五个人一视同仁，与各自负责哪条线无关。它们要解决的是同一类问题：**五个 agent 各写各的，代码都能编译、正常流程都能跑通，但拼不到一起**。

### 3.1 目录归属与三条协作铁规

五个 agent 同时改一个仓库，最典型的破坏是「顺手把别人的接口改了，好让自己这边编译通过」——对方的 agent 下一轮又会覆盖回来。用目录归属把边界写死在文件系统里，比口头约定可靠得多。

```text
ev-charging-platform/
├── CLAUDE.md               全组共享上下文 · 变更需评审
├── docs/                   ← 契约区，属主之外只读
│   ├── protocol.md         L1 独占写
│   ├── db-schema.sql       L2 独占写
│   └── conventions.md      SCML 维护（含变更记录）
├── common/                 ← 共享基座，L1 独占写
│                           协议结构体 · 错误码 · 日志宏 · 时间格式
├── server/
│   ├── net/                L1   socket · pthread 线程池 · 会话
│   ├── biz/                L2   九个业务服务
│   └── dao/                L2   SQLite 访问层
├── admin-client/           L3   PC 管理端（含 mock/ 数据源）
├── user-client/            L4   充电用户端（含 mock/ 数据源）
├── dataviz/                L5   ECharts 大屏 + JSON 快照
├── ml/                     L5   数据生成器 · 模型 · 回写脚本
└── tools/                  L1   电桩模拟器
```

**铁规 01 · 越界禁令**
agent 只能改自己目录下的文件。需要别人改接口，走群里提需求，不要自己动手改了再说——那只会引发两个 agent 来回覆盖。

**铁规 02 · 契约只读**
`docs/` 与 `common/` 对非属主只读，并在 `CLAUDE.md` 里明写这条。协议变更由属主统一改，改完按 [2.3](#23-冻结之后怎么改) 通知全组重新同步 agent 上下文。

**铁规 03 · 上下文分层**
根 `CLAUDE.md` 写全组共识（技术基线、目录归属、Qt 版本、避坑清单）；各模块目录下再放一份模块规则。开新会话前先确认 agent 确实读到了这两份。

### 3.2 评审清单：agent 最常踩的五个坑

这五条直接写进根 `CLAUDE.md`，生成时就能避开大半；剩下的靠交叉试读兜住。它们的共同点是——**代码能编译、能跑通正常流程，只在特定条件下才错**，正是 agent 最容易蒙混过关的形态。全员用 agent 时评审强度要**提高**而非降低。

1. **Socket 粘包与半包**
   agent 惯常写成「一次 `read()` 读到的就是一个完整包」。局域网小报文测试时看不出问题，一旦报文变长或连发就错乱。
   *正解：按 4 字节长度头循环读满再解析，写入同理。*

2. **SQLite 跨线程共享连接**
   线程池 + 单个 `QSqlDatabase` 连接是 agent 的默认写法，并发一上来就报错或数据错乱。
   *正解：每线程独立连接，或全局串行化访问，在 `CLAUDE.md` 里指定其中一种并统一。*

3. **金额用浮点数**
   计费、钱包余额、充值，agent 默认全部用 `double`。演示时看不出，对账和累计营收永远差几分钱。
   *正解：一律用整数「分」存储与计算，只在显示层除 100。*

4. **网络线程直接改 UI 控件**
   收到服务端推送后直接 `setText()`，Qt 下会随机崩溃且难复现。
   *正解：跨线程一律走信号槽 + QueuedConnection，这条要同时写进 L3 和 L4 的模块规则。*

5. **Qt5 / Qt6 API 混用**
   训练数据里 Qt5 用法占多数，agent 会在 Qt6 工程里写出 `QRegExp`、旧的 QChart 模块名与已废弃的信号槽写法。
   *正解：在 `CLAUDE.md` 顶部写死 Qt 具体版本号与 `.pro` 模块清单。*

### 3.3 个人纪律：三条

前两节约束的是代码，这一节约束的是人。实训的评分对象不只是跑起来的系统，还有过程成果物与个人贡献——而这三条恰好是 agent 替不了的部分。

1. **不读懂不提交。** 这是本次编制的**头号风险**。评审和答辩会问「这段为什么这么写」，agent 生成完直接提交、自己没读过，当场就露。每人对自己模块的关键设计——线程模型、加锁策略、计费口径——必须能脱稿讲。

2. **小步提交，信息写人话。** 一次提交两千行、信息写「完成管理端」，既难评审也难体现过程贡献。按功能点切分提交，说清这次解决了什么问题，让提交历史本身成为过程成果物。

3. **设计决策留人工痕迹。** 为什么用整数分记账、为什么线程池用 pthread、大屏为什么不走服务端——这些判断写进设计文档，是 agent 生成不出来的部分，也正是评分最看重的地方。

---

## 第四部分 · 分工方案

九条工作线归并为五条，一人一线。**每条线除本职模块外还挂一顶「第二顶帽子」**——那是 1.2 节里 agent 省下的时间的指定去处；不明确指定，这些时间就会消失在「我模块已经写完了」里，而联调周没人扛。

### 4.1 技术基线

按「以说明书正文文字说明为准」的约定确定，全部写入根 `CLAUDE.md` 顶部，作为五个 agent 的共同前提。

| 技术项 | 结论 | 依据 | 归属 |
| --- | --- | --- | --- |
| 业务数据存储 | **QSQLite 单一主库**，头像与站点图片存文件路径 | 1.6 关键技术 | L2 |
| 网络通信 | **自定义 TCP Socket**，4 字节长度包头 + JSON 报文体 | 1.6 关键技术 | L1 定义，L3 / L4 接入 |
| 并发模型 | **pthread 线程池**，服务端网络层不用 QThread 替代 | 1.6 + 1.2 项目目标 | L1 |
| 客户端 / 服务端 UI | **Qt 框架**，版本号在 `CLAUDE.md` 中写死 | 1.5 + 1.6 | L3 / L4 |
| 运行环境 | Ubuntu 22.04+ / Qt Creator 6.2+ | 1.5 开发环境 | 全组 |
| 大屏取数 | L5 直接只读 SQLite 导出 **JSON 快照**，大屏轮询静态文件 | ⚠️ 正文未规定，取最轻方案 | L5 |

> **关于最后一行**　说明书正文只规定了「网络通信由 Socket 编程实现」，没有规定 Web 大屏如何取数，而浏览器无法连接自定义 TCP。既然约定不引入结构图中的 HTTP API 层，就采用最轻的做法：由 L5 的 Python 脚本只读同一个 SQLite 文件、定时导出 JSON 快照，大屏轮询静态文件。
>
> 这样服务端保持**纯 Socket**、完整命中考核点，L2 也不必额外写一套 HTTP 服务。此项是推论而非说明书原文，需在设计文档中写明理由——这正是 [3.3](#33-个人纪律三条) 第三条所说的「设计决策留人工痕迹」。

### 4.2 五条线

#### L1 · 服务端通信与并发　`TL 项目负责人 · 技术最强者`

`C++` `TCP Socket` `pthread` `线程池`　—　Agent 加速 `●○○` 中

- **通信协议设计**与 `docs/protocol.md`，冻结命令字与错误码
- TCP 服务器主框架、连接接入与会话管理
- **pthread 线程池**（说明书点名的考核点，不得用 QThread 替代）
- 设备控制服务：远程重启指令下发
- 电桩模拟器：模拟设备侧连接与状态上报

> **第二顶帽子 · 契约维护者**　`common/` 与 `docs/` 的唯一写入人。任何协议变更由你评审后统一改，并按 [2.3](#23-冻结之后怎么改) 通知全组同步各自 agent 的上下文。

**依赖**：无——本线是被依赖方，进度直接决定 W3 联调能否开始。
**Agent 弱项**：粘包/半包处理与共享数据加锁，agent 生成的版本大概率是错的，必须逐行读。

#### L2 · 数据存储与业务服务　`PRL 项目评审负责人`

`C++` `QSQLite` `DAO`　—　Agent 加速 `●●○` 高

- **数据库 schema** 与建库/种子数据脚本
- DAO 封装，多线程访问下的连接与加锁策略（配合 L1 的 pthread 线程池）
- 用户 / 电站 / 电桩 / 预约 / **订单 · 计费 · 钱包** / 统计 服务
- 统计服务供管理端销售业绩与电桩状态分布取数

> **第二顶帽子 · 评审组织者**　按 [3.2](#32-评审清单agent-最常踩的五个坑) 的清单组织每周一次交叉试读，重点样本是计费与订单——业务最密集，也最容易被 agent 写出看起来对的错逻辑。

**依赖**：L1 的命令字定义；schema 反过来被所有人依赖，W1 内必须定稿。
**Agent 弱项**：金额默认会写成 `double`，事务边界会被拆散，两处都要人工指定。

#### L3 · PC 服务器管理端　`PE · 宽屏运营后台`

`Qt Widgets` `QChart` `QTableView`　—　Agent 加速 `●●●` 很高

- 管理员登录（**admin / 123456**）
- 数据总览：今日/本月/累计营收 + 近 7 日/30 日折线 + 电桩状态环形图
- 充电站管理：列表 · 站内电桩详情 · 新增电站
- 充电桩管理：列表 + **远程重启**
- 用户管理：模糊搜索 + **冻结/解冻**；订单管理

> **第二顶帽子 · 集成与构建**　一键构建与一键启动全套服务的脚本，W3 起担任**端到端联调主责**。这条线 agent 红利最大，腾出的时间正好补上 agent 最帮不上的联调。

**依赖**：仅协议文档——先接本地 Mock 数据源，W3 再换真实链路。
**提示**：界面数量最多，但也是最适合交给 agent 批量产出的一条。

#### L4 · 充电用户端　`PE · 移动端交互模拟`

`Qt Widgets` `QWebEngineView` `腾讯地图 API`　—　Agent 加速 `●●○` 高

- 手机号免密登录，首次自动注册（昵称取后 4 位）
- 附近充电站：区域/地址定位 → 地理编码 → 按距离排序
- **一键导航**：WebEngine 加载腾讯地图，驾车/步行路线
- 个人中心：头像上传、昵称修改、余额充值
- 充电全流程：**未结算订单拦截** → 预约 → 充电 → 计费 → 结算

> **第二顶帽子 · 测试与回归**　维护全组的测试用例集与回归清单。agent 写的功能路径通常没问题，**异常路径几乎必漏**：余额不足、账号冻结、断网、重复提交，这些要靠人列出来。

**最高优先风险**：WebEngine 在 Ubuntu 下的依赖安装与腾讯地图 Key 申请，agent 完全帮不上忙，必须 W1 第一天就动手，不要留到 W3。

#### L5 · 数据端：可视化大屏 + 机器学习　`SCML 项目配置负责人`

`Python` `pandas` `时序模型` `ECharts` `SQLite 只读` `天气 API`　—　Agent 加速 `●●●` 很高

- **历史充电数据生成器**：时段、时长、电量、天气、节假日特征——这是大屏和模型共同的燃料，W1 就要交付
- 特征工程与时序建模，预测 **1h / 6h / 24h** 负荷、空闲桩数、高峰时段；预测结果回写数据库
- **大屏数据源**：只读 SQLite 定时导出 JSON 快照，由本地静态服务托管，大屏轮询刷新（见 [4.1](#41-技术基线) 末行）
- ECharts 大屏：营收趋势、电桩运行、充电负荷、用户行为、站点排行，分辨率适配
- 输出用户端「低拥堵站点推荐」与管理端「负荷预警」

> **第二顶帽子 · 配置管理与文档归档**　按规范跟踪各成员的过程成果物，维护 `docs/conventions.md` 的变更记录，提前学习下阶段模板并向其他担当展开。这条线技术栈连贯（Python 造数 → 建模 → 出 JSON → ECharts 展示）且 agent 红利最满，因此最有余量承担配置职责。

**依赖**：L2 的表结构（只读，不写业务表；预测结果写入独立的预测表）。
**本次编制的变化**：若没有 agent，五人编制通常要把机器学习降级为移动平均基线；**现在不需要降级**，可按说明书要求完整实现，并把故障诊断、智能风控作为加分项。

### 4.3 管理角色如何叠加

说明书 1.7 要求 PM / TL / PRL / SCML 四个角色，其中三个兼职。关键是把管理职责挂到**与该职责天然同频**的那条线上，而不是随机分配。

| 角色 | 担当 | 为什么是这条线 |
| --- | --- | --- |
| PM | 指导教师 / 组长（专职） | 说明书要求专职，负责阶段启动前把握进度与方向，不承担编码任务 |
| TL | **L1 通信与并发** | 服务端是全组依赖中心；同时作为契约维护者，天然掌握各端的真实接入进度 |
| PRL | **L2 数据与业务服务** | 评审要通读各端代码；业务层同时对接两个客户端，最了解各方调用方式。全员用 agent 时这个角色的权重显著上升 |
| SCML | **L5 数据端** | 与 Qt 主线耦合最松、agent 红利最满、时间弹性最大，能腾出手做冻结跟踪与模板展开 |
| PE | **L3 管理端 · L4 用户端** | 纯开发担当，但各自另挂集成主责与测试主责，接住 agent 省下的时间 |

### 4.4 四周任务矩阵

节奏相比无 agent 的排法做了两处调整：**W2 开发周压缩**（agent 红利在这里兑现），**W3 联调周加重并提前启动**（agent 帮不上，只能靠人和时间）。W4 留出加分项空间。

| 工作线 | W1 契约与环境 | W2 单机跑通 | W3 联调周 | W4 加分与验收 |
| --- | --- | --- | --- | --- |
| **全组** | 需求评审 · 四项冻结 · `CLAUDE.md` 定稿 | 中期同步 · 交叉试读彼此代码 | 每日联调例会 | 同行评审 · 文档归档 · 答辩演练 |
| **L1** 通信与并发 | 协议定稿 + `common/` 基座 + 服务端骨架 | pthread 线程池 + 会话；粘包处理逐行人工复核 | 全端联调 + 电桩模拟器接入 | 断线重连 · 压测 · 错误码补全 |
| **L2** 数据与业务 | schema 定稿 + 建库种子脚本 | DAO + 用户/电站/电桩/订单/计费 | 统计服务收口 + 组织交叉评审 | 事务与加锁复查 · 金额精度审计 |
| **L3** PC 管理端 | 界面原型 + 登录 + 一键构建脚本 | 六个界面全量产出（agent 红利兑现） | 换真实链路 · **端到端集成主责** | 交互打磨 · 异常提示 · 演示动线 |
| **L4** 充电用户端 | **WebEngine 依赖 + 地图 Key 打通**（第一优先） | 五个界面 + 充电全流程 | 导航联调 + 回归用例集成型 | 异常路径回归：余额不足 · 冻结 · 未结算拦截 |
| **L5** 数据端 | 数据生成器交付 + 大屏布局（假数据） | 特征工程 + 预测模型 + ECharts 全量图表 | 接真库导出 JSON 快照 + 预测结果回写两端 | 精度评估 · 加分项 · 配置检查与归档 |

> **排期提示 · 按里程碑而非人天**
>
> 全员用 agent 后进度会变得**极不均匀**——有人两天写完整个模块，有人卡在 WebEngine 依赖上三天。这种差异被 agent 放大了，所以不要按人天平摊四周，而是**在 W1 结束、W3 开始各做一次余量重分配**：谁先跑完本职模块，谁就提前接手联调或补测试用例。
>
> 另外每条线在 W1 都必须交出「能跑的空壳」——能启动、能连上、能显示假数据即可，第一次运行绝不能拖到第三周。
