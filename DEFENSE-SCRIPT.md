# 答辩 PPT 大纲与讲稿

> 电动汽车充电桩管理平台 · 课程设计答辩
> **总时长 10:00　7 个 Part / 约 24 页　讲稿约 2200 字　建议语速 220 字/分钟**
> 按指导教师给定的七段框架组织。演示部分采用**分镜截图**，不用录屏 —— 节奏由主讲人按空格控制。

## 时间预算

| Part | 内容 | 页数 | 起止 | 时长 | 讲稿字数 |
| --- | --- | --- | --- | --- | --- |
| 1 | 封面 | 1 | 00:00–00:20 | 20s | ≈70 |
| 2 | 人员具体分工 | 1 | 00:20–01:00 | 40s | ≈145 |
| 3 | 项目背景与整体功能 | 2 | 01:00–02:00 | 60s | ≈220 |
| 4 | 项目中使用的技术 | 2 | 02:00–03:20 | 80s | ≈295 |
| **5** | **核心界面截图展示** | **16** | 03:20–07:50 | **270s** | ≈990 |
| 6 | 问题怎么解决 · 技能提升 | 2 | 07:50–09:10 | 80s | ≈295 |
| 7 | 项目心得 · 难点 · 致谢 | 1 | 09:10–10:00 | 50s | ≈185 |

Part 5 占 270 秒，是全场重心；其余六个 Part 合计 330 秒。**页数多不等于时间长** —— Part 5 的 16 页是分镜截图，平均每页 17 秒。

## 讲稿里的翻页标记

讲稿中的 `⟨切 A-02⟩` 表示**在此处按一次空格翻到下一张截图**。这是截图方案替代录屏的关键：节奏由你控制，讲快讲慢都不会脱节。

## 前置确认

**功能齐了。** 截至 2026-09-09，Part 5 涉及的全部链路都能真实演示：充电三步（1203/1204/1205）、
远程重启（2112 → 9003）、负荷预测（2305）、碳排放报告（3740–3747）均已注册并跑通。
拍截图前按 [docs/RUNBOOK.md](docs/RUNBOOK.md) 起好服务端、模拟器与大屏，数据库用演示库
（6 站 / 24 桩 / 8000+ 订单），不要用空库。

---

# Part 1 · 封面

**00:00–00:20　20 秒　1 页**

### 画面

- 项目全称：**基于 Linux + Qt 的电动汽车充电桩管理平台**
- 五位组员姓名（按 L1–L5 顺序横排，或居中两行排布）
- 指导教师姓名、班级、答辩日期
- 背景可用大屏截图做低透明度衬底，不要盖住文字

### 讲稿

> 各位老师好。我们小组的课题是「基于 Linux 和 Qt 的电动汽车充电桩管理平台」，我是主讲人某某，这是我们组的五位成员。下面由我代表小组汇报。

*≈68 字 · 目标 20 秒*

---

# Part 2 · 人员具体分工

**00:20–01:00　40 秒　1 页**

### 画面

一张五行分工表，每行：**姓名 · 工作线 · 负责模块 · 第二职责**

| 姓名 | 工作线 | 负责模块 | 第二职责 |
| --- | --- | --- | --- |
| （L1） | 服务端通信与并发 | `server/net/`、`common/`、电桩模拟器 | 契约维护者 |
| （L2） | 数据存储与业务服务 | `server/biz/`、`server/dao/` | 评审组织者 |
| （L3） | PC 服务器管理端 | `admin-client/` | 集成与构建 |
| （L4） | 充电用户端 | `user-client/` | 测试与回归 |
| （L5） | 数据可视化与机器学习 | `dataviz/`、`ml/` | 配置管理与文档归档 |

> 每行右侧可加一个极简进度条或「已完成」标记，但**不要写百分比**，容易被追问。

### 讲稿

> 先说分工。我们五个人分五条工作线并行：一号线负责服务端的通信与并发，同时兼任协议契约的维护；二号线负责数据库与九个业务服务；三号线做 PC 管理端，兼任集成与构建；四号线做充电用户端，兼任测试与回归；五号线做数据可视化和机器学习，兼任配置与文档归档。
>
> 之所以每人还挂一顶「第二顶帽子」，是因为联调、契约维护和异常路径测试这些活没人认领的话，最后就会没人干。

*≈152 字 · 目标 40 秒*

---

# Part 3 · 项目背景与整体功能

**01:00–02:00　60 秒　2 页**

## 3-1　项目背景

### 画面

左右分栏。左边三行痛点，右边一句话点题：

- **运营方**：桩的状态、订单计费、用户管理分散，缺乏统一后台
- **用户侧**：找桩靠猜，不知道哪个站有空位，也不知道会不会排队
- **数据侧**：历史运营数据沉在库里，没有被用来预测和决策

→ 本项目做一套**两个客户端 + 一个服务端 + 一条数据链路**的完整管理平台。

## 3-2　整体功能全景

### 画面

一张功能树（脑图式），四个分支：

- **充电用户端**：手机号免密登录 · 附近充电站 · 一键导航 · 个人中心 · 充电全流程
- **PC 管理端**：数据总览 · 电站管理 · 电桩管理（含远程重启）· 用户管理 · 订单管理 · 负荷预测
- **业务服务端**：Socket 通信 · 线程池并发 · 九个业务服务 · SQLite 存储
- **数据端**：负荷预测模型 · ECharts 大屏 · 碳减排报告

> 建议把说明书明文要求的功能点用统一符号标出（如 ★），一眼能看出覆盖度。

### 讲稿

> 先说背景。现在充电桩数量增长很快，但运营方缺一套统一的后台：桩是什么状态、订单怎么计费、用户怎么管理，这些是散的；用户这边找桩基本靠猜，不知道哪个站有空位；而历史运营数据一直沉在数据库里，没有被用起来。
>
> ⟨切 3-2⟩ 所以我们做了这样一套平台，四个部分。充电用户端有免密登录、附近找桩、一键导航、个人中心和完整的充电流程；PC 管理端有数据总览、电站、电桩、用户、订单五大管理模块，还有负荷预测；中间是多线程业务服务端；右边是数据端，负责负荷预测模型、数据大屏和碳减排报告。带星号的是说明书明文要求的功能点，我们全部覆盖了。

*≈237 字 · 目标 60 秒*

---

# Part 4 · 项目中使用的技术

**02:00–03:20　80 秒　2 页**

## 4-1　技术选型与系统架构

### 画面

一张架构图，**把选型结论直接标注在对应模块上**，不另开表格：

- **左**：三个客户端（管理端 / 用户端 / 电桩模拟器），三条线汇聚到中间，线上标注「TCP · 4 字节大端长度头 + UTF-8 JSON」
- **中**：服务端方框内部三层 —— 网络层（标注 **POSIX 原生 socket**）／ pthread 线程池（标注 **非 QThread**）／ DAO 层，下方连 SQLite 圆柱（标注 **QSQLite 单一主库**）
- **右**：Python 数据侧，一条**只读**虚线从 SQLite 引出，经特征工程与模型，一条实线写回 `t_load_forecast`，另一条导出 `snapshot.json` 供大屏轮询
- 图底一行：`Ubuntu 22.04 · C++17 · Qt 6.2.4 · 金额一律 qint64 整数「分」`
- 把 POSIX socket、pthread、QSQLite 三处用同一种醒目底色标出

## 4-2　技术亮点：并发模型与粘包处理

### 画面

- **左半**：线程模型图 —— 主线程（Qt 事件循环，只做 accept）→ epoll IO 线程（统一收发 + 切帧）→ pthread 线程池（N 个工作线程跑业务），箭头标注「投递完整报文」
- **右半**：两段代码截图 —— `server/net/epoll_loop.cpp` 事件循环主体、`common/frame.h` 的 `FrameParser` 切帧逻辑
- **底部一行**：`每线程独立 SQLite 连接 · 异常在分发层兜底 · 发送队列 + EPOLLOUT`

### 讲稿

> 技术选型这块，我把结论直接标在架构图上了。客户端和服务端之间是自定义的 TCP 协议，四字节大端长度头加 UTF-8 的 JSON 体。服务端网络层用的是 POSIX 原生 socket，不是 Qt 封装的 QTcpServer；并发用 pthread 线程池，不是 QThread；数据存 QSQLite 单一主库。这三处是说明书正文明确要求的。右边数据侧的 Python 只读同一个库，导出 JSON 快照给大屏轮询 —— 大屏不连服务端，因为浏览器连不上我们自定义的 TCP，单开 HTTP 又会破坏「纯 Socket」这个考核点。
>
> ⟨切 4-2⟩ 具体到并发模型：主线程跑事件循环只负责 accept，连接交给一个独立的 epoll IO 线程统一收发，切出完整报文后投递给线程池执行业务。TCP 是字节流没有消息边界，所以收发一律走统一的帧编解码器，粘包和半包在 IO 线程里处理干净，业务代码永远只看到完整的一帧。另外每个线程持有独立的数据库连接，绝不跨线程共享。

*≈318 字 · 目标 80 秒。偏慢就删掉「这三处是说明书正文明确要求的」一句。*

---

# Part 5 · 核心界面截图展示

**03:20–07:50　270 秒　16 页**

> 全场重心。每张截图单独一页，按空格推进，平均 17 秒一张。**截图规范见文末**，那一节直接决定观感成败。

## 5-A　充电用户端（7 张 · 110 秒）

| # | 截图内容 | 标注要求 |
| --- | --- | --- |
| A-01 | 登录页，输入一个库里不存在的 11 位手机号 | 红框圈手机号输入框 |
| A-02 | 「我的」页：昵称自动生成为「用户」+ 后四位、灰色默认头像 | 圈昵称与头像，**局部放大** |
| A-03 | 「附近电桩」列表：站名、距离、空闲桩数 | 圈距离列，箭头标「按距离升序」 |
| A-04 | 「导航」页：QWebEngineView 加载腾讯地图，驾车路线 | 圈路线 |
| **A-05** | **未完成订单拦截弹窗** | **弹窗放大铺满整页**，角落放缩小的主界面做位置参照 |
| A-06 | 充电中：实时计时 + 累计电量 | 圈这两个数 |
| A-07 | 结算页：电量 / 金额 / 余额扣减 | 三处圈出，右下角贴「我的」页余额变化小图做对照 |

### 讲稿

> 先看用户端。用户端模拟手机交互，登录用手机号免密。这里我输入的是一个数据库里不存在的号码。⟨切 A-02⟩ 系统直接自动注册了，昵称取「用户」加后四位，给一个默认灰头像，这是说明书要求的行为。
>
> ⟨切 A-03⟩ 进来是「附近电桩」，按定位算出距离，升序排列，每行显示站名、距离和空闲桩数。⟨切 A-04⟩ 选一个站点点一键导航，这里嵌的是腾讯地图，能规划出驾车路线。
>
> ⟨切 A-05⟩ 接下来是关键的一步。我点「充电」页的时候，系统把我拦住了，提示有未完成订单并强制跳转到结算页。这是说明书明文要求的：进入充电页之前必须校验未完成订单。
>
> ⟨切 A-06⟩ 把旧单结掉之后重新进来，选一台空闲的快充桩，预约、开始充电。界面上是实时计时和累计电量，服务端按秒计费。⟨切 A-07⟩ 结束充电进入结算，显示本次电量、金额和扣款后的余额；右下角是「我的」页，余额已经同步变了。整条链路是预约、充电、计费、结算四步闭环。

*≈418 字 · 目标 110 秒*

## 5-B　PC 服务器管理端（6 张 · 100 秒）

截图内的数据是**真实数据**：6 个站点、24 台桩、8292 笔订单，跨 2026-07-07 至 09-04，累计已结算营收约 53.9 万元。

| # | 截图内容 | 标注要求 |
| --- | --- | --- |
| B-01 | 数据总览整页：今日/本月/累计营收 + 趋势 + 电桩状态分布 | 圈三个营收指标卡 |
| B-02 | 营收趋势「近 7 日 / 近 30 日」**上下并排对照** | 标「同一位置切换」 |
| B-03 | 电站列表 + 站内详情**左右并排** | — |
| **B-04** | **远程重启合成图**：左管理端点击瞬间、右电桩模拟器日志 | 中间加箭头，标 `2112 → 9003` |
| B-05 | 用户管理：模糊搜索前后 + 冻结状态变化对照 | 圈状态列 |
| B-06 | 订单管理分页 | — |

### 讲稿

> 管理端用默认账号 admin 登录后是数据总览：今日、本月、累计三个营收指标，下面是营收趋势，右边是电桩状态分布，在用、闲置、故障三种状态及占比。这里的数字全部来自真实数据库，六个站点、二十四台桩、八千多笔订单，跨两个月，累计营收五十三万多元。⟨切 B-02⟩ 趋势图支持近七日和近三十日切换。
>
> ⟨切 B-03⟩ 电站管理是列表加站内详情，也支持新增电站。
>
> ⟨切 B-04⟩ 这一张是远程重启。左边是管理端点击重启按钮的瞬间，右边是电桩模拟器的终端日志，它收到了服务端下发的重启指令。链路是管理端发 2112 命令、服务端查在线设备连接、再用 9003 把指令推给设备。
>
> ⟨切 B-05⟩ 用户管理支持手机号模糊搜索，输入前三位就能筛出来；选中用户点冻结，状态立刻变化，被冻结用户的会话会被服务端踢掉。⟨切 B-06⟩ 最后是订单管理，支持分页。

*≈372 字 · 目标 100 秒*

## 5-C　数据端：预测 · 大屏 · 碳排放（3 张 · 60 秒）

| # | 截图内容 | 标注要求 |
| --- | --- | --- |
| C-01 | 管理端「负荷预测与预警」页 + 角落压一张精简评估表 | 表内容：`1h 负荷 MAE 28.19 / 基线 31.04 / +9.2%`、`1h 会话数 0.459 / 0.667 / +31.1%` |
| C-02 | `dataviz/index.html` 大屏整页（深色底、七块图表） | 在「全网充电负荷」图上加箭头标「左半实测 / 右半模型预测」 |
| C-03 | 碳排放报告页 + 碳排放大屏 `carbon.html` 并排 | 保留页脚免责声明那行字 |

### 讲稿

> 数据端有三块。第一块是负荷预测，说明书要求预测未来 1 小时、6 小时、24 小时的充电负荷、空闲桩数和高峰时段。我们用的是特征工程加梯度提升回归树，三个周期各训一个模型。评估上严格按时间切分训练集和测试集，绝不随机切 —— 随机切会把未来样本混进训练集，指标会虚高。最终 1 小时负荷比基线好 9.2%，并发会话数好 31.1%。
>
> ⟨切 C-02⟩ 第二块是数据大屏，七块图表。左上是全网充电负荷，做成双层：左半段实测、右半段预测，接起来能看出趋势是否延续。
>
> ⟨切 C-03⟩ 第三块是我们额外做的碳减排报告，把已结算订单的电量按峰平谷分摊，乘以电网排放因子，算出每站每天的碳排放和排放强度，可以导出 CSV 和 HTML。全流程整数运算，分摊用最大余数法，保证三段加起来严格等于总量。页面上也注明了这是课程项目估算，不是认证碳数据。

*≈363 字 · 目标 60 秒 —— 这一段偏满，若前面超时，删掉评估方法那两句和碳排放的整数运算细节。*

---

# Part 6 · 问题是怎么解决的 · 技能提升

**07:50–09:10　80 秒　2 页**

> **与 Part 7 的分工**：这里只讲**技术层面**的具体问题，Part 7 讲**协作与管理层面**的难点。两页不要重复。

## 6-1　三个技术难点与解决方案

### 画面

三栏，每栏「问题 → 现象 → 解决」三行，配一小段代码或示意图：

| 问题 | 现象 | 解决 |
| --- | --- | --- |
| **TCP 粘包与半包** | 一次 `read()` 拿到的不一定是一个完整包，可能是半个，也可能是两个半 | 自定 4 字节大端长度头，`FrameParser` 统一缓存与切帧，业务层永远只看到完整的一帧 |
| **多线程下的数据库连接** | `QSqlDatabase` 跨线程共享会崩，直接加大锁又会把并发退化成串行 | `threadDb()` 按线程 id 生成独立连接，各线程互不干扰；SQL 一律参数绑定 |
| **服务端关不掉** | 信号处理函数里直接 `stop()`，要加锁还要 `pthread_join`，工作线程卡在 `recv` 上就永远 join 不回来，进程锁死 | 信号处理里只往 self-pipe 写一个字节唤醒 accept 循环，真正的停止流程回到主线程做 |

> 第三个问题是真实踩过的坑，讲出来比讲教科书上的知识点更有说服力。备选还有一个：**大屏取数与「纯 Socket」架构冲突** —— 浏览器连不上自定义 TCP，为大屏单开 HTTP 又破坏考核点，最终决定让数据侧只读导出 JSON 快照，两边解耦。四选三讲。

## 6-2　技能提升

### 画面

左右两栏，各 4–5 条：

- **硬技能**：POSIX socket 与 epoll 事件驱动模型 · pthread 同步原语（互斥量、条件变量、读写锁）· SQLite 事务与并发控制 · Qt6 Widgets 与跨线程信号槽 · 时序预测建模与评估方法
- **软技能**：接口契约先行、冻结后按流程变更 · 跨模块协作与代码交叉评审 · 按里程碑而非人天排期 · 把踩过的坑写成裁决文档而不是留在记忆里

### 讲稿

> 这一页讲我们踩过的三个坑。第一个是 TCP 粘包和半包，一次读上来的不一定是一个完整包，我们自定了四字节长度头，用统一的帧解析器缓存和切帧，让业务层永远只看到完整的一帧。第二个是多线程下的数据库连接，跨线程共享会崩，加大锁又会让并发退化成串行，我们的做法是按线程 id 生成独立连接。第三个是服务端关不掉：一开始在信号处理函数里直接调停止，它要加锁还要 join，工作线程卡在 recv 上就永远回不来，进程直接锁死；后来改成信号里只写一个字节唤醒主循环，真正的停止流程回主线程做。
>
> ⟨切 6-2⟩ 收获上，硬技能是 epoll 事件驱动、pthread 的几种同步原语、SQLite 的事务与并发、Qt 的跨线程信号槽，还有时序预测的建模和评估方法。软技能上体会最深的是接口契约要先定下来并冻结，以及把踩过的坑写成文档，而不是留在某个人的记忆里。

*≈352 字 · 目标 80 秒。偏慢就把三个坑压缩成两个，删掉数据库连接那一条。*

---

# Part 7 · 项目心得 · 难点 · 致谢

**09:10–10:00　50 秒　1 页**

> 留 10 秒缓冲，不要掐着 10:00 结束。

### 画面

三段式，上中下或左中右：

- **项目难点（协作层面）**：五人并行，被依赖的工作线一旦停滞，下游三条线同时受阻 · 各线进度差异大，先跑完的人要主动接联调和补测试
- **项目心得**：先定契约再写代码，冻结文件非属主只读 · 小步提交，一个功能点一次 · 口径写下来，不靠记忆
- **致谢**：感谢指导教师、感谢同组成员

### 讲稿

> 最后说说难点和心得。技术难点刚才讲过了，这里说协作上的。五个人并行开发，最大的难点其实不是写代码，而是依赖关系 —— 被依赖的那条线一旦停下来，下游可能同时堵住三条线。我们的应对是把协议文档、错误码、数据库表结构这些跨模块的东西提前冻结，非属主只读，要改必须走变更流程；同时按里程碑而不是按人天排期，谁先跑完本职模块，谁就提前去接联调、补测试用例。
>
> 心得就是两句话：契约先行，以及把口径写下来而不是靠记忆。最后，感谢指导教师这一学期的指导，也感谢组里几位成员的配合。我的汇报到这里，请各位老师批评指正，谢谢。

*≈261 字 · 目标 50 秒 —— 偏满，若前面已超时，删掉「同时按里程碑……补测试用例」一句。*

---

## 截图规范

这一节直接决定 Part 5 的观感，比截图内容本身更重要。

- **窗口尺寸全程固定。** 截图前把客户端窗口调好就别再拖动。连续翻页时窗口边界钉在同一位置，画面不会左右乱跳 —— **这一条最影响观感**。
- **全部在同一台机器、同一缩放比例下截**，关掉系统通知，清空桌面，浏览器隐藏地址栏。
- **光标不要入镜**，改用统一样式的箭头做标注。
- **一张图只圈一处。** 要讲两件事就拆成两张，不要在一张图上画三个红框。
- **小字必须局部放大。** 弹窗文案、终端日志行，投影上 12px 的字看不清，做一个放大气泡引出来。
- **命名规范**：`A-01-登录.png` … `C-03-碳排放.png`。替换单张不影响其他，功能后补时改动最小。
- **PPT 切换用无动画或极快淡入**，不要花哨转场。每页顶部放一条极细的步骤轨（如 ①–⑦，当前步高亮），观众始终知道走到哪一步。

## 上台前检查清单

- [ ] 16 张截图全部就位，命名统一，窗口尺寸一致
- [ ] 截图在**投影仪上**试过一次，管理端表格文字看得清（别只在笔记本上看）
- [ ] 截图时数据库是**演示库**：6 站 / 24 桩 / 8000+ 订单，不是空库
- [ ] A-05 的拦截弹窗已做局部放大处理
- [ ] B-04 的双窗口合成图已加箭头与命令字标注
- [ ] Part 5 的翻页标记 `⟨切 X-XX⟩` 已誊到主讲人的备注页或提词卡上
- [ ] 完整走过一遍计时，总长控制在 9:30 以内（留 30 秒余量）
- [ ] 准备好三个高频追问的答案：线程池怎么退出、粘包怎么处理、金额为什么不用 `double`

---

*讲稿字数按 220 字/分钟配平；实际语速因人而异，务必掐表试讲一遍再定裁剪顺序。*
